// Auto-generated by scripts/build-theory.mjs
// 30개 SQLD 이론 챕터의 마크다운 본문.
// 직접 편집 금지 — 원본 md 파일(C:/Users/hwsyg/OneDrive/문서/Claude/Projects/SQLD 이론) 수정 후 `node scripts/build-theory.mjs` 재실행.

export const THEORY_MD: Record<string, string> = {
  c111: `# 1-1-1. 데이터 모델의 이해

## [핵심 요약]

> 데이터 모델이란 현실의 정보를 데이터베이스(DB)에 담기 위해 그린 **설계 도면**이다.

회원·상품·주문 같은 정보를 어떻게 저장하고 연결할지 미리 그려보는 작업이다. 정보의 덩어리는 [엔터티](./1-1-2_엔터티.md), 세부 항목은 [속성](./1-1-3_속성.md), 엔터티 간 연결은 [관계](./1-1-4_관계.md)로 표현한다.

---

## [개념 도식화] 모델링 흐름

개념 → 논리 → 물리 → DB, 4단계로 점점 구체화

### 1단계 — 개념적 모델 (큰 그림)

핵심 명사(엔터티)와 그 사이 연결만. 속성·자료형은 아직

\`\`\`mermaid
erDiagram
    회원 ||--o{ 주문 : ""
    상품 ||--o{ 주문상세 : ""
    주문 ||--|{ 주문상세 : ""
\`\`\`

### 2단계 — 논리적 모델 (속성·키·정규화)

엔터티마다 속성을 채우고 PK·FK 를 정함. DBMS 와는 아직 무관한 추상 모델

\`\`\`mermaid
erDiagram
    회원 {
        string 회원ID PK
        string 이름
        string 전화번호
    }
    상품 {
        string 상품ID PK
        string 상품명
        int 가격
    }
    주문 {
        int 주문번호 PK
        string 회원ID FK
        date 주문일
    }
    주문상세 {
        int 주문번호 PK
        string 상품ID PK
        int 수량
    }
    회원 ||--o{ 주문 : ""
    주문 ||--|{ 주문상세 : ""
    상품 ||--o{ 주문상세 : ""
\`\`\`

[정규화](./1-2-1_정규화.md) (1NF → 2NF → 3NF) 도 여기서

### 3단계 — 물리적 모델 (실제 DBMS 구현)

논리 모델을 실제 DB의 **테이블** 로. 자료형·인덱스·제약조건까지 명시

\`\`\`sql
CREATE TABLE MEMBER (
    MEMBER_ID  VARCHAR2(10) PRIMARY KEY,
    NAME       VARCHAR2(50) NOT NULL,
    PHONE      VARCHAR2(20)
);

CREATE TABLE PRODUCT (
    PROD_ID    VARCHAR2(10) PRIMARY KEY,
    PROD_NAME  VARCHAR2(100) NOT NULL,
    PRICE      NUMBER(10) NOT NULL
);

CREATE TABLE ORDERS (
    ORDER_ID   NUMBER PRIMARY KEY,
    MEMBER_ID  VARCHAR2(10) REFERENCES MEMBER(MEMBER_ID),
    ORD_DATE   DATE DEFAULT SYSDATE
);

CREATE INDEX IDX_ORDERS_MEMBER ON ORDERS(MEMBER_ID);  -- 성능용 인덱스
\`\`\`

### 4단계 — 데이터베이스 (운영 중인 실제 데이터)

테이블에 데이터가 **차곡차곡 쌓인 상태**. 사용자는 SQL 로 조회·입력·수정

**MEMBER**

| MEMBER_ID | NAME | PHONE |
|---|---|---|
| M01 | 김철수 | 010-1111 |
| M02 | 이영희 | 010-2222 |

**PRODUCT**

| PROD_ID | PROD_NAME | PRICE |
|---|---|---|
| P01 | 사과 | 1,000 |
| P02 | 배 | 2,000 |

**ORDERS**

| ORDER_ID | MEMBER_ID | ORD_DATE |
|---|---|---|
| 1001 | M01 | 2026-04-01 |
| 1002 | M02 | 2026-04-05 |

### 단계 정리

| 단계 | 이름 | 결과물 | 비유 |
|---|---|---|---|
| 1 | 개념적 모델링 | 핵심 엔터티·관계 ERD (큰 그림) | 손으로 그린 스케치 |
| 2 | 논리적 모델링 | 속성·키 상세 ERD + **정규화** | 정식 건축 도면 |
| 3 | 물리적 모델링 | DBMS 별 CREATE TABLE + 인덱스·파티션 | 시공 계획서 |
| → | 운영 | 데이터가 쌓인 실제 데이터베이스 | 입주 완료된 건물 |

---

## 데이터 모델링의 3가지 관점

| 관점 | 영문 | 모델링 대상 | 핵심 질문 |
|---|---|---|---|
| 데이터 관점 | Data | 업무가 관리하는 데이터 (엔터티·속성) | 무엇을(What) 저장하지 |
| 프로세스 관점 | Process | 업무가 실제 하고 있는 일 (또는 해야 할 일) | 어떻게(How) 처리하지 |
| 데이터·프로세스 상관 관점 | Interaction | 프로세스가 데이터에 미치는 영향 (CRUD 매트릭스) | 언제(When) 어떤 데이터가 변하지 |

---

## 좋은 데이터 모델의 특징

| 특징 | 의미 |
|---|---|
| 완전성 | 업무에 필요한 데이터를 빠짐없이 담았는가 |
| 중복 배제 | 같은 데이터가 여러 곳에 흩어져 있지 않은가 |
| 업무 규칙 | 회사의 규칙이 모델에 녹아 있는가 |
| 데이터 재사용 | 다른 업무에서도 활용할 수 있는가 |
| 의사소통 | 보는 사람마다 같은 의미로 읽히는가 |
| 통합성 | 같은 데이터는 한 곳에 정의되어 있는가 |

---

## 3층 스키마 (ANSI/SPARC)

같은 DB도 보는 사람에 따라 전혀 다르게 보임. 외부 / 개념 / 내부, 세 층으로 나눠봄

### 관점 1. 외부 스키마 (External) — 각 사용자/앱이 보는 화면

같은 회원·주문 데이터, 사용자마다 보는 모습이 다름

| 누가 본다 | 화면 (View) | 보이는 컬럼 예시 |
|---|---|---|
| 김철수 (모바일앱) | "내 주문 내역" | 주문번호 · 상품명 · 금액 · 주문일 |
| 이영희 (웹) | "위시리스트" | 상품명 · 가격 · 이미지 |
| 관리자 (백오피스) | "일별 매출" | 날짜 · 총매출 · 주문건수 |

각 화면은 SQL 의 **VIEW** 또는 응용 프로그램 단에서 가공한 데이터

### 관점 2. 개념 스키마 (Conceptual) — 조직 전체가 공유하는 ERD

모든 외부 화면의 원본이 되는 **하나의 통합 설계도**. 회원·상품·주문·주문상세·결제·배송 등 전 엔터티와 관계가 한 ERD 안에

\`\`\`mermaid
erDiagram
    회원 ||--o{ 주문 : ""
    주문 ||--|{ 주문상세 : ""
    상품 ||--o{ 주문상세 : ""
    주문 ||--|| 결제 : ""
    주문 ||--o| 배송 : ""
\`\`\`

설계자(DA)가 관리하는 **전사 데이터 모델**. DB 하나당 단 하나

### 관점 3. 내부 스키마 (Internal) — 디스크 위 실제 저장 구조

개념 스키마가 **물리적으로 어떻게 저장되는가**. DBA·DBMS 의 영역

| 항목 | 예시 |
|---|---|
| 데이터 파일 | \`/oracle/data/SHOP_DATA01.dbf\` (8KB 블록 단위) |
| 인덱스 | \`IDX_ORDERS_MEMBER\` (B-Tree, 회원ID 정렬) |
| 파티션 | ORDERS 테이블을 월별로 분할 (\`P_2026_04\`, \`P_2026_05\` ...) |
| 캐시 | 자주 조회되는 회원 데이터 메모리 캐시 |

### 3층이 분리된 이유 — 데이터 독립성

\`\`\`mermaid
flowchart TB
    V1[모바일앱<br/>'내 주문 내역']
    V2[웹사이트<br/>'위시리스트']
    V3[백오피스<br/>'일별 매출']
    C[개념 스키마<br/>전체 통합 ERD]
    I[내부 스키마<br/>디스크 · 인덱스 · 파일]
    V1 --> C
    V2 --> C
    V3 --> C
    C --> I
\`\`\`

- **논리적 독립성**: 개념 스키마(ERD)가 바뀌어도 외부 스키마(앱 화면)는 영향 없음
- **물리적 독립성**: 내부 스키마(저장 위치·인덱스)가 바뀌어도 개념·외부 스키마는 영향 없음

| 스키마 | 관점 | 개수 | 누가 다룬다 |
|---|---|---|---|
| 외부 (External) | 사용자·응용 프로그램 | **여러 개** | 사용자·개발자 |
| 개념 (Conceptual) | 설계자 통합 | **1개** | DA (Data Architect) |
| 내부 (Internal) | 물리 저장 | **1개** | DBA (DB Administrator) |

### 데이터 독립성 2가지

| 종류 | 의미 |
|---|---|
| 논리적 독립성 | 개념 스키마가 변경돼도 외부 스키마(응용)는 영향 없음 |
| 물리적 독립성 | 내부 스키마가 변경돼도 개념·외부 스키마는 영향 없음 |

---

## [SQL 실전] 모델이 곧 테이블이 된다

논리 모델의 PK는 물리 모델에서 [식별자](./1-1-5_식별자.md)(PRIMARY KEY 제약)가 된다.

\`\`\`sql
-- 논리 모델의 '고객' 엔터티 → 물리 모델의 CUSTOMER 테이블
CREATE TABLE CUSTOMER (
    CUST_ID    VARCHAR2(10) PRIMARY KEY,  -- 식별자
    CUST_NAME  VARCHAR2(50) NOT NULL,     -- 속성
    PHONE      VARCHAR2(20)               -- 속성
);
\`\`\`

---

## [Before & After] 모델링이 없을 때 vs 있을 때

한 테이블에 다 욱여넣은 모양 vs 고객·주문을 분리한 모양

### Before — 모델링 없이 만든 테이블

| 주문번호 | 고객이름 | 고객전화 | 상품1 | 상품2 | 상품3 |
|---|---|---|---|---|---|
| 1 | 김철수 | 010-1111 | 사과 | 배 | NULL |
| 2 | 김철수 | 010-1111 | 포도 | NULL | NULL |

고객 정보가 중복되고, 상품 칸이 모자라면 더 못 담음

### After — 모델링 후 분리된 테이블

**CUSTOMER**

| CUST_ID | NAME | PHONE |
|---|---|---|
| C001 | 김철수 | 010-1111 |

**ORDER**

| ORD_ID | CUST_ID | PRODUCT |
|---|---|---|
| 1 | C001 | 사과 |
| 2 | C001 | 배 |
| 3 | C001 | 포도 |

중복이 사라지고 상품을 무한히 추가 가능

Before 는 "상품1·상품2·상품3" 같은 컬럼을 미리 박아둬서 주문 개수가 늘면 한계가 분명하다. 고객 정보도 주문마다 반복 저장이라 전화번호 하나 바뀌면 모든 행을 손봐야 한다. 모델링은 이걸 엔터티 분리로 푼다 — 같은 의미는 한 곳에만, 관계는 키로 잇는다.

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| 물리적 모델링이 가장 먼저다 | 개념 → 논리 → 물리 순서 |
| 데이터 모델은 코드를 위한 것 | 현실 세계를 추상화한 것 |
| 좋은 모델은 데이터 중복을 일부 허용한다 | 원칙은 중복 배제. 다만 성능을 위해 의도적으로 허용하는 반정규화는 별개 |
| 데이터 모델링 3대 관점 | 데이터 / 프로세스 / 상관 |
| 좋은 모델의 6대 특징 | 완전성 / 중복배제 / 업무규칙 / 데이터 재사용 / 의사소통 / 통합성 |

단골 함정: "물리적 모델링 단계에서 정규화를 한다" — 정규화는 **논리** 단계

---

> **30초 시험 직전 정리**
> · 모델링 순서: **개념 → 논리 → 물리** (정규화 = 논리)
> · 3가지 관점: **데이터 / 프로세스 / 데이터·프로세스 상관**
> · 좋은 모델 6요소: **완전성 · 중복배제 · 업무규칙 · 재사용성 · 의사소통 · 통합성**
> · 3층 스키마: **외부(View) / 개념(ERD) / 내부(저장)** + **논리적·물리적 독립성** 보장
`,
  c112: `# 1-1-2. 엔터티(Entity)

## [핵심 요약]

> 엔터티(Entity)란 업무에서 관리해야 할 정보의 덩어리(명사)다. 실제 테이블의 후보가 된다.

ex) 쇼핑몰이라면 **회원·상품·주문·배송** 같은 명사 하나하나가 엔터티이며, 각 엔터티의 개별 사례(예: '김철수 회원')를 **인스턴스(Instance, 한 행)** 라고 부른다.

---

## [개념 도식화] 엔터티의 구성

\`\`\`mermaid
flowchart TB
    A["엔터티 (테이블) — 정보의 덩어리<br/>예: 회원"] --> B["속성 (컬럼) — 정보의 세부 항목<br/>회원ID · 이름 · 전화번호"]
    B --> C["인스턴스 (행) — 개별 사례<br/>C001, 김철수, 010-1111<br/>C002, 이영희, 010-2222"]
\`\`\`

---

## 엔터티의 5가지 특징

| 특징 | 의미 | 예시 |
|---|---|---|
| 업무에서 필요 | 회사가 관리할 가치가 있어야 함 | 쇼핑몰의 '회원'은 필요, 회원의 '발 사이즈'는 불필요 |
| 식별 가능 | 다른 인스턴스와 구분되는 [식별자](./1-1-5_식별자.md) 존재 | 회원ID로 구별 |
| 인스턴스 집합 | 2개 이상의 인스턴스 존재 | 회원이 1명만 있으면 엔터티 X |
| 속성 보유 | 2개 이상의 [속성](./1-1-3_속성.md)을 가짐 | 회원ID + 이름 + … |
| 관계 보유 | 다른 엔터티와 [관계](./1-1-4_관계.md)가 있음 | 회원 ─ 주문 |

암기: **업·식·인·속·관**

---

## 엔터티의 분류

### 유무형에 따른 분류

| 종류 | 설명 | 예시 |
|---|---|---|
| 유형 엔터티 | 물리적으로 존재 | 상품, 사원, 매장 |
| 개념 엔터티 | 개념적으로만 존재 | 부서, 조직, 학과 |
| 사건 엔터티 | 행위로 발생 | 주문, 결제, 출입기록 |

### 발생 시점에 따른 분류

\`\`\`mermaid
flowchart LR
    A[기본 엔터티<br/>독립 존재<br/>사원·부서·고객] -->|파생| B[중심 엔터티<br/>업무 핵심<br/>주문·계약·매출]
    B -->|관계에서| C[행위 엔터티<br/>2개 이상에서 발생<br/>주문상세·출고·입출금]
\`\`\`

| 종류 | 의존성 | 예시 |
|---|---|---|
| 기본(Key) 엔터티 | 독립적 존재 가능 (다른 엔터티 없이도 성립) | 사원, 부서, 고객 |
| 중심(Main) 엔터티 | 기본 엔터티에서 파생 (업무의 주요 결과물) | 주문, 계약, 매출 |
| 행위(Action) 엔터티 | 2개 이상 엔터티의 관계에서 발생 | 주문상세, 출고, 입출금 |

---

## [SQL 실전] 엔터티는 테이블이 된다

\`\`\`sql
CREATE TABLE MEMBER (
    MEMBER_ID    VARCHAR2(10) PRIMARY KEY,  -- 식별자
    NAME         VARCHAR2(50),
    PHONE        VARCHAR2(20),
    JOIN_DATE    DATE
);
\`\`\`

---

## [Before & After] 엔터티 도출 전후

"김철수가 2026-04-01에 사과 2개를 주문했다" 같은 한 줄 사건 기록을 그대로 적어두는 방식과, 회원·상품·주문 세 엔터티로 갈라낸 방식 비교

### Before — 엔터티 구분 없이 한 덩어리

| 데이터(혼재) |
|---|
| 김철수가 2026-04-01에 사과 2개 주문 |
| 이영희가 2026-04-02에 배 1개 주문 |

### After — 엔터티 분리

**회원(MEMBER)**

| MEMBER_ID | NAME |
|---|---|
| M01 | 김철수 |
| M02 | 이영희 |

**상품(PRODUCT)**

| PROD_ID | NAME |
|---|---|
| P01 | 사과 |
| P02 | 배 |

**주문(ORDER)**

| ORD_ID | MEMBER_ID | PROD_ID | QTY | ORD_DATE |
|---|---|---|---|---|
| 1 | M01 | P01 | 2 | 2026-04-01 |
| 2 | M02 | P02 | 1 | 2026-04-02 |

한 줄에 "회원·상품·주문"이라는 서로 다른 성질의 정보가 섞여 있으면 회원 전화번호 갱신·상품 단가 변경·주문 취소가 한 행을 동시에 건드린다. 엔터티 단위로 분리하면 각 변경이 그 엔터티 안에서만 끝난다. "사람 얘기인가, 물건 얘기인가, 사건 얘기인가"를 구분해 명사로 뽑아내는 것이 엔터티 도출의 출발점.

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| 엔터티는 1개 인스턴스만 있어도 된다 | 2개 이상 필요 |
| 엔터티는 속성이 1개여도 가능하다 | 2개 이상 필요 |
| 행위 엔터티의 예 | 주문, 입출금, 출고 |
| 엔터티 명명 규칙 | 현업 용어 사용, 단수 명사, 약어 지양, 대문자 표기 권장 |

단골 보기: "엔터티는 영속적으로 존재하는 인스턴스의 집합이다" → 옳음. "인스턴스가 1개여도 무방하다" → 틀림

---

> **30초 시험 직전 정리**
> · 엔터티 5대 특징: **업·식·인·속·관** (업무에 필요 / 식별 가능 / 인스턴스 ≥ 2 / 속성 ≥ 2 / 관계 보유)
> · 유무형 분류: **유형 / 개념 / 사건**
> · 발생 시점 분류: **기본 → 중심 → 행위**
> · 명명 규칙: **단수 명사 / 현업 용어 / 약어 지양 / 대문자 권장**
`,
  c113: `# 1-1-3. 속성(Attribute)

## [핵심 요약]

> 속성(Attribute)이란 [엔터티](./1-1-2_엔터티.md)가 가지는 세부 정보 항목, 곧 컬럼이다. 엔터티를 설명하는 가장 작은 단위다.

ex) 명함 한 장에서 명함 자체는 엔터티(사람), 이름·전화번호·이메일 같은 각 항목이 속성. 이름은 더 이상 쪼개지지 않는 최소 단위

---

## [개념 도식화] 엔터티 / 인스턴스 / 속성 / 속성값

회원(MEMBER) 테이블 예시:

| ID&nbsp;(속성) | 이름&nbsp;(속성) | 전화번호&nbsp;(속성) |
|---|---|---|
| **M01** | **김철수** | **010-1111** ← 속성값(Value) |
| M02 | 이영희 | 010-2222 |

각 행 = **인스턴스(Row)** / 각 칸 = **속성값(Value)** / 각 컬럼 = **속성(Attribute)**.

---

## 속성의 3가지 분류 기준

\`\`\`mermaid
flowchart TD
    A[속성 분류 3기준] --> B[① 특성]
    A --> C[② 분해 가능 여부]
    A --> D[③ 값 개수]
    B --> B1[기본 속성]
    B --> B2[설계 속성]
    B --> B3[파생 속성]
    C --> C1[단순 속성<br/>원자]
    C --> C2[복합 속성]
    D --> D1[단일값 속성]
    D --> D2[다중값 속성<br/>→ 별도 엔터티]
\`\`\`

### 특성에 따른 분류

| 종류 | 의미 | 예시 |
|---|---|---|
| 기본 속성 | 업무에서 직접 추출된 본래의 속성 | 이름, 전화번호, 주소 |
| 설계 속성 | 업무 설계 과정에서 새로 만든 속성 | 회원ID, 상품코드 |
| 파생 속성 | 다른 속성에서 계산되어 나온 속성 | 총주문금액(=수량×단가) |

시험 단골: "주민번호로부터 구한 나이" → 파생 속성

### 분해 가능 여부

| 종류 | 의미 | 예시 |
|---|---|---|
| 단순(원자) 속성 | 더 이상 분해 불가 | 이름, 나이 |
| 복합 속성 | 여러 의미가 합쳐진 형태 — 분해해서 저장하면 활용도 ↑ | 주소(시+구+동) |

> **주의** — '복합 속성' (1-1-3) 과 '복합식별자' (1-1-5) 는 **다른 개념**.
> 복합 속성 = 한 속성을 여러 의미로 쪼갤 수 있음.
> 복합식별자 = 여러 속성을 묶어 PK로 씀.

### 속성값의 개수

| 종류 | 의미 | 예시 |
|---|---|---|
| 단일값 속성 | 하나만 가짐 | 주민번호 |
| 다중값 속성 | 여러 개 가능 → 별도 엔터티로 분리 | 전화번호(집, 휴대폰) |

---

## 속성의 명명 규칙

\`\`\`
[속성 이름 짓기 4원칙]
  1. 현업 용어 사용 — 코드가 아닌 업무 용어로
  2. 명사형 (서술식 X) — '주문수량' O, '주문은얼마인가' X
  3. 약어 자제 — 'qty' 보다 '수량'
  4. 전체 모델에서 유일 — 같은 이름이 여러 엔터티에 흩어지지 않도록
\`\`\`

| 좋은 예 | 나쁜 예 |
|---|---|
| 회원_등록일 | regdt (약어 남발) |
| 상품_가격 | 상품의가격은얼마인가 (서술식) |
| 주문_수량 | qty (축약형 모호) |

---

## 도메인(Domain) — 속성의 값 범위

도메인은 속성이 가질 수 있는 **값의 종류·범위·제약**. **데이터 타입 + 크기 + 허용값(제약조건)** 세 가지가 모두 포함됨. NOT NULL 여부도 도메인의 일부로, [NULL](./1-2-4_NULL속성의이해.md) 허용 여부는 속성마다 별도 결정

| 속성 | 도메인 (타입 · 크기 · 제약) |
|---|---|
| 성별 | CHAR(1) / { 'M', 'F' } 만 허용 |
| 나이 | NUMBER(3) / 0 ~ 150 사이 정수 |
| 이메일 | VARCHAR2(100) / @ 포함 형식 |

같은 도메인을 가진 속성은 여러 엔터티에 나와도 같은 의미로 다뤄야 함 (통합성)

---

## [SQL 실전] 속성 정의

\`\`\`sql
CREATE TABLE MEMBER (
    MEMBER_ID    VARCHAR2(10),     -- 설계 속성
    NAME         VARCHAR2(50),      -- 기본 속성
    BIRTH        DATE,              -- 기본 속성
    AGE          NUMBER GENERATED ALWAYS AS  -- 파생 속성
                 (TRUNC(MONTHS_BETWEEN(SYSDATE, BIRTH)/12)) VIRTUAL
);
\`\`\`

---

## [Before & After] 다중값 속성의 분리

회원 한 명이 휴대폰·집 전화 두 개를 동시에 가진 상황. 한 칸에 "010-1111, 02-333-4444"처럼 쉼표로 이어 붙이는 방식 vs 별도 엔터티로 빼는 방식

### Before — 다중값 속성을 한 칸에

| MEMBER_ID | NAME | PHONE |
|---|---|---|
| M01 | 김철수 | 010-1111, 02-333-4444 |

검색·변경 모두 어려움

### After — 별도 엔터티로 분리

**MEMBER**

| MEMBER_ID | NAME |
|---|---|
| M01 | 김철수 |

**MEMBER_PHONE**

| MEMBER_ID | TYPE | PHONE |
|---|---|---|
| M01 | 휴대폰 | 010-1111 |
| M01 | 집 | 02-333-4444 |

한 칸에 두 값이 들어가면 "휴대폰 번호만 검색"이나 "집 전화 변경" 같은 단순 작업도 문자열 파싱부터 해야 하고, 번호가 셋으로 늘면 컬럼 자체를 늘려야 한다. 한 속성엔 한 값만 — 한 인스턴스가 같은 속성에 여러 값을 갖는 순간 별도 엔터티 후보다. 1NF 의 원자값 원칙도 같은 맥락.

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| "주문수량 × 단가 = 주문금액"은 어떤 속성인가 | 파생 속성 |
| "주민등록번호로부터 추출한 성별"은 | 파생 속성 |
| 다중값 속성을 그대로 둔다 | 별도 엔터티로 분리(정규화) |
| 복합 속성과 단순 속성 | 주소(시·구·동) = 복합, 나이 = 단순 |
| 속성은 더 이상 쪼갤 수 없는 단위인가 | 그렇다(최소 단위) |
| 속성과 인스턴스의 관계 | 한 인스턴스는 각 속성에 단 하나의 값을 가짐 |

함정 단골: "파생 속성은 가급적 줄여야 한다" → 옳음 (데이터 일관성). 단, 성능을 위해 의도적으로 둘 수는 있음

---

> **30초 시험 직전 정리**
> · 특성 분류: **기본 / 설계 / 파생** (파생 = 다른 속성에서 계산)
> · 분해 분류: **단순(원자) / 복합** (복합 ≠ 복합식별자)
> · 값 개수 분류: **단일값 / 다중값** (다중값은 별도 엔터티로 분리)
> · 도메인 = **타입 + 크기 + 제약조건**
`,
  c114: `# 1-1-4. 관계(Relationship)

## [핵심 요약]

> 관계(Relationship)란 두 [엔터티](./1-1-2_엔터티.md)가 서로 어떻게 연결되어 있는지 표현한 것이다.

ex) 가족의 '부모 ─ 자식 : 낳다/자식이다'처럼, 데이터 모델에서도 '회원 ─ 주문 : 주문하다/주문되다', '부서 ─ 사원 : 소속하다/소속되다'와 같이 양방향 동사로 표현

---

## [개념 도식화] 관계의 4요소

\`\`\`mermaid
flowchart LR
    M[회원 M01] -->|주문하다| O[주문 O100]
    O -->|주문되다| M
\`\`\`

회원 1명 → 주문 여러 건 (1:N). 양방향 관계명을 모두 명명

| 요소 | 영문 | 설명 | 예 |
|---|---|---|---|
| 관계명 | Membership | 두 엔터티를 잇는 동사·역할명 | 주문하다 / 주문되다 |
| 관계차수 | Cardinality | 1:1, 1:N, M:N | 회원 1 : N 주문 |
| 관계선택성 | Optionality | 필수(Mandatory) / 선택(Optional) | 주문엔 반드시 회원이 있어야 하는가 |
| 관계의 방향성 | Direction | 양방향 표현 (양쪽 관계명 모두 명명) | 회원→주문 / 주문→회원 |

---

## 관계차수(Cardinality) 3가지

\`\`\`mermaid
flowchart LR
    H1[남편] -- 1 : 1 --- W1[아내]
    P[부모] -- 1 : N --- C[자식들]
    S[학생] -- M : N --- K[과목]
\`\`\`

### 1:1 관계

| 예시 | 설명 |
|---|---|
| 사원 ─ 사원증 | 사원 1명당 사원증 1개 |

### 1:N 관계

| 예시 | 설명 |
|---|---|
| 부서 ─ 사원 | 1개 부서에 N명 사원 |
| 회원 ─ 주문 | 회원 1명이 N건 주문 |

### M:N 관계

| 예시 | 설명 |
|---|---|
| 학생 ─ 과목 | 학생 M명, 과목 N개 (서로 여러 개) |

M:N 은 그대로 구현 불가. 중간에 교차 엔터티를 두어 1:N 두 개로 분해

\`\`\`mermaid
flowchart LR
    S1[학생] -- M : N --- K1[과목]
    S2[학생] -- 1 : N --> R[수강<br/>교차 엔터티]
    R -- N : 1 --> K2[과목]
\`\`\`

---

## 관계선택성(Optionality)

| 종류 | 표기 | 의미 |
|---|---|---|
| 필수(Mandatory) | ─ | 반드시 있어야 함 |
| 선택(Optional) | ○ | 있을 수도 있고 없을 수도 있음 |

\`\`\`
[회원] ──── [주문]      :  주문은 반드시 회원이 있어야 함(필수)
[회원] ──○─ [쿠폰]      :  회원이 쿠폰은 없을 수도 있음(선택)
\`\`\`

---

## 관계의 분류 — 식별 vs 비식별

\`\`\`mermaid
flowchart LR
    P[부모<br/>PK: 부서코드] ==>|실선 · 식별 관계| C1[자식<br/>PK: 부서코드 + 사원번호]
    P -.->|점선 · 비식별 관계| C2[자식<br/>PK: 사원번호<br/>FK: 부서코드]
\`\`\`

| 종류 | 자식의 PK에 부모 [식별자](./1-1-5_식별자.md)가 포함? | 표기 | 비유 |
|---|---|---|---|
| 식별 관계 | 포함됨 (강한 종속) | 실선 | 자식이 부모 없이 존재 불가 (예: 주문 ↔ 주문상세) |
| 비식별 관계 | 포함 안 됨 (약한 종속) | 점선 | 자식이 부모 없이도 존재 가능 (예: 부서 ↔ 사원) |

---

## [SQL 실전] 관계는 외래키(FK)로 구현된다

설계 단계의 관계는 SQL 단계에서 [조인](./2-1-7_조인.md)으로 활용. 관계와 조인의 연결은 [관계와 조인의 이해](./1-2-2_관계와조인의이해.md)에서

\`\`\`sql
-- 회원(부모) 1 : N 주문(자식)
CREATE TABLE MEMBER (
    MEMBER_ID  VARCHAR2(10) PRIMARY KEY,
    NAME       VARCHAR2(50)
);

CREATE TABLE ORDERS (
    ORDER_ID   NUMBER PRIMARY KEY,
    MEMBER_ID  VARCHAR2(10),  -- 외래키(관계의 구현)
    ORD_DATE   DATE,
    CONSTRAINT FK_ORDER_MEMBER
        FOREIGN KEY(MEMBER_ID) REFERENCES MEMBER(MEMBER_ID)
);
\`\`\`

---

## [Before & After] M:N 관계의 분해

학생은 여러 과목을 듣고 한 과목엔 여러 학생이 등록 — 양쪽 모두 다(多)인 M:N. 그대로 두는 시도 vs 가운데 '수강' 교차 엔터티를 끼워 1:N 두 개로 쪼갠 형태

### Before — M:N 관계는 그대로 구현 불가

\`\`\`
[학생] M ────── N [과목]
\`\`\`

| 학생 | 듣는 과목들 |
|---|---|
| 김철수 | 수학, 영어, 국어 |
| 이영희 | 영어, 과학 |

### After — 1:N + 1:N으로 분해

\`\`\`
[학생] 1 ── N [수강] N ── 1 [과목]
\`\`\`

**학생**

| STU_ID | NAME |
|---|---|
| S01 | 김철수 |
| S02 | 이영희 |

**과목**

| SUB_ID | NAME |
|---|---|
| K1 | 수학 |
| K2 | 영어 |
| K3 | 국어 |
| K4 | 과학 |

**수강(교차 엔터티)**

| STU_ID | SUB_ID |
|---|---|
| S01 | K1 |
| S01 | K2 |
| S01 | K3 |
| S02 | K2 |
| S02 | K4 |

M:N 을 그대로 두면 학생 행에 "수학, 영어, 국어" 같은 다중값 컬럼을 넣을 수밖에 없는데, 이는 1NF 위반이고 성적·수강일 같은 관계 속성도 둘 곳이 없어진다. 교차 엔터티를 두면 (S01, K1) 한 줄이 한 수강 사실을 표현하므로 관계 속성도 자연스럽게 거기에 붙는다. 양쪽이 다(多)인 관계를 만나면 반사적으로 교차 엔터티를 끼워 1:N + 1:N 으로 분해하는 것이 안전.

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| M:N 관계는 그대로 구현 가능하다 | 교차 엔터티로 1:N 두 개로 분해 |
| 식별 관계 = 점선 | 식별 관계 = 실선, 비식별 = 점선 |
| 관계의 4가지 표기 요소 | 관계명 / 차수 / 선택성 / 방향성 |
| 관계차수 3종류 | 1:1, 1:N, M:N |
| 자식 엔터티에 부모 식별자가 PK로 들어가면 | 식별 관계 |
| 부모 식별자가 일반 속성(FK)으로만 들어가면 | 비식별 관계 |

단골 트릭: "관계의 페어링(pairing)은 인스턴스 간의 개별적 연결" → 관계의 구체적 사례를 가리키는 옳은 설명

---

> **30초 시험 직전 정리**
> · 관계 4요소: **관계명 / 관계차수 / 관계선택성 / 관계방향**
> · 차수 3종: **1:1 · 1:N · M:N** (M:N은 교차 엔터티로 분해)
> · 식별 = **실선**, 비식별 = **점선**
> · 관계는 SQL 에서 **외래키(FK)** 로 구현된다
`,
  c115: `# 1-1-5. 식별자(Identifier)

## [핵심 요약]

> 식별자(Identifier)란 [엔터티](./1-1-2_엔터티.md) 안의 인스턴스를 서로 구분해 주는 유일한 표식이다. 테이블의 기본키(Primary Key)가 된다.

ex) 같은 이름의 김철수가 100명이어도 주민등록번호는 모두 다름 — 이름은 일반 속성, 주민번호는 식별자(겹치지 않는 유일값)

---

## [개념 도식화] 식별자의 위치

회원(MEMBER) 테이블 — PK 가 모든 행을 유일하게 구분:

| PK&nbsp;(주식별자) | 이름 | 전화번호 |
|---|---|---|
| **M01** | 김철수 | 010-1111 |
| **M02** | 이영희 | 010-2222 |

같은 이름이어도 PK가 다르므로 **100% 구분 가능**.

---

## 식별자의 4가지 분류 기준

\`\`\`mermaid
flowchart TD
    A[식별자] --> B[① 대표성]
    A --> C[② 생성 여부]
    A --> D[③ 속성 수]
    A --> E[④ 대체 여부]
    B --> B1[주식별자]
    B --> B2[보조식별자]
    C --> C1[내부식별자]
    C --> C2[외부식별자<br/>= FK]
    D --> D1[단일식별자]
    D --> D2[복합식별자]
    E --> E1[본질식별자]
    E --> E2[인조식별자]
\`\`\`

| 기준 | 분류 | 핵심 차이 |
|---|---|---|
| ① 대표성 | **주식별자 / 보조식별자** | 엔터티의 대표 식별자 vs 보조용 |
| ② 생성 여부 | **내부식별자 / 외부식별자** | 자기 엔터티에서 생성 vs 다른 엔터티에서 가져옴 (= FK) |
| ③ 속성 수 | **단일식별자 / 복합식별자** | 1개 속성 vs 2개 이상 결합 |
| ④ 대체 여부 | **본질식별자 / 인조식별자** | 업무 자연 도출 vs 인위적 부여 |

### 대표성

| 종류 | 의미 | 예 |
|---|---|---|
| 주식별자(Primary) | 엔터티 대표 식별자 | 회원ID |
| 보조식별자(Alternate) | 보조적 식별자 | 이메일 |

### 스스로 만든 vs 외부에서 받음

| 종류 | 의미 | 예 |
|---|---|---|
| 내부식별자 | 자기 엔터티 내에서 생성 | MEMBER 테이블의 MEMBER_ID |
| 외부식별자 | 다른 엔터티의 주식별자를 참조해 가져옴 = **외래키(FK)** | 주문 테이블의 MEMBER_ID (← 회원 PK 참조) |

### 속성의 개수

| 종류 | 의미 | 예 |
|---|---|---|
| 단일식별자 | 1개 속성 | 회원ID |
| 복합식별자 | 2개 이상 속성 결합 | (주문ID + 상품ID) → 주문상세 PK |

### 본질 vs 인조

| 종류 | 의미 | 예 |
|---|---|---|
| 본질식별자 | 업무에서 자연스럽게 도출 | 주민번호, 학번 |
| 인조식별자 | 인위적으로 만든 식별자 | 자동증가 ID, UUID |

본질·인조의 트레이드오프는 [본질식별자 vs 인조식별자](./1-2-5_본질식별자vs인조식별자.md)에서

---

## 주식별자의 4가지 조건

| 조건 | 의미 |
|---|---|
| 유일성 | 모든 인스턴스를 유일하게 구분 |
| 최소성 | 꼭 필요한 속성만으로 구성 ([정규화](./1-2-1_정규화.md)와 직결) |
| 불변성 | 한번 정해지면 거의 변하지 않음 |
| 존재성 | NULL이 될 수 없음 |

암기: **유·최·불·존**

---

## [SQL 실전] PK / FK 정의

\`\`\`sql
-- 회원: 단일 PK
CREATE TABLE MEMBER (
    MEMBER_ID  VARCHAR2(10) PRIMARY KEY,   -- 주식별자
    EMAIL      VARCHAR2(100) UNIQUE,       -- 보조식별자
    NAME       VARCHAR2(50) NOT NULL
);

-- 주문상세: 복합 PK
CREATE TABLE ORDER_DETAIL (
    ORDER_ID   NUMBER,
    PROD_ID    VARCHAR2(10),
    QTY        NUMBER,
    CONSTRAINT PK_ORD_DTL PRIMARY KEY (ORDER_ID, PROD_ID),  -- 복합 식별자
    CONSTRAINT FK_OD_ORD  FOREIGN KEY (ORDER_ID) REFERENCES ORDERS(ORDER_ID),
    CONSTRAINT FK_OD_PROD FOREIGN KEY (PROD_ID)  REFERENCES PRODUCT(PROD_ID)
);
\`\`\`

---

## [Before & After] 식별자를 잘못 잡으면

이름이 똑같은 김철수가 두 명 가입한 상황. 이름을 PK로 잡은 경우 vs 회원ID 를 따로 부여한 경우

### Before — 식별자가 '이름'

| NAME (PK?) | PHONE |
|---|---|
| 김철수 | 010-1111 |
| 김철수 | 010-2222 |

동명이인 등록 불가 (유일성 위반)

### After — 식별자가 '회원ID'

| MEMBER_ID (PK) | NAME | PHONE |
|---|---|---|
| M01 | 김철수 | 010-1111 |
| M02 | 김철수 | 010-2222 |

동명이인이라도 ID로 구분 가능

이름은 같은 값이 얼마든지 나올 수 있어 유일성을 보장하지 못한다. 식별자의 4대 조건(유·최·불·존) 중 가장 먼저 깨지는 게 유일성이고, 결혼·개명 등으로 변할 수 있어 불변성도 위태롭다. 업무 속성 중 자연스러운 유일값이 마땅치 않으면 회원ID 같은 인조식별자를 PK 로 두는 것이 안전.

---

## 식별자 표기

ERD 표기 — 회원 엔터티:

| 영역 | 속성 | 비고 |
|---|---|---|
| **PK 영역** | MEMBER_ID | 주식별자 (Primary) |
| 일반 속성 | NAME | |
| 일반 속성 | EMAIL | UNIQUE = 보조식별자 |
| 일반 속성 | PHONE | |

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| 주식별자 4대 조건 | 유일성 / 최소성 / 불변성 / 존재성(NOT NULL) |
| 주식별자는 NULL이 가능하다 | 불가 |
| 복합식별자는 PK가 될 수 없다 | 될 수 있다(여러 속성으로 구성된 PK) |
| 외부식별자는 다른 엔터티의 주식별자를 참조 | 옳다(= 외래키, FK) |
| 인조식별자의 단점 | 업무적 의미가 없어 데이터 품질 검증이 어려움 |
| 본질식별자가 너무 길거나 자주 변하면 | 인조식별자(대체키) 도입 고려 |

단골 트릭: "주식별자는 자주 변경되어도 된다" → 불변성 위반

---

> **30초 시험 직전 정리**
> · 4대 조건: **유·최·불·존** (유일성 / 최소성 / 불변성 / 존재성=NOT NULL)
> · 4대 분류: **대표성·생성여부·속성수·대체여부** 4가지 기준 ⟶ 8개 종류
> · 외부식별자 = **외래키(FK)** = 다른 엔터티의 PK 참조
> · 본질식별자가 너무 길거나 자주 변하면 → **인조식별자(대체키)** 도입
> · NULL 가능? → **불가** (존재성 위반)
`,
  c121: `# 1-2-1. 정규화(Normalization)

## [핵심 요약]

> 정규화(Normalization)란 데이터의 중복을 없애고 깔끔하게 정리하는 작업이다. 테이블을 단계적으로 쪼개서 이상현상을 막는다.

ex) 옷장 한 칸에 속옷·양말·셔츠·외투를 다 욱여넣으면 양말 찾기가 어렵고, 셔츠를 빼다가 다른 옷이 떨어진다. 칸을 나눠 종류별로 분리하는 것이 정규화.

---

## [개념 도식화] 정규화 단계

\`\`\`mermaid
flowchart LR
    A[비정규형<br/>중복·다중값] -->|원자값으로| B[1NF]
    B -->|부분함수 종속 제거| C[2NF]
    C -->|이행함수 종속 제거| D[3NF]
    D -->|모든 결정자 = 후보키| E[BCNF]
\`\`\`

| 단계 | 핵심 규칙 | 한줄 의미 |
|---|---|---|
| 1NF | 모든 속성이 원자값 | 한 칸에 한 값만 |
| 2NF | 부분 함수 종속 제거 | PK 일부에만 종속된 컬럼 분리 |
| 3NF | 이행 함수 종속 제거 | A→B→C 형태 분리 |
| BCNF | 모든 결정자가 후보키 | 더 엄격한 3NF |

---

## 이상현상(Anomaly) 3가지

| 종류 | 무슨 일인가 | 비유 |
|---|---|---|
| 삽입 이상 | 데이터 넣으려는데 다른 정보가 없어 못 넣음 | 학생 등록하려는데 수강과목 칸을 비울 수 없음 |
| 갱신 이상 | 한 곳만 바꿨는데 다른 곳이 안 바뀜 | 회원 주소 1곳만 수정해 데이터 불일치 |
| 삭제 이상 | 데이터 지우려다 필요한 정보까지 사라짐 | 마지막 수강 취소했는데 학생 정보까지 삭제 |

---

## 1NF — 원자값 만들기

### Before(비정규형)

| 학생 | 수강과목 |
|---|---|
| 김철수 | 수학, 영어, 국어 |

### After(1NF)

| 학생 | 수강과목 |
|---|---|
| 김철수 | 수학 |
| 김철수 | 영어 |
| 김철수 | 국어 |

모든 칸이 하나의 값을 가짐

---

## 2NF — 부분 함수 종속 제거

부분 함수 종속이란 [복합 식별자](./1-1-5_식별자.md)인 PK (A, B) 중 어떤 컬럼이 A에만 또는 B에만 의존하는 상태

### Before(1NF, 부분종속 존재)

PK = (학생ID, 과목ID)

| 학생ID | 과목ID | 학생이름 | 과목명 | 성적 |
|---|---|---|---|---|
| S01 | K1 | 김철수 | 수학 | 90 |
| S01 | K2 | 김철수 | 영어 | 85 |

학생이름은 학생ID에만, 과목명은 과목ID에만 종속됨

### After(2NF)

**학생**

| 학생ID | 학생이름 |
|---|---|
| S01 | 김철수 |

**과목**

| 과목ID | 과목명 |
|---|---|
| K1 | 수학 |
| K2 | 영어 |

**수강**

| 학생ID | 과목ID | 성적 |
|---|---|---|
| S01 | K1 | 90 |
| S01 | K2 | 85 |

---

## 3NF — 이행 함수 종속 제거

이행 종속이란 A → B → C 형태로, PK가 아닌 B를 통해 C가 결정되는 상태

### Before(2NF, 이행종속)

| 사원ID(PK) | 부서ID | 부서명 |
|---|---|---|
| E01 | D1 | 영업부 |
| E02 | D1 | 영업부 |

사원ID → 부서ID → 부서명 (이행 종속)

### After(3NF)

**사원**

| 사원ID | 부서ID |
|---|---|
| E01 | D1 |
| E02 | D1 |

**부서**

| 부서ID | 부서명 |
|---|---|
| D1 | 영업부 |

---

## BCNF — 더 엄격한 3NF

3NF 까지 정규화해도 일부 비정상 종속이 남는 경우가 있다. **모든 결정자(determinant)가 후보키여야** BCNF.

> **결정자란?** — A → B 일 때 B 를 결정하는 A. 이 A 가 후보키 (PK 또는 UNIQUE) 가 아니면 BCNF 위반.

### 예시 (3NF 만족, BCNF 위반)

PK = (학생, 과목)

| 학생 | 과목 | 강사 |
|---|---|---|
| S01 | 수학 | 김교수 |
| S02 | 수학 | 김교수 |
| S03 | 영어 | 이교수 |

규칙: 한 강사는 한 과목만 가르침 → **강사 → 과목** 종속 존재

문제: '강사' 는 후보키가 아닌데 '과목' 을 결정 → BCNF 위반

### After (BCNF)

**강사_과목**: (강사, 과목)
**학생_강사**: (학생, 강사)

| 단계 | 차이 |
|---|---|
| 3NF | PK가 아닌 컬럼이 PK에 직접 종속 (이행 종속 제거 완료) |
| BCNF | 모든 함수 종속의 **결정자가 후보키** 여야 함 |

비유하면 3NF 는 거의 깔끔, BCNF 는 완벽주의 정리

---

## [SQL 실전] 정규화로 분리

\`\`\`sql
-- 정규화 전: 한 테이블에 모든 정보
CREATE TABLE BAD_ORDER (
    ORDER_ID NUMBER,
    PROD_ID  VARCHAR2(10),
    PROD_NAME VARCHAR2(50),  -- 상품명이 함께
    QTY      NUMBER,
    CUST_NAME VARCHAR2(50)   -- 고객명이 함께
);

-- 정규화 후: 책임에 따라 분리
CREATE TABLE PRODUCT (PROD_ID VARCHAR2(10) PRIMARY KEY, PROD_NAME VARCHAR2(50));
CREATE TABLE CUSTOMER(CUST_ID VARCHAR2(10) PRIMARY KEY, CUST_NAME VARCHAR2(50));
CREATE TABLE ORDERS  (ORDER_ID NUMBER PRIMARY KEY, CUST_ID VARCHAR2(10));
CREATE TABLE ORDER_DETAIL(ORDER_ID NUMBER, PROD_ID VARCHAR2(10), QTY NUMBER);
\`\`\`

---

## [Before & After] 정규화 효과

주문 한 건마다 고객 정보·상품 단가까지 한 줄에 다 적어두는 비정규형 테이블. 김철수가 두 번 주문하면 그의 전화번호가 두 번 똑같이 적히고, 사과 단가는 주문될 때마다 반복. 정규화 전후 차이.

### Before

| 주문ID | 고객명 | 고객전화 | 상품명 | 단가 | 수량 |
|---|---|---|---|---|---|
| 1 | 김철수 | 010-1111 | 사과 | 1000 | 2 |
| 2 | 김철수 | 010-1111 | 배 | 2000 | 1 |
| 3 | 이영희 | 010-2222 | 사과 | 1000 | 3 |

고객 정보 중복, 상품 단가 중복, 수정 시 일관성 깨짐

### After (3NF)

고객·상품·주문·주문상세 4개로 분리되어 중복 제거

같은 고객 전화번호가 주문 행마다 반복되는 것은 함수 종속이 한 테이블 안에 섞여 있기 때문(고객명·고객전화는 고객ID 에, 상품명·단가는 상품ID 에 종속). 종속이 다른 데이터끼리 같은 테이블을 공유하면 갱신·삭제·삽입 이상이 한 자리에서 다 터진다. "한 테이블엔 한 가지 의미만"이라는 원칙으로 단계적으로 쪼개면, 김철수의 전화번호는 고객 테이블 한 줄에서만 관리하면 된다.

---

## 정규화의 장단점

| 장점 | 단점 |
|---|---|
| 데이터 중복 제거 | 테이블이 많아짐 |
| 이상현상 방지 | [조인](./1-2-2_관계와조인의이해.md)이 늘어남 |
| 데이터 무결성 | 조회 성능 일부 저하 가능 |
| 변경/유지보수 용이 | — |

성능을 위해 의도적 반정규화(역정규화)도 가능

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| 정규화 단계 순서 | 1NF → 2NF → 3NF → BCNF |
| 1NF 핵심 | 원자값 |
| 2NF 핵심 | 부분 함수 종속 제거 |
| 3NF 핵심 | 이행 함수 종속 제거 |
| 정규화의 단점 | 조인 증가로 인한 조회 성능 저하 |
| 이상현상 3종 | 삽입/갱신/삭제 이상 |
| "정규화하면 항상 성능이 좋아진다" | 조회는 느려질 수 있음. 단 데이터 무결성은 향상 |

단골 출제: "복합 PK 중 일부 속성에만 종속되는 컬럼이 있으면 어느 정규형 위반인가" → 2NF

---

> **30초 시험 직전 정리**
> · 단계: **1NF → 2NF → 3NF → BCNF** (순서 함정 단골)
> · 1NF = **원자값** / 2NF = **부분종속 제거** / 3NF = **이행종속 제거** / BCNF = **결정자 = 후보키**
> · 이상현상 3종: **삽입 / 갱신 / 삭제**
> · 정규화 ↔ 반정규화 — 무결성 vs 성능. 조회 성능을 위해 의도적 반정규화 가능
`,
  c122: `# 1-2-2. 관계와 조인의 이해

## [핵심 요약]

> 모델에서의 [관계](./1-1-4_관계.md)는 외래키(FK)로 구현되고, 그 관계를 따라 여러 테이블을 합쳐 보는 작업이 조인(JOIN)이다.

ex) 영수증엔 주문 정보만, 회원카드엔 회원 정보만 있음. 같은 사람의 영수증과 카드를 맞춰 보면 "김철수가 사과 2개 샀구나"가 보인다 — 흩어진 정보를 키로 맞춰 합치는 것이 조인.

---

## [개념 도식화] 관계 → FK → JOIN

\`\`\`mermaid
flowchart LR
    A[설계 단계<br/>관계<br/>개념 모델] --> B[구현 단계<br/>외래키 FK<br/>논리/물리 모델]
    B --> C[조회 단계<br/>JOIN<br/>SQL 실행]
\`\`\`

\`\`\`mermaid
flowchart LR
    M["MEMBER<br/><b>MEMBER_ID</b> (PK)<br/>NAME"] -->|관계| O["ORDERS<br/><b>ORDER_ID</b> (PK)<br/><b>MEMBER_ID</b> (FK)"]
\`\`\`

자식 테이블의 **FK = 조인 키**.

---

## 조인의 4가지 종류 — 개념 미리보기

| 종류 | 의미 | 비유 |
|---|---|---|
| INNER JOIN | 양쪽에 다 있는 것만 | 양쪽 카드 모두 가진 사람 |
| LEFT OUTER | 왼쪽 다, 오른쪽은 매칭만 | 왼쪽 기준, 오른쪽 없으면 NULL |
| RIGHT OUTER | 오른쪽 다 | 오른쪽 기준 |
| FULL OUTER | 양쪽 모두 다 | 한쪽만 있어도 표시 |

자세한 구문은 [조인](./2-1-7_조인.md) / [표준 조인](./2-1-8_표준조인.md)에서

---

## 관계의 종류와 조인 시 주의점

### 1:1 관계 — 단순 매칭

\`\`\`
[사원] 1 ── 1 [사원증]
\`\`\`

INNER JOIN 결과: 사원 한 명당 사원증 한 개 → 사원 행 수 그대로 유지 (사원증 없는 사원은 LEFT JOIN 써야 결과에 남음)

### 1:N 관계 — 행이 늘어남

\`\`\`
[회원] 1 ── N [주문]
\`\`\`

회원 1명에게 주문 3건이면 조인 후 3행으로 늘어남

### M:N 관계 — 교차 엔터티 필요

\`\`\`
[학생] M ── N [과목]    →    [학생] 1─N [수강] N─1 [과목]
\`\`\`

조인은 반드시 수강(교차)을 거쳐 두 번

---

## [SQL 실전] 관계로 연결된 테이블 조인

\`\`\`sql
-- 1:N 조인: 회원과 주문
SELECT  M.NAME, O.ORDER_ID, O.ORD_DATE
FROM    MEMBER M
        JOIN ORDERS O ON M.MEMBER_ID = O.MEMBER_ID;

-- M:N → 교차 테이블 거쳐 두 번 조인
SELECT  S.NAME AS STUDENT, K.NAME AS SUBJECT, R.GRADE
FROM    STUDENT  S
        JOIN REGISTER R ON S.STU_ID = R.STU_ID
        JOIN SUBJECT  K ON R.SUB_ID = K.SUB_ID;
\`\`\`

---

## [Before & After] 데이터가 합쳐지는 모습

회원은 2명(김철수·이영희), 주문은 3건(김철수가 2건, 이영희가 1건). MEMBER 와 ORDERS 가 MEMBER_ID 로 1:N 관계일 때, INNER JOIN 결과 행 수가 어떻게 결정되는가

### 입력 테이블

**MEMBER**

| MEMBER_ID | NAME |
|---|---|
| M01 | 김철수 |
| M02 | 이영희 |

**ORDERS**

| ORDER_ID | MEMBER_ID | ORD_DATE |
|---|---|---|
| 1 | M01 | 2026-04-01 |
| 2 | M01 | 2026-04-05 |
| 3 | M02 | 2026-04-10 |

### After — INNER JOIN 결과

| NAME | ORDER_ID | ORD_DATE |
|---|---|---|
| 김철수 | 1 | 2026-04-01 |
| 김철수 | 2 | 2026-04-05 |
| 이영희 | 3 | 2026-04-10 |

회원 2명이지만 주문이 3건이므로 결과는 3행

1:N 조인의 결과 행 수는 부모(1쪽)가 아니라 자식(N쪽)에 맞춰 결정된다 — 부모 한 행이 매칭된 자식 수만큼 복제된다고 보면 됨. 이 사실을 잊고 부모 컬럼(예: 회원 등급별 포인트)을 그대로 SUM 하면 자식 수만큼 부풀려진 합계가 나온다. 1:N 조인 후 부모 쪽 집계가 필요하면 자식 합계를 서브쿼리로 먼저 구해 1:1 로 만들어 붙이는 것이 안전.

---

## 조인 시 자주 하는 실수

| 실수 | 결과 | 해결 |
|---|---|---|
| 조인 조건 빠짐 | 카티션 곱(행 수 폭발) | ON 명시 |
| 1:N 관계인데 합계 계산 | 중복 합산 | DISTINCT 또는 서브쿼리 분리 |
| OUTER JOIN과 WHERE 혼동 | NULL 행이 빠짐 | OUTER JOIN 시 조건은 ON에 둠 |

---

## 조인의 방향성

일반적으로 PK(부모) ← FK(자식) 방향으로 조인

\`\`\`
[부서] PK ←─── FK [사원]
   조인 키 = 부서.PK = 사원.FK
\`\`\`

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| 관계는 SQL에서 무엇으로 구현되는가 | 외래키(FK) |
| 1:N 관계를 조인하면 행 수는 | 부모 1행에 자식 N개면 결과는 N행 |
| M:N을 조인할 때 | 반드시 교차 엔터티를 거침 |
| 조인 조건 누락 시 | 카티션 곱(Cartesian Product) |
| 외래키는 항상 NOT NULL인가 | 선택 관계면 NULL 허용 |

단골 트릭: 1:N 관계를 조인한 뒤 부모의 합계를 그대로 SUM 하면 자식 수만큼 부풀려짐. 서브쿼리로 자식 합계를 먼저 구한 뒤 조인해야 안전

---

> **30초 시험 직전 정리**
> · 관계 → SQL 에서 **외래키(FK)** → 조회 시 **JOIN** 으로 활용
> · 1:N 조인 → 결과 행 수 = 자식 N (부모 1행이 N행으로 부풀려짐)
> · M:N → **교차 엔터티** 거쳐 두 번 조인 (수강·주문상세 등)
> · 외부 조인 시 추가 조건은 반드시 **ON 절**에 (WHERE 에 두면 INNER 됨)
> · 조인 조건 누락 → **카티션 곱** (행 수 폭발)
`,
  c123: `# 1-2-3. 모델이 표현하는 트랜잭션의 이해

## [핵심 요약]

> 트랜잭션(Transaction)이란 함께 성공하거나 함께 실패해야 하는 데이터 작업의 묶음이다. 모델은 이 묶음을 관계의 필수성과 식별성으로 표현한다.

ex) 계좌 이체에서 A에서 1만원이 빠지고 B에 1만원이 들어와야 정상. A에서만 빠지고 멈추면 돈이 사라지므로, 둘 다 성공하거나 둘 다 취소되어야 함

---

## [개념 도식화] 트랜잭션의 ACID

\`\`\`mermaid
flowchart TD
    T[트랜잭션 ACID] --> A[A. 원자성<br/>Atomicity<br/>전부 또는 전무]
    T --> C[C. 일관성<br/>Consistency<br/>무결성 규칙 유지]
    T --> I[I. 고립성<br/>Isolation<br/>다른 트랜잭션 간섭 없음]
    T --> D[D. 지속성<br/>Durability<br/>커밋되면 영구 보존]
\`\`\`

| 성질 | 의미 | 비유 |
|---|---|---|
| 원자성 (Atomicity) | 모두 성공하거나 모두 실패 | 이체는 출금+입금 한 묶음 |
| 일관성 (Consistency) | 트랜잭션 전후 데이터 무결성 규칙 유지 | 잔고 합계는 변하지 않음 |
| 고립성 (Isolation) | 동시 실행이라도 서로 간섭 없음 | 다른 사람 이체와 겹치지 않음 |
| 지속성 (Durability) | 커밋되면 시스템이 죽어도 보존 | 영수증으로 기록됨 |

---

## 모델에서 트랜잭션은 어떻게 보이는가

### 관계의 필수성

\`\`\`mermaid
flowchart LR
    O[주문] -->|주문상세는 반드시 주문이 있어야 함<br/>필수 Mandatory| D[주문상세]
\`\`\`

주문을 INSERT할 때 주문상세도 함께 INSERT 되어야 정상 트랜잭션

### 관계의 식별성

\`\`\`
[주문]  ──── [주문상세]   :  식별 관계(실선), 주문 없이 주문상세 존재 불가
\`\`\`

주문을 DELETE 할 때 주문상세도 함께 DELETE 되거나 막아야 함

---

## 트랜잭션 표현 예시

\`\`\`
[고객]  ──○──── [주문]  ──────── [주문상세]
              선택              식별(실선)
\`\`\`

| 표기 | 의미 |
|---|---|
| 고객 ─○─ 주문 | 고객은 주문이 없을 수 있음 (선택, Optional) |
| 주문 ──── 주문상세 (실선) | **식별 관계** — 주문상세는 주문 PK 일부를 자기 PK로 가짐 (주문 없이 존재 불가) |
| 주문 ─ ─ ─ 주문상세 (점선) | **비식별 관계** — 주문 키는 일반 FK 로만 (주문상세가 독자 PK 보유) |

---

## [SQL 실전] 트랜잭션 묶음

INSERT/UPDATE/DELETE 는 [DML](./2-3-1_DML.md), COMMIT/ROLLBACK 은 [TCL](./2-3-2_TCL.md) 로 트랜잭션 마감

\`\`\`sql
-- 시작 (Oracle은 DML 실행 시 자동 시작)
INSERT INTO ORDERS(ORDER_ID, CUST_ID, ORD_DATE)
VALUES (1001, 'C001', SYSDATE);

INSERT INTO ORDER_DETAIL(ORDER_ID, PROD_ID, QTY)
VALUES (1001, 'P01', 2);

INSERT INTO ORDER_DETAIL(ORDER_ID, PROD_ID, QTY)
VALUES (1001, 'P02', 1);

COMMIT;  -- 한 묶음으로 영구 저장

-- 중간에 오류 발생 시
ROLLBACK; -- 모두 취소
\`\`\`

---

## [Before & After] 트랜잭션이 깨지면

A 계좌에서 B 계좌로 1만원 이체. 출금(A) → 입금(B) 두 작업 사이에서 시스템이 멈춘 상황 vs 두 작업을 한 트랜잭션으로 묶어 ROLLBACK 한 상황의 잔고 비교

### Before — 출금만 성공, 입금 실패

| 계좌 | 잔고 |
|---|---|
| A | 90,000 (10,000 빠짐) |
| B | 50,000 (그대로) |

1만원이 사라짐

### After — 트랜잭션으로 묶었을 때

ROLLBACK 후

| 계좌 | 잔고 |
|---|---|
| A | 100,000 (원상복구) |
| B | 50,000 |

데이터 보존

출금과 입금은 업무적으로 한 사건(이체)이지만 SQL 입장에선 별개 UPDATE 두 개. 묶지 않으면 중간 실패가 곧 데이터 모순(원자성 깨짐). 모델에서 두 엔터티가 식별 관계나 필수 관계로 묶여 있다는 건 곧 "이 INSERT/UPDATE/DELETE 들은 한 트랜잭션 안에서 함께 처리해야 한다"는 신호. 함께 변해야 하는 데이터는 코드에서도 BEGIN ~ COMMIT 으로 한 묶음이 되도록 설계해야 한다.

---

## 트랜잭션 처리 패턴

| 패턴 | 모델 단서 | SQL |
|---|---|---|
| 묶어서 INSERT | 식별 관계 / 필수 관계 | INSERT ... INSERT ... COMMIT |
| 묶어서 UPDATE | 1:1 관계 | UPDATE A ... UPDATE B ... COMMIT |
| 묶어서 DELETE | 식별 관계(CASCADE) | DELETE 부모 → 자식 자동/명시 삭제 |

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| ACID 4성질 | 원자성 / 일관성 / 고립성 / 지속성 |
| 모델에서 트랜잭션의 흔적 | 관계의 필수성, 식별성 |
| "원자성은 일부만 반영해도 된다" | 전부 또는 전무 |
| 식별 관계의 의미 | 부모 없이 자식 존재 불가 → 함께 처리 |
| 필수 관계의 의미 | 관계 자체가 반드시 있어야 함(NOT NULL FK) |
| COMMIT의 역할 | 트랜잭션을 영구 반영(지속성) |

단골 출제: "모델은 트랜잭션을 표현할 수 없다" → 틀림. 관계의 필수성·식별성으로 표현 가능

---

> **30초 시험 직전 정리**
> · ACID = **원자성·일관성·고립성·지속성**
> · 모델 단서 = **관계의 필수성** (반드시 있어야) + **관계의 식별성** (실선 = 부모 키가 자식 PK 일부)
> · COMMIT = 영구 반영 (지속성) / ROLLBACK = 트랜잭션 시작 시점으로 복구
> · DDL 은 자동 COMMIT — 따라서 ROLLBACK 불가
`,
  c124: `# 1-2-4. NULL 속성의 이해

## [핵심 요약]

> NULL이란 값이 없거나 아직 모름을 나타내는 특수한 표시다. 0이나 공백과는 전혀 다르다.

ex) 설문지에서 "월 소득: 0원"은 0이라는 값이 적힌 것, "월 소득: ␣"(한 칸 띈 상태)는 공백 문자, "월 소득: 미작성"이 NULL(값 자체가 없음)

---

## [개념 도식화] NULL의 비교

\`\`\`
   ┌───────┬───────┬───────┐
   │   0   │  ' '  │  NULL  │
   ├───────┼───────┼───────┤
   │ 숫자값  │ 공백문자 │ 값 없음│
   │ 존재O  │ 존재O  │ 존재X  │
   └───────┴───────┴───────┘
\`\`\`

| 비교 | 0 | 공백 | NULL |
|---|---|---|---|
| 데이터 존재 여부 | 있음 | 있음 | 없음 |
| 비교 가능 여부 | 가능 | 가능 | 불가(\`= NULL\` 안 됨) |
| 산술연산 | 가능 | — | 불가(결과는 모두 NULL) |

---

## NULL의 4가지 규칙

| 규칙 | 결과 |
|---|---|
| NULL과 산술 연산 | 결과는 NULL (예: 1000 + NULL = NULL) |
| NULL과 비교 연산 | 결과는 **UNKNOWN** — SQL 의 3-값 논리 (TRUE / FALSE / UNKNOWN). [WHERE 절](./2-1-4_WHERE절.md)에서는 행이 제외됨 |
| NULL은 IS NULL / IS NOT NULL로만 비교 | \`= NULL\`, \`!= NULL\` 모두 항상 거짓 |
| 집계 함수는 NULL을 무시 | COUNT(*) 만 NULL 포함 카운트 |

> **SQL 3-값 논리** — 일반 프로그래밍은 TRUE/FALSE 2값이지만 SQL 은 NULL 때문에 **TRUE / FALSE / UNKNOWN** 3값. \`NULL = NULL\` 도 결과가 UNKNOWN.

---

## NULL 처리 함수

NVL/NVL2/NULLIF/COALESCE 의 자세한 동작은 [함수](./2-1-3_함수.md) 챕터에서도

| 함수 | 동작 | 예 |
|---|---|---|
| NVL(A, B) | A가 NULL이면 B로 대체 | NVL(SAL, 0) |
| NVL2(A, B, C) | A가 NULL이 아니면 B, NULL이면 C | NVL2(COMM, '있음', '없음') |
| NULLIF(A, B) | A=B면 NULL, 아니면 A | NULLIF(A, 0) |
| COALESCE(A, B, C, …) | NULL이 아닌 첫 값 반환 | COALESCE(P1, P2, '없음') |

---

## 집계 함수에서의 NULL

| 함수 | NULL 처리 |
|---|---|
| COUNT(*) | NULL 포함 모든 행 카운트 |
| COUNT(컬럼) | NULL 제외 |
| SUM/AVG/MAX/MIN | NULL 무시 |
| AVG(컬럼) | (NULL 제외 합) / (NULL 제외 개수) |

### 예시

| EMP | SAL |
|---|---|
| A | 100 |
| B | NULL |
| C | 200 |

| 쿼리 | 결과 |
|---|---|
| COUNT(*) | 3 |
| COUNT(SAL) | 2 |
| SUM(SAL) | 300 |
| AVG(SAL) | 150 (300/2, 0 포함이 아님에 주의) |

---

## [SQL 실전] NULL 다루기

\`\`\`sql
-- 잘못된 비교
SELECT * FROM EMP WHERE COMM = NULL;     -- 결과 0건
SELECT * FROM EMP WHERE COMM != NULL;    -- 결과 0건

-- 올바른 비교
SELECT * FROM EMP WHERE COMM IS NULL;
SELECT * FROM EMP WHERE COMM IS NOT NULL;

-- NULL 대체
SELECT NAME, NVL(COMM, 0) AS COMM_FIX FROM EMP;
SELECT NAME, COALESCE(PHONE, EMAIL, '연락처 없음') FROM MEMBER;
\`\`\`

---

## [Before & After] NULL이 끼면 결과가 달라진다

기본급(SAL)에 커미션(COMM)을 더해 총급여를 구하는 상황. 아래 EMP 에서 B는 커미션이 등록되지 않아 COMM 이 NULL. 단순 합산 vs NVL 처리 결과

### EMP

| NAME | SAL | COMM |
|---|---|---|
| A | 100 | 10 |
| B | 200 | NULL |
| C | 300 | 20 |

### \`SELECT NAME, SAL + COMM FROM EMP;\`

| NAME | SAL+COMM |
|---|---|
| A | 110 |
| B | NULL |
| C | 320 |

### \`SELECT NAME, SAL + NVL(COMM, 0) FROM EMP;\`

| NAME | TOTAL |
|---|---|
| A | 110 |
| B | 200 |
| C | 320 |

NULL 은 산술 연산에 들어가면 결과 전체를 NULL 로 만든다. B처럼 커미션이 비어 있을 뿐인 직원의 총급여까지 사라져버린다. NVL(COMM, 0) 으로 "커미션 없음 = 0"으로 안전하게 치환하면 NULL 행도 정상 합산되어 보고서에 누락이 안 생긴다. 합계·평균 같은 집계 전에는 늘 NULL 처리 방식을 정해두는 것이 안전.

---

## NULL과 정렬

| DBMS | 기본 정렬 시 NULL 위치 |
|---|---|
| Oracle | 오름차순(ASC)에서 마지막, 내림차순(DESC)에서 처음 |
| MS-SQL | 오름차순에서 처음 |

직접 지정: \`ORDER BY COL NULLS FIRST\` / \`NULLS LAST\` (자세히는 [ORDER BY 절](./2-1-6_ORDERBY절.md) 에서)

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| \`WHERE COL = NULL\`의 결과 | 결과 없음(0건). IS NULL을 사용 |
| \`NULL + 100\` | NULL |
| AVG(컬럼) | NULL 제외 평균(분모도 줄어듦) |
| COUNT(*) vs COUNT(컬럼) | \`*\`는 NULL 포함, 컬럼명은 NULL 제외 |
| \`NULL = NULL\`의 결과 | FALSE(UNKNOWN) |
| Oracle ORDER BY ASC 기본 NULL 위치 | 마지막(LAST) |
| \`NULLIF(10, 10)\` | NULL |
| \`COALESCE(NULL, NULL, 'X')\` | 'X' |

단골 함정: "NULL은 0이다" "NULL은 공백이다" → 둘 다 틀림. NULL = 값 없음

---

> **30초 시험 직전 정리**
> · NULL = **값 없음** (≠ 0, ≠ 공백)
> · 비교는 \`IS NULL\` / \`IS NOT NULL\` 만 가능 (\`= NULL\` 항상 거짓)
> · NULL + 값 = **NULL**
> · COUNT(*) = NULL 포함 / COUNT(컬럼) = NULL 제외 / SUM·AVG·MAX·MIN = NULL **무시**
> · NVL(A, B) = A null 이면 B / NVL2(A, B, C) / NULLIF(A, B) / COALESCE(...)
> · Oracle ASC 기본 NULL 위치 = **마지막 (NULLS LAST)**
`,
  c125: `# 1-2-5. 본질식별자 vs 인조식별자

## [핵심 요약]

> 본질[식별자](./1-1-5_식별자.md)는 업무에서 자연스럽게 도출된 식별자, 인조식별자는 인위적으로 만든 식별자다. 둘의 장단점을 알고 상황에 맞게 선택.

ex) 도서관에서 ISBN(출판사 부여 = 본질식별자)과 자체 분류번호 A-001(도서관 임의 부여 = 인조식별자) 두 가지로 책을 구분 가능. ISBN이 길고 형식이 들쑥날쑥해서 도서관은 자체 번호를 더 자주 씀

---

## [개념 도식화] 본질 vs 인조

| 구분 | 본질식별자 | 인조식별자 |
|---|---|---|
| 출처 | 업무에서 자연 발생 | 인위적으로 부여 |
| 예시 | 주민번호 · 사업자번호 · ISBN | 자동증가 ID · UUID · GUID |
| 의미 | 업무적 의미 있음 | 의미 없음 (대체 키) |
| 변경 | 변경되면 큰 영향 | 자유롭게 부여 가능 |

\`\`\`
[ 식별자 후보 ]
      │
      ▼
업무에서 자연 도출되는가?
      ├─ NO  ─→  [ 인조식별자 ]
      │
      └─ YES ─→  너무 길거나 보안·변경 위험 있는가?
                       ├─ YES ─→ [ 인조식별자 ]
                       └─ NO  ─→ [ 본질식별자 ]
\`\`\`

---

## 비교표

| 항목 | 본질식별자 | 인조식별자 |
|---|---|---|
| 정의 | 업무에서 자연스럽게 도출 | 인위적으로 부여 |
| 예시 | 주민번호, 사업자번호, ISBN | 자동증가 ID, UUID, GUID |
| 의미 | 업무적 의미 있음 | 의미 없음 (단순 키) |
| 변경 위험 | **존재** — 업무 규칙 변동 시 변할 수 있음 (예: 주민번호 체계 개편) | **없음** — 한 번 부여되면 영구 고정 |
| 길이 | 길 수 있음 | 짧고 일관 |
| 데이터 품질 검증 | 가능 (중복 자연 검출) | 어려움 — 본질키에 별도 UNIQUE 필요 |
| PK 사용 시 장점 | 의미 검증 용이 | JOIN/인덱스/관리 효율 |

---

## 인조식별자의 장단점

| 장점 | 단점 |
|---|---|
| 짧고 일관된 형식 | 업무적 의미 없음 |
| 변경에 자유로움 | 중복 데이터 식별 어려움 |
| JOIN/인덱스 효율 | 추가 UNIQUE 제약 필요 |
| 보안(주민번호 노출 회피) | 모델이 직관적이지 않음 |

---

## 본질식별자의 장단점

| 장점 | 단점 |
|---|---|
| 업무적 의미 명확 | 길거나 복잡 |
| 자연스러운 데이터 검증 | 변경되면 큰 영향 |
| 모델 직관적 | 보안 이슈(개인정보 등) |

---

## 언제 무엇을 쓸까

\`\`\`mermaid
flowchart TD
    A[식별자 결정] --> B{업무 본질키 존재?}
    B -- NO --> X[인조식별자]
    B -- YES --> C{길거나 자주 바뀌나?}
    C -- YES --> X
    C -- NO --> D{보안 / 개인정보?}
    D -- YES --> X
    D -- NO --> Y[본질식별자]
\`\`\`

**현업 권장 패턴**: PK 는 인조식별자, 본질키는 UNIQUE 제약으로 별도 보존 → 보안·성능·일관성 모두 만족. 본질키를 PK 로 쓰면 [정규화](./1-2-1_정규화.md) 단계의 종속성 분석이 자연스럽지만, 변경 위험이 따름

---

## [SQL 실전]

### 본질식별자 사용

\`\`\`sql
CREATE TABLE BOOK (
    ISBN     VARCHAR2(20) PRIMARY KEY,  -- 본질식별자
    TITLE    VARCHAR2(200),
    AUTHOR   VARCHAR2(100)
);
\`\`\`

### 인조식별자 사용

\`\`\`sql
CREATE TABLE BOOK (
    BOOK_ID  NUMBER GENERATED ALWAYS AS IDENTITY PRIMARY KEY,  -- 인조식별자
    ISBN     VARCHAR2(20) UNIQUE,                              -- 본질키는 UNIQUE로 유지
    TITLE    VARCHAR2(200),
    AUTHOR   VARCHAR2(100)
);
\`\`\`

권장 패턴: 인조식별자를 PK로, 본질식별자를 UNIQUE 제약으로 함께

---

## [Before & After] 인조식별자 도입의 효과

회원 김철수의 주민번호(800101-1234567)를 회원 PK 로 쓰는 경우 vs 인조식별자(M01)를 PK 로 두고 주민번호는 UNIQUE 로 보존하는 경우. 자식 테이블(주문)에 어떤 값이 흩뿌려지는가

### Before — 본질식별자(주민번호)를 PK로

| 주문 (FK: 주민번호) |
|---|
| 800101-1234567 → 김철수 주문 1 |
| 800101-1234567 → 김철수 주문 2 |

모든 자식 테이블에 주민번호가 흩뿌려져 개인정보 노출 위험 증가

### After — 인조식별자(MEMBER_ID) 도입

**MEMBER**

| MEMBER_ID(PK) | RRN(UQ) | NAME |
|---|---|---|
| M01 | 800101-1234567 | 김철수 |

**ORDER (FK: MEMBER_ID)**

| ORDER_ID | MEMBER_ID |
|---|---|
| 1 | M01 |
| 2 | M01 |

자식 테이블엔 의미 없는 ID만 들어가 보안·JOIN 효율 모두 향상

PK 는 모든 자식 테이블에 FK 로 흩어지므로, 본질식별자가 PK 면 그 값(주민번호)도 함께 흩어진다 — 보안 사고의 표면적이 그대로 늘어남. 본질키는 길거나 형식이 들쑥날쑥해 인덱스·조인 비용도 커진다. 인조식별자(M01 같은 짧은 키)를 PK 로 두고 본질키는 회원 테이블 한 곳에 UNIQUE 로 가둬두면 보안·성능·일관성이 한꺼번에 정리된다. 단, 본질키의 무결성 검증을 위해 UNIQUE 제약을 빼먹지 않는 것이 핵심.

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| 인조식별자 단점 | 업무적 의미 없음, 중복 검증 어려움 |
| 본질식별자 단점 | 길거나 변경 위험, 보안 이슈 |
| "인조식별자는 항상 본질식별자보다 좋다" | 상황에 따라 다름 |
| 인조식별자 도입 시 본질키 처리 | UNIQUE 제약으로 무결성 보존 |
| "주민등록번호를 PK로 쓰는 것이 보안상 권장된다" | 보통 인조식별자 권장 |

단골 출제: 인조식별자 도입 시 본질키에 UNIQUE 처리를 안 하면 중복 데이터 발생 가능

---

> **30초 시험 직전 정리**
> · 본질식별자 = 업무 자연 도출 (주민번호·사업자번호·ISBN)
> · 인조식별자 = 인위적 부여 (자동증가 ID·UUID)
> · "주민번호를 PK로 쓰는 것이 권장된다" → **틀림**. 보안·길이·변경 위험 → 인조식별자 권장
> · 인조식별자 도입 시 본질키는 **UNIQUE 제약**으로 무결성 보존
> · 인조식별자 단점 = **업무적 의미 없음**, 중복 데이터 자연 검출 어려움
`,
  c211: `# 2-1-1. 관계형 데이터베이스 개요

## [핵심 요약]

> 관계형 데이터베이스(RDB)는 데이터를 행과 열로 된 표(테이블)로 저장하고, 표들 사이의 관계로 데이터를 관리하는 시스템이다.

ex) 엑셀 한 권에 여러 시트(=테이블), VLOOKUP 키(=외래키)로 시트끼리 연결되는 구조를 안전·동시 처리까지 가능하게 만든 것이 RDB

---

## [개념 도식화] 데이터베이스의 구조

\`\`\`
   ┌──────────────────────────────────┐
   │            DBMS                  │
   │  ┌─────────────────────────┐    │
   │  │         Database          │    │
   │  │   ┌───────┐ ┌───────┐   │    │
   │  │   │TABLE A│ │TABLE B│   │    │
   │  │   │       │ │       │   │    │
   │  │   └───┬───┘ └───┬───┘   │    │
   │  │       └────관계──┘        │    │
   │  └─────────────────────────┘    │
   └──────────────────────────────────┘
\`\`\`

| 용어 | 의미 |
|---|---|
| DBMS | 데이터베이스를 관리하는 SW (Oracle, MySQL 등) |
| Database | 데이터의 저장소 |
| Table | 행과 열로 된 표 |
| Row(Record) | 한 줄(한 인스턴스) |
| Column(Attribute) | 한 칸(속성) |

---

## SQL 명령어 분류

\`\`\`mermaid
flowchart TD
    SQL[SQL] --> DDL[DDL · 정의어<br/>CREATE / ALTER / DROP<br/>TRUNCATE / RENAME]
    SQL --> DML[DML · 조작어<br/>SELECT / INSERT / UPDATE<br/>DELETE / MERGE]
    SQL --> DCL[DCL · 제어어<br/>GRANT / REVOKE]
    SQL --> TCL[TCL · 트랜잭션<br/>COMMIT / ROLLBACK / SAVEPOINT]
\`\`\`

| 분류 | 풀이 | 명령어 | 비유 |
|---|---|---|---|
| [DDL](./2-3-3_DDL.md) | Data Definition | CREATE, ALTER, DROP, TRUNCATE, RENAME | 표(설계도) 만들기/바꾸기 |
| [DML](./2-3-1_DML.md) | Data Manipulation | [SELECT](./2-1-2_SELECT문.md), INSERT, UPDATE, DELETE, MERGE | 표 안의 데이터 조작 |
| [DCL](./2-3-4_DCL.md) | Data Control | GRANT, REVOKE | 권한 부여/회수 |
| [TCL](./2-3-2_TCL.md) | Transaction Control | COMMIT, ROLLBACK, SAVEPOINT | 트랜잭션 묶음 처리 |

헷갈리기 쉬운 점: TRUNCATE 는 DDL, DELETE 는 DML

---

## 주요 데이터 타입

| 타입 | 의미 | 예 |
|---|---|---|
| CHAR(n) | **고정** 길이 문자열 — 모자라면 공백으로 채움 | CHAR(5) 에 'AB' 저장 → 'AB&nbsp;&nbsp;&nbsp;' (공백 3칸) |
| VARCHAR2(n) | **가변** 길이 문자열 — 입력한 만큼만 저장 | VARCHAR2(5) 에 'AB' 저장 → 'AB' |
| NUMBER(p, s) | 숫자 (전체 p자리, 소수 s자리) | NUMBER(7, 2) → 12345.67 |
| DATE | 날짜+시간 | 2026-04-26 13:24:00 |
| TIMESTAMP | 정밀 시간 (소수초까지) | 2026-04-26 13:24:00.123456 |

---

## 트랜잭션의 핵심 약속 — ACID

| 성질 | 의미 |
|---|---|
| Atomicity | 전부 성공 또는 전부 실패 |
| Consistency | 규칙 유지 |
| Isolation | 동시 처리 시 격리 |
| Durability | 커밋 후 영구 저장 |

---

## [SQL 실전] 가장 기본적인 흐름

\`\`\`sql
-- DDL: 테이블 생성
CREATE TABLE EMP (
    EMP_ID NUMBER PRIMARY KEY,
    NAME   VARCHAR2(50),
    SAL    NUMBER
);

-- DML: 데이터 입력/조회
INSERT INTO EMP VALUES (1, '김철수', 3000);
SELECT * FROM EMP;

-- TCL: 영구 저장
COMMIT;

-- DCL: 권한 부여
GRANT SELECT ON EMP TO USER1;
\`\`\`

---

## [Before & After] 파일 시스템 vs RDB

직원 데이터(EMP_ID, NAME, SAL) 2건을 보관하고 "급여 2000 초과인 사람"을 찾는 상황. 텍스트 파일 vs RDB 의 EMP 테이블.

### Before — 일반 파일에 저장

\`\`\`
emp.txt
1, 김철수, 3000
2, 이영희, 4000
\`\`\`

동시 수정 불가, 검색 느림, 무결성 보장 어려움

### After — RDB 사용

\`\`\`sql
SELECT * FROM EMP WHERE SAL > 2000;
\`\`\`

빠른 검색(인덱스), 동시 처리(트랜잭션), 무결성 제약 보장

파일은 의미·타입·관계 정보가 없어 매번 직접 파싱해야 하고, 동시 수정 시 한쪽 변경이 사라짐. RDB 는 스키마·인덱스·트랜잭션을 엔진이 보장하므로 데이터가 많거나 다중 사용자 환경에서는 RDB 가 정답.

---

## RDB의 5대 장점

| 장점 | 설명 |
|---|---|
| 데이터 독립성 | DB 변경이 응용 프로그램에 영향을 덜 줌 |
| 데이터 중복 최소화 | 정규화로 일관성 유지 |
| 무결성 | 제약조건으로 잘못된 데이터 차단 |
| 보안 | DCL로 권한 관리 |
| 동시성 | 트랜잭션 격리로 충돌 방지 |

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| TRUNCATE는 DDL인가 DML인가 | DDL(롤백 불가) |
| DELETE는 어디에 속하는가 | DML(롤백 가능) |
| COMMIT은 | TCL |
| GRANT/REVOKE는 | DCL |
| RDB 데이터 저장 단위 | 테이블(행과 열) |
| 한 행을 무엇이라 하는가 | Row, Record, Tuple |
| 한 열을 무엇이라 하는가 | Column, Attribute, Field |

단골 함정: "INSERT/UPDATE/DELETE/SELECT 모두 DML" 은 옳음. MERGE 도 DML.

---

> **30초 시험 직전 정리**
> · DDL: **CREATE / ALTER / DROP / TRUNCATE / RENAME** (자동 COMMIT, 롤백 불가)
> · DML: **SELECT / INSERT / UPDATE / DELETE / MERGE**
> · DCL: **GRANT / REVOKE** (권한)
> · TCL: **COMMIT / ROLLBACK / SAVEPOINT** (트랜잭션)
> · 함정: **TRUNCATE = DDL** (롤백 X), **DELETE = DML** (롤백 O)
> · 행/열 동의어: Row=Record=Tuple / Column=Attribute=Field
`,
  c212: `# 2-1-2. SELECT 문

## [핵심 요약]

> SELECT 문은 데이터베이스에서 원하는 데이터를 골라서 뽑아내는 가장 기본적인 명령어다.

ex) 식당 주문처럼 "어느 식당(FROM)·어떤 음식(SELECT)·조건(WHERE)·그룹별(GROUP BY)·정렬(ORDER BY)" 을 차례로 지정

---

## [개념 도식화] SELECT 문의 6대 절

\`\`\`
SELECT    컬럼1, 컬럼2, ...      ← ② 어떤 컬럼? (함수 활용)
FROM      테이블                   ← ① 어디서? (조인 가능)
WHERE     조건                     ← ③ 어떤 행?
GROUP BY  그룹기준                 ← ④ 어떻게 묶을지?
HAVING    그룹조건                 ← ⑤ 묶은 후 조건?
ORDER BY  정렬기준;                ← ⑥ 어떻게 정렬?
\`\`\`

각 절의 자세한 사용은 [WHERE](./2-1-4_WHERE절.md), [GROUP BY/HAVING](./2-1-5_GROUPBY_HAVING절.md), [ORDER BY](./2-1-6_ORDERBY절.md), [함수](./2-1-3_함수.md), [조인](./2-1-7_조인.md) 챕터 참고

### 작성 순서 vs 실행 순서

\`\`\`mermaid
flowchart LR
    F[FROM<br/>① 어디서] --> W[WHERE<br/>② 어떤 행]
    W --> G[GROUP BY<br/>③ 어떻게 묶나]
    G --> H[HAVING<br/>④ 그룹 조건]
    H --> S[SELECT<br/>⑤ 어떤 컬럼]
    S --> O[ORDER BY<br/>⑥ 어떻게 정렬]
\`\`\`

| 작성 순서 | 실행 순서 |
|---|---|
| 1. SELECT | 5. SELECT |
| 2. FROM | 1. FROM |
| 3. WHERE | 2. WHERE |
| 4. GROUP BY | 3. GROUP BY |
| 5. HAVING | 4. HAVING |
| 6. ORDER BY | 6. ORDER BY |

암기: FROM → WHERE → GROUP → HAVING → SELECT → ORDER (FWGHSO)

---

## SELECT 절의 다양한 표현

| 형태 | 의미 |
|---|---|
| \`SELECT *\` | 모든 컬럼 |
| \`SELECT col1, col2\` | 특정 컬럼 |
| \`SELECT DISTINCT col\` | 중복 제거 |
| \`SELECT col AS 별명\` | 별칭 부여 |
| <code>SELECT col &#124;&#124; '문자'</code> | 문자열 연결(Oracle) |
| \`SELECT 1+1 FROM DUAL\` | 계산식 |

---

## [SQL 실전]

\`\`\`sql
-- 1. 모든 컬럼
SELECT * FROM EMP;

-- 2. 특정 컬럼
SELECT EMP_ID, NAME, SAL FROM EMP;

-- 3. 별칭(AS 생략 가능, 한글 별칭은 "")
SELECT NAME AS 사원명, SAL*12 "연봉" FROM EMP;

-- 4. 중복 제거
SELECT DISTINCT DEPT_ID FROM EMP;

-- 5. 문자열 연결
SELECT NAME || '님' AS GREETING FROM EMP;
\`\`\`

---

## [Before & After]

EMP 4명 중 동명이인 김철수가 D1·D2 부서에 한 명씩 있는 상황. 컬럼 지정·DISTINCT·연봉 계산 별칭에 따라 결과가 달라지는 양상 비교.

### EMP

| EMP_ID | NAME | DEPT_ID | SAL |
|---|---|---|---|
| 1 | 김철수 | D1 | 3000 |
| 2 | 이영희 | D1 | 4000 |
| 3 | 박민수 | D2 | 3500 |
| 4 | 김철수 | D2 | 3200 |

### \`SELECT NAME, SAL FROM EMP;\`

| NAME | SAL |
|---|---|
| 김철수 | 3000 |
| 이영희 | 4000 |
| 박민수 | 3500 |
| 김철수 | 3200 |

### \`SELECT DISTINCT NAME FROM EMP;\`

| NAME |
|---|
| 김철수 |
| 이영희 |
| 박민수 |

### \`SELECT NAME, SAL*12 AS "연봉" FROM EMP;\`

| NAME | 연봉 |
|---|---|
| 김철수 | 36000 |
| 이영희 | 48000 |
| 박민수 | 42000 |
| 김철수 | 38400 |

DISTINCT 는 SELECT 절 전체(여러 컬럼이면 조합) 단위로 중복 제거 → NAME 만 보면 동명이인 김철수도 한 줄로 합쳐짐. SAL*12 같은 표현은 행 단위 계산이라 같은 이름이라도 별개 행. 원본 그대로면 컬럼 나열, 값 종류만이면 DISTINCT, 가공값에 의미있는 이름이면 별칭.

---

## 별칭(Alias) 규칙

| 규칙 | 예 |
|---|---|
| AS 생략 가능 | \`SELECT NAME 사원명\` |
| 공백/한글/특수문자 포함 시 \`""\` 사용 | \`"사원 이름"\` |
| 테이블 별칭은 AS 사용 불가(Oracle) | \`FROM EMP E\` |
| WHERE/GROUP/HAVING에서 SELECT의 별칭 사용 | 불가(실행 순서 때문) |
| ORDER BY에서 별칭 사용 | 가능 |

---

## DUAL 테이블 (Oracle)

시스템 내장 가짜 테이블. 1행 1컬럼. 계산이나 함수 결과 확인용.

\`\`\`sql
SELECT 1+1 FROM DUAL;        -- 2
SELECT SYSDATE FROM DUAL;    -- 현재 시각
SELECT USER FROM DUAL;       -- 접속 계정
\`\`\`

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| SELECT 실행 순서 | FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY |
| WHERE에서 SELECT 별칭 사용 가능? | 불가(SELECT가 나중에 실행됨) |
| ORDER BY에서 SELECT 별칭 사용 가능? | 가능 |
| DISTINCT 적용 범위 | SELECT 절 전체(여러 컬럼이면 조합 단위) |
| Oracle 테이블 별칭에 AS | 사용 불가 |
| \`SELECT * FROM DUAL\`의 결과 | 1행 1컬럼('X') |

자주 출제: WHERE 절에서 SELECT 별칭 사용 시 에러. 실행 순서상 WHERE 가 먼저 처리되기 때문.

---

> **30초 시험 직전 정리**
> · 작성: SELECT → FROM → WHERE → GROUP → HAVING → ORDER
> · 실행: **FROM → WHERE → GROUP → HAVING → SELECT → ORDER (FWGHSO)**
> · 별칭 사용: **ORDER BY 만 가능**, WHERE/GROUP/HAVING 은 불가 (SELECT 가 더 늦게 실행됨)
> · DISTINCT = SELECT 절 전체에 적용 (여러 컬럼이면 컬럼 조합 단위로 중복 제거)
> · Oracle 테이블 별칭 = AS 사용 **불가** / 컬럼 별칭 = AS 사용 **가능 (생략도 가능)**
> · DUAL = Oracle 시스템 내장 1행 1컬럼 가짜 테이블 (계산·함수 테스트용)
`,
  c213: `# 2-1-3. 함수(Function)

## [핵심 요약]

> 함수는 데이터를 입력받아 가공된 결과를 돌려주는 도구다. 문자, 숫자, 날짜, [NULL](./1-2-4_NULL속성의이해.md)을 가공할 수 있다.

---

## [개념 도식화] 함수의 분류

\`\`\`mermaid
flowchart TD
    F[SQL 함수] --> S[단일행 함수<br/>1행 → 1결과]
    F --> M[다중행 = 집계 함수<br/>N행 → 1결과<br/>SUM / AVG / COUNT<br/>MAX / MIN]
    S --> S1[문자<br/>UPPER · SUBSTR<br/>INSTR · LENGTH]
    S --> S2[숫자<br/>ROUND · TRUNC<br/>MOD · ABS]
    S --> S3[날짜<br/>SYSDATE · ADD_MONTHS<br/>MONTHS_BETWEEN]
    S --> S4[변환<br/>TO_CHAR · TO_NUMBER<br/>TO_DATE]
    S --> S5[일반/NULL<br/>NVL · NVL2 · COALESCE<br/>CASE · DECODE]
\`\`\`

---

## 문자 함수

| 함수 | 설명 | 예 | 결과 |
|---|---|---|---|
| UPPER(s) | 대문자 | UPPER('abc') | ABC |
| LOWER(s) | 소문자 | LOWER('ABC') | abc |
| INITCAP(s) | 첫글자만 대문자 | INITCAP('hi sql') | Hi Sql |
| LENGTH(s) | 문자열 길이 | LENGTH('한글') | 2 |
| SUBSTR(s, m, n) | n글자 추출 | SUBSTR('ABCDE', 2, 3) | BCD |
| INSTR(s, sub) | 위치 찾기 | INSTR('ABCDE', 'C') | 3 |
| LPAD/RPAD | 자리 채우기 | LPAD('1', 3, '0') | 001 |
| LTRIM/RTRIM/TRIM | 공백 제거 | TRIM(' ABC ') | ABC |
| REPLACE(s, a, b) | 문자열 교체 | REPLACE('A-B', '-', '+') | A+B |
| CONCAT(a, b) 또는 \`||\` | 문자열 연결 | 'A' \\|\\| 'B' | AB |

---

## 숫자 함수

| 함수 | 설명 | 예 | 결과 |
|---|---|---|---|
| ABS(n) | 절댓값 | ABS(-5) | 5 |
| ROUND(n, m) | 반올림 | ROUND(3.456, 1) | 3.5 |
| TRUNC(n, m) | 버림 | TRUNC(3.456, 1) | 3.4 |
| MOD(a, b) | 나머지 | MOD(10, 3) | 1 |
| CEIL(n) | 올림 | CEIL(3.1) | 4 |
| FLOOR(n) | 내림 | FLOOR(3.9) | 3 |
| POWER(a, b) | 거듭제곱 | POWER(2, 3) | 8 |
| SIGN(n) | 부호(-1/0/1) | SIGN(-5) | -1 |

---

## 날짜 함수

| 함수 | 설명 |
|---|---|
| SYSDATE | 현재 날짜+시간 |
| CURRENT_DATE | 세션 기준 현재 |
| ADD_MONTHS(d, n) | 개월 더하기 |
| MONTHS_BETWEEN(d1, d2) | 개월 차이 |
| LAST_DAY(d) | 그 달의 마지막 날 |
| NEXT_DAY(d, '월') | 다음 월요일 |
| EXTRACT(YEAR FROM d) | 연도 추출 |
| TRUNC(d, 'MM') | 월 단위 자름 |

날짜 - 날짜 = 일수(숫자), 날짜 + 숫자 = 날짜

---

## 변환 함수

\`\`\`
        TO_CHAR
[숫자] ────────→ [문자] ←────────  [날짜]
   ↑              │                  │
   └─ TO_NUMBER ──┘                  │
                  │                  │
                  └──── TO_DATE ────→┘
\`\`\`

| 함수 | 변환 | 예 |
|---|---|---|
| TO_CHAR(d, 'YYYY-MM-DD') | 날짜 → 문자 | 2026-04-26 |
| TO_CHAR(n, '999,999') | 숫자 → 문자 | 1,000 |
| TO_NUMBER('1234') | 문자 → 숫자 | 1234 |
| TO_DATE('20260426', 'YYYYMMDD') | 문자 → 날짜 | 2026-04-26 |

| 종류 | 의미 |
|---|---|
| 명시적 형변환 | TO_CHAR, TO_NUMBER, TO_DATE 직접 호출 |
| 암시적 형변환 | DBMS가 자동 변환(의도치 않은 결과 위험) |

---

## NULL 관련 함수

| 함수 | 설명 |
|---|---|
| NVL(A, B) | A가 NULL이면 B |
| NVL2(A, B, C) | A가 NULL 아니면 B, NULL이면 C |
| NULLIF(A, B) | A=B면 NULL 반환 |
| COALESCE(A, B, C, …) | NULL 아닌 첫 번째 값 |

---

## [SQL 실전]

### 조건 함수 — CASE / DECODE

\`\`\`sql
-- DECODE (Oracle 전용, 동등비교만 가능)
SELECT NAME,
       DECODE(GENDER, 'M', '남자', 'F', '여자', '기타') AS GENDER_KOR
FROM   MEMBER;

-- CASE (표준, 모든 비교 가능)
SELECT NAME,
       CASE WHEN SAL >= 5000 THEN '고급'
            WHEN SAL >= 3000 THEN '중급'
            ELSE '초급'
       END AS LEVEL
FROM   EMP;
\`\`\`

| 함수 | 동등비교 | 범위비교 |
|---|---|---|
| DECODE | 가능 | 불가 |
| CASE | 가능 | 가능 |

---

## 다중행(집계) 함수

여러 행을 묶어 하나의 결과로 요약. 자세한 분석은 [그룹함수](./2-2-3_그룹함수.md) 챕터 참고. 보통 [WHERE](./2-1-4_WHERE절.md) 이후의 GROUP BY 와 함께 사용.

| 함수 | 의미 | NULL 처리 |
|---|---|---|
| COUNT(*) | 모든 행 수 | NULL 포함 |
| COUNT(col) | 컬럼 NULL 제외 | NULL 제외 |
| SUM(col) | 합 | NULL 무시 |
| AVG(col) | 평균 | NULL 무시 |
| MAX(col) | 최대 | NULL 무시 |
| MIN(col) | 최소 | NULL 무시 |

---

## [Before & After] 함수 활용

EMP 의 NAME(소문자), HIRE_DATE(날짜형), SAL+COMM(NULL 가능)을 한 번에 가공해야 하는 상황. 김철수는 COMM 이 NULL, 이영희는 200. 문자·날짜·NULL 함수를 결합해 "대문자 이름 / 입사연도 / 총급여" 를 동시에 뽑음.

### EMP

| NAME | HIRE_DATE | SAL | COMM |
|---|---|---|---|
| 김철수 | 2020-03-15 | 3000 | NULL |
| 이영희 | 2022-08-20 | 4000 | 200 |

### 쿼리

\`\`\`sql
SELECT  UPPER(NAME)              AS UPPER_NAME,
        TO_CHAR(HIRE_DATE,'YYYY')   AS HIRE_YEAR,
        SAL + NVL(COMM, 0)         AS TOTAL
FROM    EMP;
\`\`\`

| UPPER_NAME | HIRE_YEAR | TOTAL |
|---|---|---|
| 김철수 | 2020 | 3000 |
| 이영희 | 2022 | 4200 |

UPPER 는 문자형, TO_CHAR(HIRE_DATE,'YYYY') 는 날짜→문자, NVL(COMM,0) 은 NULL 안전 산술 — 함수마다 다루는 도메인이 다름. NVL 을 빼면 김철수의 TOTAL 이 NULL 로 사라지는 사고 발생. 출력 형식·표시 단위·NULL 처리를 모두 함수 단계에서 정리해 SELECT 결과를 화면에 바로 쓸 수 있는 형태로 만드는 것이 안전

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| \`SUBSTR('ABCDE', 2, 3)\` | 'BCD' (2번째부터 3글자) |
| Oracle \`LENGTH('한글')\` | 2(글자수). LENGTHB는 바이트수 |
| \`MOD(10, 3)\` | 1 |
| \`ROUND(3.456, 2)\` | 3.46 |
| \`TRUNC(3.456, 2)\` | 3.45 |
| DECODE로 범위비교 가능? | 불가 |
| 날짜 - 날짜 결과 | 숫자(일수) |
| \`NVL2(NULL, 'A', 'B')\` | 'B' |
| 암시적 형변환 권장? | 명시적이 안전 |

단골: COUNT(*) vs COUNT(컬럼) 차이를 묻는 문제. NULL 처리 주의

---

> **30초 시험 직전 정리**
> · 함수 = **단일행** (1행 → 1결과) vs **다중행 = 집계** (여러 행 → 1결과)
> · SUBSTR 인덱스 = **1부터** 시작 (Oracle)
> · LENGTH = 글자 수 / LENGTHB = 바이트 수 (한글 1자 = 3바이트)
> · ROUND vs TRUNC = 반올림 vs 버림
> · DECODE = **동등 비교만** / CASE = **모든 비교 (범위 가능)**
> · NVL(A, B) / NVL2(A, B, C) / NULLIF(A, B) / COALESCE(A, B, ...)
> · 집계 함수는 **NULL 무시** (단 COUNT(*) 만 NULL 포함)
> · 명시적 형변환 (TO_*) 권장 — 암시적은 의도치 않은 결과 위험
`,
  c214: `# 2-1-4. WHERE 절

## [핵심 요약]

> WHERE 절은 어떤 행만 가져올지 조건을 거는 필터다. 테이블 전체에서 원하는 행만 골라낸다.

ex) 옷가게에서 "검은색(\`= '검정'\`)·5만원 이하(\`<= 50000\`)·M 또는 L 사이즈(\`IN ('M','L')\`)" 라고 조건을 거는 것과 같음

---

## [개념 도식화] WHERE의 위치

[SELECT 문](./2-1-2_SELECT문.md) 의 6대 절 중 행 단위 필터 역할

\`\`\`mermaid
flowchart LR
    A[전체 행<br/>1000행] -->|WHERE 조건 필터| B[조건 만족<br/>100행]
\`\`\`

---

## WHERE 절의 연산자

### 비교 연산자

| 연산자 | 의미 |
|---|---|
| \`=\` | 같다 |
| \`!=\`, \`<>\`, \`^=\` | 다르다 |
| \`>\`, \`>=\`, \`<\`, \`<=\` | 크다/작다 |

### 논리 연산자

| 연산자 | 의미 | 비유 |
|---|---|---|
| AND | 둘 다 | 검정색 그리고 5만원 이하 |
| OR | 둘 중 하나 | 검정색 또는 흰색 |
| NOT | 부정 | 검정색이 아닌 |

### SQL 연산자

| 연산자 | 의미 | 예 |
|---|---|---|
| BETWEEN A AND B | A ≤ x ≤ B | \`BETWEEN 100 AND 200\` |
| IN (a, b, c) | 그 안에 있으면 (값 목록 또는 [서브쿼리](./2-2-1_서브쿼리.md) 가능) | \`IN ('M', 'F')\` |
| LIKE '패턴' | 문자 패턴 매칭 | \`LIKE '김%'\` |
| IS NULL | NULL인지 | \`IS NULL\` |

### 부정형

NOT BETWEEN, NOT IN, NOT LIKE, IS NOT NULL

---

## LIKE 연산자의 와일드카드

| 기호 | 의미 | 예 |
|---|---|---|
| \`%\` | 0개 이상의 문자 | \`'김%'\` → '김', '김철수', '김OO' |
| \`_\` | 정확히 1개 문자 | \`'김_수'\` → '김철수', '김영수' |

\`\`\`sql
-- 김으로 시작
SELECT * FROM EMP WHERE NAME LIKE '김%';

-- 정확히 3글자, 가운데가 '철'
SELECT * FROM EMP WHERE NAME LIKE '_철_';

-- 와일드카드 자체를 검색하려면 ESCAPE
SELECT * FROM ITEM WHERE NAME LIKE '%50\\%%' ESCAPE '\\';
\`\`\`

---

## NULL 비교

| 잘못된 표현 | 올바른 표현 |
|---|---|
| \`WHERE COL = NULL\` | \`WHERE COL IS NULL\` |
| \`WHERE COL != NULL\` | \`WHERE COL IS NOT NULL\` |

NULL 과의 비교는 항상 UNKNOWN → 결과가 비어버림

---

## 연산자 우선순위

\`\`\`
1. 산술 ( *, /, +, - )
2. 연결 ( || )
3. 비교 ( =, <, > 등)
4. NOT
5. AND
6. OR
\`\`\`

AND 가 OR 보다 먼저 처리됨. \`A OR B AND C\` = \`A OR (B AND C)\`

\`\`\`sql
-- 의도: 부서가 D1이거나 D2 이면서 급여 3000 이상
-- 잘못된 예
WHERE DEPT = 'D1' OR DEPT = 'D2' AND SAL >= 3000
-- AND 먼저 해석되어 D1 또는 (D2 AND SAL>=3000)이 됨

-- 올바른 예
WHERE (DEPT = 'D1' OR DEPT = 'D2') AND SAL >= 3000
\`\`\`

---

## [SQL 실전]

\`\`\`sql
-- 비교
SELECT * FROM EMP WHERE SAL >= 3000;

-- BETWEEN
SELECT * FROM EMP WHERE SAL BETWEEN 3000 AND 5000;

-- IN
SELECT * FROM EMP WHERE DEPT_ID IN ('D1', 'D2', 'D3');

-- LIKE
SELECT * FROM EMP WHERE NAME LIKE '김%';

-- IS NULL
SELECT * FROM EMP WHERE COMM IS NULL;

-- AND/OR
SELECT * FROM EMP WHERE DEPT_ID = 'D1' AND SAL >= 3000;
\`\`\`

---

## [Before & After]

EMP 4명에서 부서·급여·이름·커미션 조건을 조합해 행을 골라내는 상황. IN·LIKE·IS NULL 결합 방식에 따라 결과 행이 어떻게 달라지는지 비교.

### EMP

| EMP_ID | NAME | DEPT_ID | SAL | COMM |
|---|---|---|---|---|
| 1 | 김철수 | D1 | 3000 | NULL |
| 2 | 이영희 | D1 | 4500 | 200 |
| 3 | 박민수 | D2 | 2500 | NULL |
| 4 | 김영수 | D3 | 5000 | 500 |

### \`WHERE DEPT_ID IN ('D1', 'D3') AND SAL >= 3000\`

| EMP_ID | NAME | DEPT_ID | SAL |
|---|---|---|---|
| 1 | 김철수 | D1 | 3000 |
| 2 | 이영희 | D1 | 4500 |
| 4 | 김영수 | D3 | 5000 |

### \`WHERE NAME LIKE '김%' AND COMM IS NULL\`

| EMP_ID | NAME |
|---|---|
| 1 | 김철수 |

WHERE 는 행을 하나씩 검사해 조건이 TRUE 인 행만 통과 → 조건 표현이 곧 결과 집합. NULL 비교는 항상 UNKNOWN 이라 \`=\`·\`!=\` 로는 잡히지 않으니 IS NULL 사용. IN 으로 다중 선택과 비교 연산자를 AND 로 묶어 조건 세분화. "정확히 어떤 행이 필요한가" 를 먼저 말로 풀어 쓴 뒤 그대로 연산자로 옮기면 실수가 줄어듦

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| \`WHERE COMM = NULL\`의 결과 | 0건. IS NULL을 사용 |
| \`BETWEEN 100 AND 200\` | 100, 200 포함(양 끝 포함) |
| \`LIKE '_김%'\` | 두 번째 글자가 '김'으로 시작 |
| \`IN (a, b, c)\`은 OR과 같음 | 옳음 |
| AND와 OR 우선순위 | AND가 먼저 |
| \`NOT IN\` 사용 시 NULL 주의 | NULL이 포함되면 결과가 비어버릴 수 있음 |
| \`_\`와 \`%\` 차이 | \`_\`은 1글자, \`%\`은 0개 이상 |

단골 함정: \`WHERE A NOT IN (1, 2, NULL)\` 은 항상 거짓 → 결과 0건

---

> **30초 시험 직전 정리**
> · BETWEEN A AND B = **A·B 양 끝 포함** (A ≤ x ≤ B)
> · IN(a,b,c) = OR 의 단축형 / NOT IN + NULL = **항상 거짓** (결과 0건)
> · LIKE: \`%\` = 0개 이상 / \`_\` = 정확히 1개. 자체 검색은 ESCAPE
> · NULL 비교는 **IS NULL / IS NOT NULL** 만 가능 (\`= NULL\` 항상 거짓)
> · 우선순위: 산술 > 연결(||) > 비교 > NOT > AND > OR
> · 함정: \`A=1 OR B=2 AND C=3\` = \`A=1 OR (B=2 AND C=3)\` (AND 가 먼저)
`,
  c215: `# 2-1-5. GROUP BY, HAVING 절

## [핵심 요약]

> GROUP BY는 데이터를 그룹별로 묶는 절, HAVING은 묶인 그룹에 조건을 거는 절이다.

ex) 영수증 50장을 "분류별로 묶어 합계(GROUP BY 분류)" → "합계 10만원 넘는 분류만(HAVING 합계 > 10만)" 으로 거르는 것과 같음

---

## [개념 도식화] WHERE vs HAVING의 위치

\`\`\`mermaid
flowchart LR
    A[원본 행] -->|WHERE<br/>행 단위 필터| B[거른 행]
    B -->|GROUP BY<br/>그룹화| C[그룹들]
    C -->|HAVING<br/>그룹 단위 필터| D[최종 그룹]
\`\`\`

실행 순서: **FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY**

---

## GROUP BY의 핵심 규칙

GROUP BY 에 명시되지 않은 컬럼은 SELECT 절에 그대로 사용 불가. 집계함수로만 사용 가능

\`\`\`sql
-- 잘못된 쿼리
SELECT DEPT_ID, NAME, AVG(SAL)   -- NAME이 GROUP BY에 없음
FROM   EMP
GROUP BY DEPT_ID;

-- 옳은 쿼리
SELECT DEPT_ID, AVG(SAL)
FROM   EMP
GROUP BY DEPT_ID;
\`\`\`

---

## 집계 함수 복습

GROUP BY 와 함께 쓰이는 [그룹함수](./2-2-3_그룹함수.md) 의 기본형. 행 단위 분석이 필요하면 [윈도우함수](./2-2-4_윈도우함수.md) 로 확장

| 함수 | 설명 |
|---|---|
| COUNT(*) | 행 수(NULL 포함) |
| COUNT(컬럼) | NULL 제외 행 수 |
| SUM(컬럼) | 합 |
| AVG(컬럼) | 평균 |
| MAX/MIN(컬럼) | 최대/최소 |

---

## WHERE vs HAVING

[WHERE 절](./2-1-4_WHERE절.md) 이 행 단위 필터라면, HAVING 은 그룹 단위 필터

| 비교 | WHERE | HAVING |
|---|---|---|
| 적용 시점 | 그룹화 전(행 단위) | 그룹화 후(그룹 단위) |
| 집계함수 사용 | 불가 | 가능 |
| 예 | \`WHERE SAL > 3000\` | \`HAVING AVG(SAL) > 3000\` |

\`\`\`sql
-- 잘못된 예: WHERE에 집계함수
SELECT DEPT_ID, AVG(SAL)
FROM   EMP
WHERE  AVG(SAL) > 3000;     -- 에러

-- 옳은 예: HAVING에 집계함수
SELECT DEPT_ID, AVG(SAL)
FROM   EMP
GROUP BY DEPT_ID
HAVING AVG(SAL) > 3000;
\`\`\`

---

## [SQL 실전]

\`\`\`sql
-- 부서별 인원수와 평균급여
SELECT  DEPT_ID,
        COUNT(*)    AS CNT,
        AVG(SAL)    AS AVG_SAL
FROM    EMP
GROUP BY DEPT_ID;

-- 부서별 평균급여가 3500 이상인 부서만
SELECT  DEPT_ID, AVG(SAL)
FROM    EMP
GROUP BY DEPT_ID
HAVING  AVG(SAL) >= 3500;

-- WHERE + GROUP BY + HAVING 조합
SELECT  DEPT_ID, AVG(SAL)
FROM    EMP
WHERE   HIRE_DATE >= DATE '2020-01-01'   -- 2020년 이후 입사자만
GROUP BY DEPT_ID
HAVING  COUNT(*) >= 3;                   -- 3명 이상인 부서
\`\`\`

---

## [Before & After]

EMP 5명을 D1·D2·D3 세 부서로 묶어 부서별 인원수·평균급여를 구하고, 그중 평균급여 3500 이상 부서만 남기는 상황. GROUP BY 로 그룹화한 단계와 HAVING 으로 거른 단계의 결과를 단계별로 비교.

### EMP

| NAME | DEPT_ID | SAL |
|---|---|---|
| 김 | D1 | 3000 |
| 이 | D1 | 4000 |
| 박 | D2 | 2500 |
| 최 | D2 | 3500 |
| 홍 | D3 | 5000 |

### \`GROUP BY DEPT_ID\`

| DEPT_ID | COUNT(*) | AVG(SAL) |
|---|---|---|
| D1 | 2 | 3500 |
| D2 | 2 | 3000 |
| D3 | 1 | 5000 |

### \`HAVING AVG(SAL) >= 3500\`

| DEPT_ID | AVG(SAL) |
|---|---|
| D1 | 3500 |
| D3 | 5000 |

GROUP BY 는 행을 키 단위로 접어 한 그룹당 한 행으로 만들고, 그 위에서 집계 함수 동작. 평균급여 같은 집계 결과를 조건으로 걸려면 행 단위 WHERE 가 아니라 그룹 단위 HAVING 사용. "어떤 행을 걸러야 하나" 는 WHERE, "묶은 결과 중 어떤 그룹을 남기나" 는 HAVING — 필터를 어느 단계에 둘지 먼저 정하고 쿼리를 짜는 것이 안전

---

## 다중 컬럼 그룹화

\`\`\`sql
SELECT  DEPT_ID, JOB, COUNT(*)
FROM    EMP
GROUP BY DEPT_ID, JOB;
\`\`\`

| DEPT_ID | JOB | COUNT(*) |
|---|---|---|
| D1 | MGR | 1 |
| D1 | CLERK | 1 |
| D2 | CLERK | 2 |

---

## SELECT - GROUP BY 매칭 규칙

| SELECT 절에 쓸 수 있는 것 | 가능 여부 |
|---|---|
| GROUP BY 컬럼 | 가능 |
| 집계 함수(SUM, AVG 등) | 가능 |
| 상수, 함수, 표현식 | 가능 |
| 그 외 일반 컬럼 | 불가 |

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| WHERE에 집계함수 사용 | 불가 → HAVING에서 사용 |
| GROUP BY에 없는 일반 컬럼을 SELECT에 사용 | 불가 |
| HAVING은 GROUP BY 없이 쓸 수 있는가 | 가능(전체를 1개 그룹으로 봄) |
| 실행 순서 | FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY |
| COUNT(*) vs COUNT(컬럼) | NULL 포함 여부의 차이 |
| GROUP BY (A, B) 결과 행 수 | A, B 조합 개수 |

단골 함정: SELECT 에 그룹화되지 않은 컬럼이 끼어 있는 쿼리를 에러로 판별하라는 문제

---

> **30초 시험 직전 정리**
> · WHERE = **행 단위 필터** / HAVING = **그룹 단위 필터** (실행 순서상 GROUP BY 후 적용)
> · WHERE 에 집계 함수 = **불가** / HAVING 에 집계 함수 = **가능**
> · GROUP BY 에 없는 일반 컬럼을 SELECT 에 그대로 사용 = **불가**
> · GROUP BY 없이 HAVING 단독 = **가능** (전체를 1개 그룹으로 봄)
> · 결과 행 수 = GROUP BY 컬럼 조합의 개수
`,
  c216: `# 2-1-6. ORDER BY 절

## [핵심 요약]

> ORDER BY는 결과를 어떤 순서로 보여줄지 정렬하는 절이다. SELECT 문에서 가장 마지막에 실행된다.

ex) 책장 정리처럼 "가나다순(\`ASC\`)·최신순(\`DESC\`)·분야별 → 분야 내 가나다순(\`ORDER BY 분야, 책이름\`)" 식으로 1차·2차 정렬 가능

---

## [개념 도식화] ORDER BY의 위치

\`\`\`
SELECT  ...
FROM    ...
WHERE   ...
GROUP BY ...
HAVING  ...
ORDER BY ...   ← 항상 맨 마지막
\`\`\`

\`\`\`mermaid
flowchart LR
    A[정리 안 된 결과] -->|ORDER BY| B[정렬된 결과]
\`\`\`

---

## ASC vs DESC

| 키워드 | 의미 | 기본값 |
|---|---|---|
| ASC | 오름차순(작→큰, ㄱ→ㅎ) | 기본 |
| DESC | 내림차순(큰→작) | — |

\`\`\`sql
ORDER BY SAL          -- ASC(기본)
ORDER BY SAL ASC      -- 명시
ORDER BY SAL DESC     -- 내림차순
\`\`\`

---

## 정렬 기준 작성 방법 3가지

\`\`\`sql
SELECT NAME, SAL FROM EMP
-- 방법 1: 컬럼명
ORDER BY SAL DESC;

-- 방법 2: SELECT 별칭(Alias)
SELECT NAME, SAL*12 AS ANNUAL_SAL FROM EMP
ORDER BY ANNUAL_SAL DESC;

-- 방법 3: SELECT의 N번째 컬럼(정수)
SELECT NAME, SAL FROM EMP
ORDER BY 2 DESC;        -- 2번째 컬럼 = SAL
\`\`\`

ORDER BY 는 SELECT 다음에 실행되므로 별칭 사용 가능(WHERE, GROUP BY 는 불가)

---

## 다중 정렬

\`\`\`sql
-- 부서별로 묶고, 같은 부서 안에서는 급여 높은 순
SELECT NAME, DEPT_ID, SAL
FROM   EMP
ORDER BY DEPT_ID ASC, SAL DESC;
\`\`\`

\`\`\`
DEPT_ID 오름차순
   └ SAL 내림차순으로 2차 정렬
\`\`\`

---

## NULL의 정렬 순서

[NULL](./1-2-4_NULL속성의이해.md) 은 비교 불가 값이므로 정렬에서도 DBMS 마다 다르게 처리

| DBMS | ASC 기본 | DESC 기본 |
|---|---|---|
| Oracle | NULL이 마지막 | NULL이 처음 |
| MS-SQL Server | NULL이 처음 | NULL이 마지막 |

명시적 지정:

\`\`\`sql
ORDER BY COMM ASC NULLS FIRST   -- NULL 먼저
ORDER BY COMM ASC NULLS LAST    -- NULL 마지막
\`\`\`

---

## [SQL 실전]

\`\`\`sql
-- 단일 컬럼
SELECT NAME, SAL FROM EMP
ORDER BY SAL DESC;

-- 다중 컬럼
SELECT NAME, DEPT_ID, SAL FROM EMP
ORDER BY DEPT_ID, SAL DESC;

-- 별칭으로
SELECT NAME, SAL*12 AS YEAR_SAL FROM EMP
ORDER BY YEAR_SAL DESC;

-- 컬럼 번호로
SELECT NAME, DEPT_ID, SAL FROM EMP
ORDER BY 3 DESC, 2 ASC;
\`\`\`

---

## [Before & After]

EMP 5명을 부서별로 묶고, 같은 부서 안에서는 급여가 높은 사람이 먼저 보이도록 정렬하는 상황. DEPT_ID(오름차순)와 SAL(내림차순)을 한 ORDER BY 절에 섞었을 때 결과 순서가 어떻게 결정되는지 확인.

### EMP

| NAME | DEPT_ID | SAL |
|---|---|---|
| 김 | D1 | 3000 |
| 이 | D2 | 4000 |
| 박 | D1 | 2500 |
| 최 | D2 | 4500 |
| 홍 | D1 | 4000 |

### \`ORDER BY DEPT_ID ASC, SAL DESC\`

| NAME | DEPT_ID | SAL |
|---|---|---|
| 홍 | D1 | 4000 |
| 김 | D1 | 3000 |
| 박 | D1 | 2500 |
| 최 | D2 | 4500 |
| 이 | D2 | 4000 |

부서 오름차순 → 부서 내 급여 내림차순으로 정렬됨

ORDER BY 는 나열된 컬럼 순서대로 1차·2차·3차 정렬을 적용하고, ASC/DESC 는 컬럼별로 따로 지정됨. "DEPT_ID 는 오름차순, SAL 은 내림차순" 처럼 키마다 방향이 달라도 한 줄에 함께 쓸 수 있음. 결과 순서를 사람 눈에 보일 형태로 확정해야 하는 보고서·페이지네이션·TopN 쿼리에서는 정렬 키를 의도대로 명시해두는 것이 안전

---

## ORDER BY 주의사항

ORDER BY 는 정렬된 상위 N 개만 추출하는 [TopN 쿼리](./2-2-5_TopN쿼리.md) 의 기반

| 주의 | 설명 |
|---|---|
| 항상 맨 끝 | UNION 등 집합연산자 사용 시도 마지막에 한 번만 |
| 숫자 정렬 | 1, 2, 10... 숫자 그대로 |
| 문자 정렬 | '1', '10', '2'... 사전순 → 1, 10, 2 |
| NULL 위치 | DBMS별로 다름. 명시 권장 |
| 인덱스 활용 | 정렬 작업에 인덱스가 활용될 수 있음 |

---

## SELECT 실행 순서

| 순서 | 절 |
|---|---|
| 1 | FROM |
| 2 | WHERE |
| 3 | GROUP BY |
| 4 | HAVING |
| 5 | SELECT |
| 6 | ORDER BY |

ORDER BY 가 마지막이라 별칭과 컬럼번호 사용 가능

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| ORDER BY 기본값 | ASC(오름차순) |
| ORDER BY에서 SELECT 별칭 사용 | 가능 |
| ORDER BY에서 컬럼 번호(숫자) 사용 | 가능 |
| Oracle ORDER BY ASC, NULL의 위치 | 마지막 |
| MS-SQL ORDER BY ASC, NULL의 위치 | 처음 |
| \`ORDER BY DEPT, SAL DESC\`의 의미 | DEPT는 ASC(생략값), SAL만 DESC |
| 집합연산자(UNION) 후 ORDER BY 위치 | 맨 마지막에 한 번만 |

단골 함정: 문자형 컬럼에 숫자값이 들어 있을 때 정렬 순서. 예) '1', '10', '2' 를 정렬하면 사전순으로 '1', '10', '2'

---

> **30초 시험 직전 정리**
> · 기본값 = **ASC** (오름차순). DESC 명시 필수
> · ORDER BY 에는 **컬럼명 / 별칭 / 컬럼 번호** 모두 사용 가능 (가장 마지막 실행이라)
> · \`ORDER BY DEPT, SAL DESC\` = DEPT ASC + SAL DESC (정렬 옵션은 컬럼별 개별 적용)
> · NULL 위치: Oracle ASC = **마지막** / MS-SQL ASC = **처음**. 명시는 \`NULLS FIRST/LAST\`
> · 문자형 정렬 = **사전순** ('1', '10', '2' 순). 숫자 정렬 원하면 TO_NUMBER 후
> · UNION 등 집합연산 후 ORDER BY = **맨 마지막에 한 번만**
`,
  c217: `# 2-1-7. 조인(JOIN)

## [핵심 요약]

> 조인은 두 개 이상의 테이블을 키로 연결해 한 결과로 합치는 작업이다.

ex) 영화관 티켓(영화 정보)과 회원증(회원 정보)을 회원번호라는 공통 키로 매칭해 "누가 어떤 영화를 봤는지" 알아내는 것과 같음

---

## [개념 도식화] 조인의 기본 원리

조인은 모델링 단계의 [관계](./1-1-4_관계.md) 가 SQL 단에서 실제로 사용되는 방식. 이론적 배경은 [관계와 조인의 이해](./1-2-2_관계와조인의이해.md) 참고

\`\`\`mermaid
flowchart LR
    subgraph MEMBER
      M1[M01 · 김]
      M2[M02 · 이]
    end
    subgraph ORDERS
      O1[M01 · 주문 #1]
      O2[M01 · 주문 #2]
      O3[M02 · 주문 #3]
    end
    M1 <-->|공통 키 M_ID| O1
    M1 <--> O2
    M2 <--> O3
\`\`\`

---

## 조인의 종류

\`\`\`mermaid
flowchart TD
    J[조인] --> E[EQUI JOIN<br/>= 으로 연결<br/>가장 흔함]
    J --> N[Non-EQUI JOIN<br/>BETWEEN / >, <]
    J --> S[SELF JOIN<br/>같은 테이블<br/>사원-매니저]
    J --> O[OUTER JOIN<br/>LEFT / RIGHT / FULL<br/>한쪽 다 포함]
    J --> C[CROSS JOIN<br/>모든 조합<br/>카티션 곱]
\`\`\`

| 종류 | 의미 | 예 |
|---|---|---|
| EQUI 조인 | \`=\`으로 연결(가장 흔함) | \`A.ID = B.ID\` |
| Non-EQUI | \`=\` 외(BETWEEN 등) | \`SAL BETWEEN LO AND HI\` |
| SELF JOIN | 같은 테이블끼리 | 사원-매니저(같은 EMP) |
| OUTER JOIN | 매칭 안 돼도 한쪽 다 포함 | LEFT/RIGHT/FULL |
| CROSS JOIN | 모든 조합(카티션) | 두 테이블의 곱 |

---

## EQUI JOIN — 가장 기본

### 전통적 방식 (Oracle)

\`\`\`sql
SELECT  E.NAME, D.DEPT_NAME
FROM    EMP E, DEPT D
WHERE   E.DEPT_ID = D.DEPT_ID;     -- 조인 조건
\`\`\`

### 표준 ANSI 방식

\`\`\`sql
SELECT  E.NAME, D.DEPT_NAME
FROM    EMP  E
        JOIN DEPT D ON E.DEPT_ID = D.DEPT_ID;
\`\`\`

자세한 [표준 조인](./2-1-8_표준조인.md) 은 다음 챕터 참고

---

## Non-EQUI JOIN — 범위 매칭

\`\`\`sql
-- 급여 등급표를 조인
SELECT  E.NAME, E.SAL, S.GRADE
FROM    EMP E, SALGRADE S
WHERE   E.SAL BETWEEN S.LOSAL AND S.HISAL;
\`\`\`

| EMP | SALGRADE |
|---|---|
| (사원, 급여) | (등급, 하한, 상한) |

---

## SELF JOIN — 자기 자신

한 테이블에 부모-자식 관계가 들어있을 때 사용(예: 사원-매니저). 계층 구조 조회와 함께 [계층형 질의와 셀프조인](./2-2-6_계층형질의와셀프조인.md) 챕터에서 심화.

\`\`\`sql
SELECT  E.NAME    AS 사원,
        M.NAME    AS 매니저
FROM    EMP E, EMP M
WHERE   E.MGR_ID = M.EMP_ID;
\`\`\`

| EMP | EMP_ID |
|---|---|
| 김(매니저: 이) | E1 |
| 이(매니저: 박) | E2 |
| 박 | E3 |

---

## OUTER JOIN — 한쪽 다 포함

\`\`\`
LEFT  OUTER : A 모두 + 매칭되는 B    (A 에 없으면 결과에서 빠짐, B 에 없으면 NULL)
RIGHT OUTER : B 모두 + 매칭되는 A    (B 에 없으면 결과에서 빠짐, A 에 없으면 NULL)
FULL  OUTER : A·B 모두                (한쪽이라도 있으면 결과에 포함, 없는 쪽은 NULL)
\`\`\`

\`\`\`sql
-- LEFT OUTER (왼쪽 다)
SELECT M.NAME, O.ORDER_ID
FROM   MEMBER M LEFT OUTER JOIN ORDERS O
       ON M.MEMBER_ID = O.MEMBER_ID;

-- Oracle 전통 방식 (+) 기호
SELECT M.NAME, O.ORDER_ID
FROM   MEMBER M, ORDERS O
WHERE  M.MEMBER_ID = O.MEMBER_ID(+);   -- (+)는 NULL 채워질 쪽
\`\`\`

---

## CROSS JOIN — 모든 조합(카티션 곱)

\`\`\`sql
SELECT * FROM A CROSS JOIN B;
-- A 3행, B 2행 → 6행 (3×2)
\`\`\`

조인 조건을 빼먹어도 발생 → 의도치 않으면 위험

---

## [SQL 실전] 다양한 조인

\`\`\`sql
-- INNER JOIN
SELECT E.NAME, D.DEPT_NAME
FROM   EMP E JOIN DEPT D ON E.DEPT_ID = D.DEPT_ID;

-- 3개 테이블 조인
SELECT E.NAME, D.DEPT_NAME, L.CITY
FROM   EMP E
       JOIN DEPT D     ON E.DEPT_ID = D.DEPT_ID
       JOIN LOCATION L ON D.LOC_ID  = L.LOC_ID;

-- LEFT OUTER JOIN
SELECT M.NAME, O.ORDER_ID
FROM   MEMBER M LEFT JOIN ORDERS O ON M.MEMBER_ID = O.MEMBER_ID;
\`\`\`

---

## [Before & After]

EMP 에는 부서가 없는 박, DEPT 에는 아무도 속하지 않은 D3(개발부)가 있는 상황. 같은 두 테이블에 INNER / LEFT / RIGHT / FULL OUTER 조인을 차례로 적용해 결과 행이 늘었다 줄었다 하는 양상 비교.

### EMP

| EMP_ID | NAME | DEPT_ID |
|---|---|---|
| 1 | 김 | D1 |
| 2 | 이 | D2 |
| 3 | 박 | NULL |

### DEPT

| DEPT_ID | DEPT_NAME |
|---|---|
| D1 | 영업부 |
| D2 | 인사부 |
| D3 | 개발부 |

### INNER JOIN

| NAME | DEPT_NAME |
|---|---|
| 김 | 영업부 |
| 이 | 인사부 |

### LEFT OUTER (EMP 기준)

| NAME | DEPT_NAME |
|---|---|
| 김 | 영업부 |
| 이 | 인사부 |
| 박 | NULL |

### RIGHT OUTER (DEPT 기준)

| NAME | DEPT_NAME |
|---|---|
| 김 | 영업부 |
| 이 | 인사부 |
| NULL | 개발부 |

### FULL OUTER

| NAME | DEPT_NAME |
|---|---|
| 김 | 영업부 |
| 이 | 인사부 |
| 박 | NULL |
| NULL | 개발부 |

INNER 조인은 양쪽 모두 매칭되는 행만 남기므로 박과 개발부가 동시에 사라짐. LEFT/RIGHT OUTER 는 기준 쪽을 다 살리고 반대쪽이 비면 NULL 을 채움. FULL OUTER 는 양쪽 모두 살림. "어느 쪽에 매칭이 없는 데이터까지 보고 싶은가" 가 INNER vs OUTER 선택 기준. OUTER 조인 후 WHERE 에 비기준 컬럼 조건을 걸면 NULL 행이 도로 빠지므로 추가 조건은 ON 절에 두는 것이 안전

---

## 조인 시 주의사항

| 함정 | 결과 | 해결 |
|---|---|---|
| 조인 조건 누락 | 카티션 곱(행수 폭발) | ON 또는 WHERE 명시 |
| OUTER JOIN과 WHERE 혼용 | NULL 행 사라짐 | 조건은 ON에 |
| N+1 테이블 조인 | N개 조인 조건 필요 | 잘 매핑 |

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| 조인 조건이 빠지면 | 카티션 곱(Cartesian Product) |
| Oracle 외부조인 표기 | \`(+)\` 기호(NULL 보충될 쪽에 붙임) |
| N개 테이블 조인 시 조인 조건 수 | 최소 N-1개 |
| Non-EQUI 조인 예 | BETWEEN, >, < 등 |
| SELF 조인은 같은 테이블을 두 번 사용? | 그렇다(별칭 필수) |
| INNER vs OUTER 차이 | OUTER는 매칭 안 되는 행도 NULL과 함께 포함 |

단골 함정: Oracle 의 \`(+)\` 위치는 NULL 을 채울 쪽(없을 수 있는 쪽)에 붙임

---

> **30초 시험 직전 정리**
> · 5종류: **EQUI / Non-EQUI / SELF / OUTER / CROSS**
> · 조인 조건 누락 = **카티션 곱** (행 수 폭발: A행 × B행)
> · N개 테이블 조인 = 조인 조건 **최소 N-1 개**
> · SELF 조인 = **별칭 필수** (같은 테이블을 두 번 사용)
> · Oracle \`(+)\` 표기 = **NULL 채울 쪽** (없을 수 있는 쪽)에 붙임
> · OUTER JOIN 추가 조건 = **ON 절에** (WHERE 두면 NULL 행 빠져 INNER 와 같아짐)
`,
  c218: `# 2-1-8. 표준 조인 (ANSI Standard Join)

## [핵심 요약]

> 표준 조인은 ANSI/ISO에서 정의한, 모든 DBMS에서 통하는 조인 문법이다. \`JOIN ... ON\` 키워드를 사용한다. 앞 챕터의 [조인](./2-1-7_조인.md) 종류를 표준 문법으로 정리.

---

## [개념 도식화] 표준 조인의 종류

\`\`\`mermaid
flowchart TD
    A[표준 조인 ANSI] --> I[INNER JOIN<br/>양쪽 매칭만<br/>INNER 생략 가능]
    A --> L[LEFT OUTER JOIN<br/>왼쪽 모두 + 매칭]
    A --> R[RIGHT OUTER JOIN<br/>오른쪽 모두 + 매칭]
    A --> F[FULL OUTER JOIN<br/>양쪽 모두 + NULL]
    A --> C[CROSS JOIN<br/>모든 조합<br/>조건 없음]
    A --> N[NATURAL JOIN<br/>같은 이름 자동]
    A --> U[USING 컬럼<br/>명시 컬럼만]
\`\`\`

---

## [SQL 실전]

### INNER JOIN — 양쪽 매칭만

\`\`\`sql
SELECT  E.NAME, D.DEPT_NAME
FROM    EMP E
        INNER JOIN DEPT D ON E.DEPT_ID = D.DEPT_ID;
        -- INNER 생략 가능: JOIN ... ON
\`\`\`

\`\`\`
   EMP    DEPT
   ┌──┐  ┌──┐
   │  ╲╱  │      ← 교집합만
   └──┘  └──┘
\`\`\`

---

### LEFT OUTER JOIN — 왼쪽 다, 오른쪽 매칭

\`\`\`sql
SELECT  E.NAME, D.DEPT_NAME
FROM    EMP E
        LEFT OUTER JOIN DEPT D ON E.DEPT_ID = D.DEPT_ID;
        -- OUTER 생략 가능: LEFT JOIN
\`\`\`

\`\`\`
   EMP    DEPT
   ┌──┐  ┌──┐
   │██╲╱  │      ← 왼쪽 모두 + 매칭된 오른쪽
   └──┘  └──┘
\`\`\`

---

### RIGHT OUTER JOIN — 오른쪽 다, 왼쪽 매칭

\`\`\`sql
SELECT  E.NAME, D.DEPT_NAME
FROM    EMP E
        RIGHT OUTER JOIN DEPT D ON E.DEPT_ID = D.DEPT_ID;
\`\`\`

---

### FULL OUTER JOIN — 양쪽 다

\`\`\`sql
SELECT  E.NAME, D.DEPT_NAME
FROM    EMP E
        FULL OUTER JOIN DEPT D ON E.DEPT_ID = D.DEPT_ID;
\`\`\`

\`\`\`
   EMP    DEPT
   ┌──┐  ┌──┐
   │██╲╱██│      ← 양쪽 모두(한쪽만 있어도 NULL 채워서)
   └──┘  └──┘
\`\`\`

---

### CROSS JOIN — 모든 조합

\`\`\`sql
SELECT  *
FROM    EMP E CROSS JOIN DEPT D;
\`\`\`

조인 조건 없음. 결과는 A행수 × B행수

---

### NATURAL JOIN — 같은 이름 컬럼 자동 조인

\`\`\`sql
SELECT  *
FROM    EMP NATURAL JOIN DEPT;     -- DEPT_ID로 자동 조인
\`\`\`

| 특징 | 설명 |
|---|---|
| 자동 매칭 | 이름이 같은 컬럼으로 매칭 |
| 별칭 사용 X | 같은 이름 컬럼에 별칭 사용 불가 |
| 같은 이름 컬럼은 한 번만 등장 | 중복 제거 |

의도치 않은 컬럼이 매칭될 수 있어 실무에서는 잘 안 씀

---

### USING 절 — 명시적 컬럼 매칭

\`\`\`sql
SELECT  NAME, DEPT_NAME, DEPT_ID
FROM    EMP JOIN DEPT USING (DEPT_ID);
\`\`\`

| NATURAL vs USING | 차이 |
|---|---|
| NATURAL | 자동(모든 같은 이름) |
| USING | 지정한 컬럼만 |

USING 으로 조인된 컬럼은 테이블 별칭 사용 불가(예: \`E.DEPT_ID\` 불가)

---

### ON 절 vs WHERE 절 (OUTER JOIN 핵심)

\`\`\`sql
-- 잘못된 예: OUTER JOIN인데 WHERE로 필터하면 NULL 행이 빠짐
SELECT M.NAME, O.ORDER_ID
FROM   MEMBER M LEFT JOIN ORDERS O ON M.MEMBER_ID = O.MEMBER_ID
WHERE  O.AMOUNT > 1000;     -- 주문 없는 회원이 사라짐

-- 올바른 예: 조인 조건에 함께
SELECT M.NAME, O.ORDER_ID
FROM   MEMBER M LEFT JOIN ORDERS O
       ON M.MEMBER_ID = O.MEMBER_ID
       AND O.AMOUNT > 1000;
\`\`\`

---

## [Before & After] 표준 조인 결과 비교

EMP 3행(박은 부서 미지정)과 DEPT 3행(D3 개발은 소속 사원 없음)을 같은 키 DEPT_ID 로 조인하는 상황. 동일한 두 테이블에 INNER / LEFT / RIGHT / FULL / CROSS 를 차례로 적용해 ANSI 표준 문법별 결과 행 수가 어떻게 달라지는지 비교.

### 입력

**EMP**

| EMP_ID | NAME | DEPT_ID |
|---|---|---|
| 1 | 김 | D1 |
| 2 | 이 | D2 |
| 3 | 박 | NULL |

**DEPT**

| DEPT_ID | DEPT_NAME |
|---|---|
| D1 | 영업 |
| D2 | 인사 |
| D3 | 개발 |

### INNER JOIN

| NAME | DEPT_NAME |
|---|---|
| 김 | 영업 |
| 이 | 인사 |

### LEFT JOIN

| NAME | DEPT_NAME |
|---|---|
| 김 | 영업 |
| 이 | 인사 |
| 박 | NULL |

### RIGHT JOIN

| NAME | DEPT_NAME |
|---|---|
| 김 | 영업 |
| 이 | 인사 |
| NULL | 개발 |

### FULL JOIN

| NAME | DEPT_NAME |
|---|---|
| 김 | 영업 |
| 이 | 인사 |
| 박 | NULL |
| NULL | 개발 |

### CROSS JOIN

3 × 3 = 9행

ANSI 표준은 결과 집합을 키워드 한 단어로 명확히 구분 — INNER 는 매칭만, LEFT/RIGHT 는 한쪽 기준 모두, FULL 은 양쪽 모두, CROSS 는 카티션 곱. "어느 쪽에 매칭이 없는 데이터까지 살릴 것인가" 를 먼저 정한 뒤 그에 맞는 키워드 선택. OUTER 조인의 추가 조건은 반드시 ON 절에 두어야 매칭 안 되는 NULL 행이 그대로 살아남음

---

## 정리표

| 종류 | 매칭 안 되는 왼쪽 | 매칭 안 되는 오른쪽 |
|---|---|---|
| INNER | 제외 | 제외 |
| LEFT | 포함(NULL) | 제외 |
| RIGHT | 제외 | 포함(NULL) |
| FULL | 포함 | 포함 |
| CROSS | 모든 조합 | 모든 조합 |

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| INNER JOIN 키워드 생략 가능? | 가능. \`JOIN ... ON\`이 INNER JOIN |
| OUTER 키워드 생략 가능? | 가능. \`LEFT JOIN\` = \`LEFT OUTER JOIN\` |
| NATURAL JOIN의 매칭 기준 | 이름이 같은 모든 컬럼 |
| NATURAL JOIN에서 같은 이름 컬럼에 별칭 | 사용 불가 |
| USING 절의 컬럼에 테이블 별칭 | 사용 불가 |
| OUTER JOIN의 조건은 ON vs WHERE | ON에 둬야 NULL 행이 살아남음 |
| FULL OUTER 결과 | 양쪽 매칭 안 되는 것까지 모두 |
| CROSS JOIN 행수 | A × B |

단골 함정: LEFT JOIN 인데 WHERE 에 오른쪽 조건을 거는 케이스 → 결과적으로 INNER JOIN 과 같아짐

---

> **30초 시험 직전 정리**
> · \`JOIN ... ON\` = 표준 ANSI 문법 (어느 DBMS 든 통함)
> · INNER 키워드 생략 가능 / OUTER 키워드 생략 가능 (\`LEFT JOIN\` = \`LEFT OUTER JOIN\`)
> · NATURAL JOIN = 같은 이름 컬럼 **자동 전부** 매칭. 별칭 사용 **불가**
> · USING (컬럼) = **지정한 컬럼만** 매칭. USING 컬럼에 테이블 별칭 **불가**
> · OUTER JOIN 추가 조건은 반드시 **ON** 에 (WHERE 두면 INNER 효과)
> · 결과 행: INNER ≤ LEFT/RIGHT ≤ FULL, CROSS = A × B
`,
  c221: `# 2-2-1. 서브쿼리(Sub Query)

## [핵심 요약]

> 서브쿼리는 쿼리 안에 들어 있는 또 다른 쿼리다. 메인쿼리가 사용할 값을 미리 계산해 알려주는 보조 쿼리다.

ex) "평균 가격보다 비싼 상품"을 찾을 때, 평균을 먼저 계산(서브쿼리)한 뒤 그 값보다 큰 상품을 찾는(메인쿼리) 두 단계 작업.

---

## [개념 도식화] 서브쿼리의 위치

\`\`\`sql
SELECT  컬럼들                    ← SELECT 서브쿼리(스칼라)
FROM    (서브쿼리)                 ← FROM 서브쿼리(인라인 뷰)
WHERE   컬럼 = (서브쿼리);         ← WHERE 서브쿼리(대표적)
\`\`\`

\`\`\`mermaid
flowchart TD
    M1[메인 쿼리] -->|① 값 필요| S[서브 쿼리]
    S -->|② 값 반환| M2[메인 쿼리 계속]
\`\`\`

---

## 서브쿼리의 분류

### 결과 행수에 따른 분류

| 종류 | 결과 행수 | 사용 연산자 |
|---|---|---|
| 단일행 서브쿼리 | 1행 | \`=\`, \`>\`, \`<\`, \`>=\`, \`<=\`, \`<>\` |
| 다중행 서브쿼리 | 여러 행 | IN, ANY, ALL, EXISTS |
| 다중컬럼 서브쿼리 | 여러 컬럼 | \`(A, B) IN (...)\` |

### 위치에 따른 분류

| 위치 | 이름 | 예 |
|---|---|---|
| SELECT 절 | 스칼라 서브쿼리 | \`SELECT (SELECT MAX(SAL) FROM EMP) FROM ...\` |
| FROM 절 | 인라인 뷰 | \`FROM (SELECT ...) X\` |
| [WHERE](./2-1-4_WHERE절.md) 절 | 중첩 서브쿼리 | \`WHERE SAL > (SELECT AVG(SAL)...)\` |

### 메인쿼리와의 관계

| 종류 | 의미 | 특징 |
|---|---|---|
| 비연관 서브쿼리 | 메인쿼리 컬럼 사용 안 함 | 한 번만 실행 |
| 연관 서브쿼리 | 메인쿼리 컬럼 참조 | 메인의 각 행마다 반복 실행 |

---

## [SQL 실전]

### 단일행 서브쿼리

\`\`\`sql
-- 평균 급여보다 많이 받는 사원
SELECT  NAME, SAL
FROM    EMP
WHERE   SAL > (SELECT AVG(SAL) FROM EMP);
\`\`\`

서브쿼리 결과가 1행 1값이므로 \`>\` 같은 단순 연산자 사용 가능

---

### 다중행 서브쿼리 — IN / ANY / ALL

| 연산자 | 의미 |
|---|---|
| IN (...) | 값 중 하나와 같으면 |
| > ANY(...) | 하나 이상보다 크면 → 최솟값보다 크면 됨 |
| > ALL(...) | 모두보다 크면 → 최댓값보다 커야 함 |
| EXISTS (...) | 존재하면 참 |

\`\`\`sql
-- IN
SELECT  *
FROM    EMP
WHERE   DEPT_ID IN (SELECT DEPT_ID FROM DEPT WHERE LOC = 'SEOUL');

-- > ANY: 어느 한 값보다 크면 → 최솟값보다만 크면 됨
SELECT  *
FROM    EMP
WHERE   SAL > ANY (SELECT SAL FROM EMP WHERE DEPT_ID = 'D1');

-- > ALL: 모든 값보다 커야 함 → 최댓값보다 커야 함
SELECT  *
FROM    EMP
WHERE   SAL > ALL (SELECT SAL FROM EMP WHERE DEPT_ID = 'D1');
\`\`\`

---

### EXISTS 서브쿼리

\`\`\`sql
-- 주문 내역이 있는 회원만
SELECT  *
FROM    MEMBER M
WHERE   EXISTS (SELECT 1 FROM ORDERS O WHERE O.MEMBER_ID = M.MEMBER_ID);
\`\`\`

| 특징 | 설명 |
|---|---|
| 결과가 존재하기만 하면 TRUE | 1건이라도 있으면 통과 |
| IN보다 효율적일 수 있음 | 존재 확인만 하면 됨 |

---

### 스칼라 서브쿼리 (SELECT 절)

\`\`\`sql
SELECT  E.NAME,
        E.SAL,
        (SELECT AVG(SAL) FROM EMP) AS 전체평균
FROM    EMP E;
\`\`\`

반드시 1행 1값 반환. 여러 행 반환 시 에러

---

### 인라인 뷰 (FROM 절)

\`\`\`sql
-- 부서별 평균 중 평균 4000 이상인 부서
SELECT  *
FROM    (SELECT DEPT_ID, AVG(SAL) AS AVG_SAL
         FROM   EMP
         GROUP BY DEPT_ID) X
WHERE   X.AVG_SAL >= 4000;
\`\`\`

임시 테이블처럼 사용. 별칭 필수

---

### 연관 서브쿼리

\`\`\`sql
-- 자기 부서 평균보다 많이 받는 사원
SELECT  E.NAME, E.SAL, E.DEPT_ID
FROM    EMP E
WHERE   E.SAL > (SELECT AVG(SAL)
                 FROM   EMP
                 WHERE  DEPT_ID = E.DEPT_ID);   -- E.DEPT_ID는 메인쿼리 참조
\`\`\`

| 특징 | 설명 |
|---|---|
| 메인쿼리의 각 행마다 서브쿼리 실행 | 행 수만큼 반복 |
| 성능 영향 | 큼. 필요시 [조인](./2-1-7_조인.md)으로 변환 |

---

## [Before & After]

같은 EMP 4명 데이터에 같은 비교(자기보다 평균이 큰 사람)를 적용해도, 비교 기준이 "회사 전체 평균"이냐 "자기 부서 평균"이냐에 따라 결과가 갈린다. 단일행 서브쿼리(전체 평균 1개) vs 연관 서브쿼리(부서별 평균 2개).

### EMP

| NAME | DEPT_ID | SAL |
|---|---|---|
| 김 | D1 | 3000 |
| 이 | D1 | 5000 |
| 박 | D2 | 4000 |
| 최 | D2 | 4500 |

### 단일행 서브쿼리

\`\`\`sql
SELECT NAME, SAL FROM EMP WHERE SAL > (SELECT AVG(SAL) FROM EMP);
\`\`\`

전체 평균 = 4125

| NAME | SAL |
|---|---|
| 이 | 5000 |
| 최 | 4500 |

### 연관 서브쿼리

\`\`\`sql
SELECT NAME, SAL FROM EMP E
WHERE  SAL > (SELECT AVG(SAL) FROM EMP WHERE DEPT_ID = E.DEPT_ID);
\`\`\`

부서별 평균: D1=4000, D2=4250

| NAME | SAL |
|---|---|
| 이 | 5000 |
| 최 | 4500 |

비연관(단일행) 서브쿼리는 메인쿼리와 무관하게 한 번만 실행되어 4125라는 한 값을 만들고, 모든 행이 그 값과 비교된다. 반면 연관 서브쿼리는 바깥의 E.DEPT_ID를 참조하므로 행마다 평균이 다시 계산된다(D1행은 4000, D2행은 4250 기준). 결과 행 집합이 우연히 같아 보여도 비교 기준은 행마다 다르다. "전체 기준"이면 단일행, "그룹 기준"이면 연관.

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| 다중행에 \`=\` 사용 | 에러 → IN 사용 |
| \`> ANY\` 의미 | 최솟값보다 크면 통과 |
| \`> ALL\` 의미 | 최댓값보다 커야 통과 |
| 스칼라 서브쿼리 결과 | 1행 1값 |
| 인라인 뷰의 위치 | FROM 절 |
| EXISTS는 결과를 사용? | 존재 여부만 확인 |
| 연관 서브쿼리 특징 | 메인쿼리 컬럼 참조 |

단골 함정: \`SAL > ANY(...)\`를 "모두보다 큰"으로 잘못 해석하는 보기. ANY=최소 기준, ALL=최대 기준. 두 SELECT 결과를 합치는 도구는 서브쿼리가 아니라 [집합 연산자](./2-2-2_집합연산자.md)(혼동 주의).

---

> **30초 시험 직전 정리**
> · 행수 분류: **단일행** (\`=\`,\`>\`,…) / **다중행** (IN, ANY, ALL, EXISTS) / **다중컬럼**
> · 위치 분류: **스칼라** (SELECT) / **인라인뷰** (FROM) / **중첩** (WHERE)
> · \`> ANY\` = **최솟값보다 크면** 통과 (헐거움) / \`> ALL\` = **최댓값보다 커야** 통과 (엄격)
> · EXISTS = 존재 여부만 확인 (1건이라도 있으면 TRUE)
> · 연관 서브쿼리 = 메인쿼리 컬럼 참조, **각 행마다 반복 실행** (성능 주의)
> · 인라인뷰 = **별칭 필수** (FROM 절 서브쿼리)
`,
  c222: `# 2-2-2. 집합 연산자(Set Operators)

## [핵심 요약]

> 집합 연산자는 두 개 이상의 SELECT 결과를 합치거나 비교하는 도구다. 합집합(UNION), 교집합(INTERSECT), 차집합(MINUS).

ex) 도시락 A={김밥,사과,우유}와 B={사과,빵,우유} → UNION={김밥,사과,우유,빵}, INTERSECT={사과,우유}, MINUS(A-B)={김밥}.

---

## [개념 도식화]

\`\`\`
A = { 김, 이, 박 }      B = { 이, 박, 최 }

UNION       (A ∪ B, 중복 제거)  →  { 김, 이, 박, 최 }
UNION ALL   (A + B, 중복 유지)  →  { 김, 이, 박, 이, 박, 최 }
INTERSECT   (A ∩ B, 교집합)     →  { 이, 박 }
MINUS       (A − B, 차집합)     →  { 김 }       (A 에만 있는 것)
\`\`\`

---

## 4가지 집합 연산자

| 연산자 | 의미 | 중복 처리 |
|---|---|---|
| UNION | 합집합 | 중복 제거 |
| UNION ALL | 합집합 | 중복 유지(가장 빠름) |
| INTERSECT | 교집합 | 중복 제거 |
| MINUS(Oracle) / EXCEPT(표준) | 차집합 | 중복 제거 |

---

## 사용 조건

\`\`\`
[ 집합 연산자 사용 4조건 ]
  ① 양쪽 SELECT 의 컬럼 개수 동일
  ② 같은 자리 컬럼은 데이터 타입 호환
  ③ 결과의 컬럼명은 첫 SELECT 기준
  ④ ORDER BY 는 맨 마지막에 한 번만
\`\`\`

| 조건 | 설명 |
|---|---|
| 컬럼 개수 | 두 SELECT의 컬럼 수가 같아야 함 |
| 데이터 타입 | 같은 자리의 컬럼은 호환되는 타입 |
| 컬럼명 | 결과의 컬럼명은 첫 SELECT 기준 |
| ORDER BY | 맨 마지막에 한 번만 |

---

## [SQL 실전]

\`\`\`sql
-- UNION (중복 제거)
SELECT NAME FROM EMP_A
UNION
SELECT NAME FROM EMP_B;

-- UNION ALL (중복 유지)
SELECT NAME FROM EMP_A
UNION ALL
SELECT NAME FROM EMP_B;

-- INTERSECT (교집합)
SELECT NAME FROM EMP_A
INTERSECT
SELECT NAME FROM EMP_B;

-- MINUS (차집합, A에만 있는 것)
SELECT NAME FROM EMP_A
MINUS
SELECT NAME FROM EMP_B;

-- ORDER BY는 마지막에
SELECT NAME FROM EMP_A
UNION
SELECT NAME FROM EMP_B
ORDER BY NAME;
\`\`\`

---

## [Before & After]

본사(EMP_A)와 지사(EMP_B) 두 명단을 합치거나 비교. 두 표에 "이"와 "박"이 양쪽 모두 있고, "김"은 A에만, "최"는 B에만 있다. 같은 데이터를 네 가지 집합 연산자(UNION/UNION ALL/INTERSECT/MINUS)에 넣었을 때 결과가 어떻게 갈라지는지.

### EMP_A

| NAME |
|---|
| 김 |
| 이 |
| 박 |

### EMP_B

| NAME |
|---|
| 이 |
| 박 |
| 최 |

### UNION

| NAME |
|---|
| 김 |
| 이 |
| 박 |
| 최 |

(중복 제거, 4행)

### UNION ALL

| NAME |
|---|
| 김 |
| 이 |
| 박 |
| 이 |
| 박 |
| 최 |

(중복 그대로, 6행)

### INTERSECT

| NAME |
|---|
| 이 |
| 박 |

### MINUS

| NAME |
|---|
| 김 |

같은 두 입력에서 4가지 결과가 갈라지는 이유는 "중복을 어떻게 다루느냐"와 "어느 쪽 기준으로 비교하느냐"가 연산마다 다르기 때문이다. UNION은 중복 제거를 위해 정렬을 한 번 더 거치므로 행 수가 줄고, UNION ALL은 그 단계를 건너뛰어 그대로 이어붙인다. INTERSECT는 양쪽 모두에 있는 값만, MINUS는 A에만 있는 값만 남긴다. "합칠 때 중복이 의미가 있는가", "두 집합의 공통 부분만 보고 싶은가", "한쪽에만 있는 것을 추리는가"를 먼저 묻고 연산자를 고를 것.

---

## UNION vs UNION ALL 성능 차이

| 비교 | UNION | UNION ALL |
|---|---|---|
| 중복 제거 | 함 | 안 함 |
| 정렬 작업 | 일어남(중복 제거 위해) | 안 일어남 |
| 성능 | 느림 | 빠름 |
| 권장 | 중복 가능성이 있을 때 | 중복 없거나 그대로 둬도 될 때 |

중복이 절대 없다고 확신하면 UNION ALL로 성능을 높인다. 두 결과를 "행 단위로 이어붙이는" 집합 연산자와 달리 [조인](./2-1-7_조인.md)은 컬럼을 옆으로 붙이고, [서브쿼리](./2-2-1_서브쿼리.md)는 메인쿼리에 값 하나를 공급. 역할이 다르다.

---

## 다중 컬럼 집합 연산

\`\`\`sql
SELECT NAME, DEPT_ID, SAL FROM EMP_A
UNION
SELECT NAME, DEPT_ID, SAL FROM EMP_B;
\`\`\`

모든 컬럼 값이 일치할 때만 중복으로 간주

---

## NULL과 집합 연산

집합 연산에서 NULL = NULL 로 취급되어 중복 처리(일반 비교 \`=\` 와 다름)

| EMP_A.COL | EMP_B.COL |
|---|---|
| NULL | NULL |

UNION 시 1행만 남음

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| 컬럼 개수가 다르면 | 에러 |
| UNION vs UNION ALL | UNION은 중복 제거, ALL은 그대로 |
| 결과 컬럼명 기준 | 첫 SELECT |
| ORDER BY 위치 | 마지막에 한 번 |
| 정렬 발생 여부 | UNION/INTERSECT/MINUS는 정렬 발생(중복 제거 위해) |
| MINUS는 표준인가 | Oracle 전용. 표준은 EXCEPT |
| NULL의 처리 | NULL = NULL로 간주(중복) |

단골 함정: 컬럼 데이터 타입이 다른데 합치려는 케이스는 에러

---

> **30초 시험 직전 정리**
> · UNION = **중복 제거** (정렬 발생, 느림) / UNION ALL = **중복 유지** (정렬 없음, 빠름)
> · INTERSECT = 교집합 (중복 제거) / MINUS(Oracle) = EXCEPT(표준) = A − B (중복 제거)
> · 사용 조건: **컬럼 개수 동일** + **타입 호환** + 컬럼명은 첫 SELECT
> · ORDER BY = **맨 마지막에 한 번만**
> · NULL 처리 = \`NULL = NULL\` 로 간주 (중복 제거 대상)
> · 중복 없다고 확신 → **UNION ALL** 로 성능 ↑
`,
  c223: `# 2-2-3. 그룹 함수(Group Functions)

## [핵심 요약]

> 그룹 함수는 다양한 단위의 소계와 합계를 한 쿼리로 한꺼번에 뽑아주는 확장 [GROUP BY](./2-1-5_GROUPBY_HAVING절.md) 도구다(ROLLUP, CUBE, GROUPING SETS).

ex) 매장 매출 보고서에서 "부서별 / 부서+상품별 / 전체 합계"를 따로따로 SELECT하지 않고 한 번에 뽑는 도구.

---

## [개념 도식화] 그룹 함수의 종류

\`\`\`mermaid
flowchart TD
    G[그룹 함수<br/>확장 GROUP BY] --> R[ROLLUP A,B<br/>계층적 소계<br/>A,B → A → ⌀<br/>N+1 단계]
    G --> C[CUBE A,B<br/>모든 조합<br/>A,B → A → B → ⌀<br/>2^N 단계]
    G --> S[GROUPING SETS<br/>지정한 조합만<br/>가장 유연]
\`\`\`

| 함수 | 의미 | 비유 |
|---|---|---|
| ROLLUP(A, B) | A별, A+B별, 전체 합계 | 단계적 소계 |
| CUBE(A, B) | 모든 조합(A별, B별, A+B별, 전체) | 큐브의 모든 면 |
| GROUPING SETS((A), (B)) | 특정 조합만 | 골라서 보기 |

---

## [SQL 실전]

### ROLLUP — 계층적 소계

\`\`\`sql
SELECT  DEPT_ID, JOB, SUM(SAL)
FROM    EMP
GROUP BY ROLLUP(DEPT_ID, JOB);
\`\`\`

### 결과 — 4단계(부서+직무, 부서, 전체)

| DEPT_ID | JOB | SUM(SAL) |
|---|---|---|
| D1 | MGR | 5000 |
| D1 | CLERK | 2000 |
| D1 | NULL | 7000 (D1 소계) |
| D2 | MGR | 4500 |
| D2 | NULL | 4500 (D2 소계) |
| NULL | NULL | 11500 (전체 합계) |

ROLLUP(A, B): A → A+B 로 갈수록 세분화, 위로 갈수록 소계

---

### CUBE — 모든 조합

\`\`\`sql
SELECT  DEPT_ID, JOB, SUM(SAL)
FROM    EMP
GROUP BY CUBE(DEPT_ID, JOB);
\`\`\`

### 결과 — ROLLUP 결과 + JOB별 합계까지

| DEPT_ID | JOB | SUM(SAL) |
|---|---|---|
| D1 | MGR | 5000 |
| D1 | CLERK | 2000 |
| D1 | NULL | 7000 |
| D2 | MGR | 4500 |
| D2 | NULL | 4500 |
| NULL | MGR | 9500 (MGR 합계, CUBE만 추가) |
| NULL | CLERK | 2000 (CLERK 합계) |
| NULL | NULL | 11500 |

N 개 컬럼 CUBE = 2^N 가지 조합

---

### GROUPING SETS — 골라 보기

\`\`\`sql
SELECT  DEPT_ID, JOB, SUM(SAL)
FROM    EMP
GROUP BY GROUPING SETS((DEPT_ID), (JOB), ());
\`\`\`

### 결과 — 부서별, 직무별, 전체

| DEPT_ID | JOB | SUM(SAL) |
|---|---|---|
| D1 | NULL | 7000 |
| D2 | NULL | 4500 |
| NULL | MGR | 9500 |
| NULL | CLERK | 2000 |
| NULL | NULL | 11500 |

---

## 정리표

| 함수 | 결과 |
|---|---|
| GROUP BY (A, B) | A, B 조합 |
| GROUP BY ROLLUP(A, B) | (A, B), (A), () |
| GROUP BY CUBE(A, B) | (A, B), (A), (B), () |
| GROUP BY GROUPING SETS((A, B), (A)) | 지정한 조합만 |

---

### GROUPING 함수 — 소계 행 구분

결과에 NULL이 보일 때, 원래 NULL 인지 소계로 인한 NULL 인지 구분하는 함수

| 결과 | 의미 |
|---|---|
| GROUPING(컬럼) = 0 | 일반 행(실제 그룹) |
| GROUPING(컬럼) = 1 | 소계 행 |

\`\`\`sql
SELECT  DEPT_ID, JOB, SUM(SAL),
        GROUPING(DEPT_ID) AS G_DEPT,
        GROUPING(JOB)     AS G_JOB
FROM    EMP
GROUP BY ROLLUP(DEPT_ID, JOB);
\`\`\`

| DEPT_ID | JOB | SUM | G_DEPT | G_JOB |
|---|---|---|---|---|
| D1 | MGR | 5000 | 0 | 0 |
| D1 | NULL | 7000 | 0 | 1 (소계) |
| NULL | NULL | 11500 | 1 | 1 (전체 합계) |

---

## [Before & After] ROLLUP vs CUBE 비교

EMP 3행(D1·D2 부서, A·B 직무)을 부서/직무로 집계. 같은 데이터를 일반 GROUP BY, ROLLUP, CUBE에 차례로 넣어 어디까지 소계가 추가되는지 비교. 핵심은 "부서 소계만 필요한가, 직무 소계까지 필요한가".

### EMP

| DEPT_ID | JOB | SAL |
|---|---|---|
| D1 | A | 1000 |
| D1 | B | 2000 |
| D2 | A | 3000 |

### \`GROUP BY (DEPT, JOB)\` — 기본

| D | J | SUM |
|---|---|---|
| D1 | A | 1000 |
| D1 | B | 2000 |
| D2 | A | 3000 |

### \`ROLLUP(DEPT, JOB)\`

| D | J | SUM |
|---|---|---|
| D1 | A | 1000 |
| D1 | B | 2000 |
| D1 | NULL | 3000 |
| D2 | A | 3000 |
| D2 | NULL | 3000 |
| NULL | NULL | 6000 |

### \`CUBE(DEPT, JOB)\` 추가 행

| NULL | A | 4000 |
| NULL | B | 2000 |

기본 GROUP BY는 (DEPT, JOB) 조합 1단계만, ROLLUP(DEPT, JOB)은 (DEPT, JOB) → (DEPT) → ()로 왼쪽부터 한 칸씩 떼어내며 소계를 쌓는다(N+1단계). CUBE(DEPT, JOB)는 거기에 더해 (JOB) 단독 소계까지 만들어 모든 조합 2^N단계를 채운다. 부서 단위 소계만 필요하면 ROLLUP으로 충분하지만, 직무별 가로 합계까지 한 번에 보고 싶다면 CUBE가 정답. 특정 조합만 골라 뽑고 싶다면 GROUPING SETS.

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| ROLLUP(A, B) 결과 단계 | (A, B), (A), () |
| CUBE(A, B) 결과 단계 | (A, B), (A), (B), () |
| 그룹함수에서 NULL 행의 의미 | 소계 행 또는 원래 NULL |
| GROUPING 함수 결과가 1이면 | 소계 행 |
| CUBE의 조합 수 | 2^N (N=컬럼수) |
| 소계 NULL과 데이터 NULL 구분 | GROUPING 함수 사용 |

단골 함정: ROLLUP(A, B, C) 의 단계 수는 N+1 = 4단계, CUBE 는 2^N = 8단계. 행을 합치는 그룹 함수와 달리, 행을 그대로 두고 옆에 통계를 붙이는 도구는 [윈도우 함수](./2-2-4_윈도우함수.md), 행을 컬럼으로 펼치는 도구는 [PIVOT](./2-2-7_PIVOT_UNPIVOT절.md).

---

> **30초 시험 직전 정리**
> · ROLLUP(A, B) = (A,B) → (A) → () — **N+1 단계** (계층적 소계)
> · CUBE(A, B) = (A,B) → (A) → (B) → () — **2^N 단계** (모든 조합)
> · GROUPING SETS = 원하는 조합만 골라 (유연)
> · GROUPING(컬럼) = **1 이면 소계 행** / 0 이면 일반 행
> · 결과의 NULL 이 데이터인지 소계인지 → **GROUPING 함수로 구분**
> · "ROLLUP 시작 위치만 다름" 함정 — \`ROLLUP(A,B)\` ≠ \`ROLLUP(B,A)\` (단계 다름)
`,
  c224: `# 2-2-4. 윈도우 함수(Window Function)

## [핵심 요약]

> 윈도우 함수는 행을 합치지 않으면서, 행마다 [그룹](./2-2-3_그룹함수.md)별 통계나 순위를 옆에 붙여주는 함수다(분석 함수라고도 한다).

ex) 학생 명단을 GROUP BY로 묶으면 학년 평균만 남아 개별 정보가 사라지지만, 윈도우 함수는 학생 행은 그대로 두고 옆에 학년 평균/등수를 추가한다.

---

## [개념 도식화] 일반 집계 vs 윈도우 함수

| 일반 집계 (GROUP BY) | 윈도우 함수 (OVER) |
|---|---|
| 행이 그룹별로 **합쳐짐** | 행은 **그대로 유지** + 옆에 분석값 |
| 결과 1행 | 결과 N행 (원본과 같음) |
| AVG = 4000 | NAME · SAL · RANK · AVG 모두 보임 |

윈도우 함수 결과 예시 (\`AVG(SAL) OVER ()\` + \`RANK()\`):

| NAME | SAL | RANK | AVG |
|---|---|---|---|
| 김 | 3000 | 3 | 4000 |
| 이 | 5000 | 1 | 4000 |
| 박 | 4000 | 2 | 4000 |

\`\`\`
[원본 행]  ──OVER 절로 윈도우 정의──→  [행 그대로 유지 + 옆에 분석값 추가]
\`\`\`

---

## 기본 문법

\`\`\`sql
함수명(컬럼) OVER (
    PARTITION BY 그룹기준        -- 어떻게 묶을지
    ORDER BY 정렬기준              -- 어떻게 정렬할지
    ROWS BETWEEN ... AND ...       -- 어디부터 어디까지(윈도우)
)
\`\`\`

| 구성 | 의미 |
|---|---|
| PARTITION BY | 그룹 기준(생략 시 전체) |
| [ORDER BY](./2-1-6_ORDERBY절.md) | 그룹 내 정렬 |
| ROWS/RANGE | 윈도우 프레임 범위 |

---

## 윈도우 함수의 분류

\`\`\`mermaid
flowchart TD
    W[윈도우 함수<br/>OVER 절] --> R[순위<br/>RANK · DENSE_RANK<br/>ROW_NUMBER · NTILE]
    W --> A[집계<br/>SUM · AVG · COUNT<br/>MAX · MIN]
    W --> O[행 순서<br/>LAG · LEAD<br/>FIRST_VALUE · LAST_VALUE]
    W --> P[비율<br/>CUME_DIST · PERCENT_RANK<br/>RATIO_TO_REPORT]
\`\`\`

## [SQL 실전]

### 순위 함수

| 함수 | 동점 처리 | 다음 순위 |
|---|---|---|
| RANK() | 같은 등수 | 건너뜀 (1, 1, 3) |
| DENSE_RANK() | 같은 등수 | 연속 (1, 1, 2) |
| ROW_NUMBER() | 무조건 다른 등수 | (1, 2, 3) |

\`\`\`sql
SELECT  NAME, SAL,
        RANK()       OVER (ORDER BY SAL DESC) AS R,
        DENSE_RANK() OVER (ORDER BY SAL DESC) AS DR,
        ROW_NUMBER() OVER (ORDER BY SAL DESC) AS RN
FROM    EMP;
\`\`\`

| NAME | SAL | R | DR | RN |
|---|---|---|---|---|
| 이 | 5000 | 1 | 1 | 1 |
| 박 | 5000 | 1 | 1 | 2 |
| 최 | 4500 | 3 | 2 | 3 |
| 김 | 3000 | 4 | 3 | 4 |

---

### 집계 윈도우 함수

| 함수 | 의미 |
|---|---|
| SUM/AVG/MAX/MIN/COUNT | 행 옆에 그룹 통계 |

\`\`\`sql
-- 부서별 평균 급여를 옆에 붙임
SELECT  NAME, DEPT_ID, SAL,
        AVG(SAL) OVER (PARTITION BY DEPT_ID) AS DEPT_AVG
FROM    EMP;
\`\`\`

| NAME | DEPT_ID | SAL | DEPT_AVG |
|---|---|---|---|
| 김 | D1 | 3000 | 4000 |
| 이 | D1 | 5000 | 4000 |
| 박 | D2 | 4000 | 4000 |
| 최 | D2 | 4000 | 4000 |

---

### 행 순서 함수

| 함수 | 의미 |
|---|---|
| LAG(col, n) | n행 전 값(앞 행) |
| LEAD(col, n) | n행 후 값(뒷 행) |
| FIRST_VALUE(col) | 윈도우의 첫 값 |
| LAST_VALUE(col) | 윈도우의 마지막 값 |

\`\`\`sql
SELECT  NAME, HIRE_DATE,
        LAG(NAME)  OVER (ORDER BY HIRE_DATE) AS PREV_EMP,
        LEAD(NAME) OVER (ORDER BY HIRE_DATE) AS NEXT_EMP
FROM    EMP;
\`\`\`

---

### 비율 함수

| 함수 | 의미 |
|---|---|
| CUME_DIST() | 누적 비율 |
| PERCENT_RANK() | 백분위 순위 |
| NTILE(N) | N등분 그룹 |
| RATIO_TO_REPORT(col) | 전체 합 대비 비율(Oracle) |

\`\`\`sql
-- 4등분으로 나눠서 그룹 번호 부여
SELECT NAME, SAL, NTILE(4) OVER (ORDER BY SAL DESC) AS QUARTILE FROM EMP;
\`\`\`

---

### 윈도우 프레임 — ROWS / RANGE

\`\`\`sql
ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW   -- 처음~현재
ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING           -- 앞1, 자신, 뒤1
RANGE BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING  -- 전체
\`\`\`

| 키워드 | 의미 |
|---|---|
| UNBOUNDED PRECEDING | 그룹의 처음부터 |
| n PRECEDING | 앞 n번째부터 |
| CURRENT ROW | 현재 행 |
| n FOLLOWING | 뒤 n번째까지 |
| UNBOUNDED FOLLOWING | 그룹의 끝까지 |

\`\`\`sql
-- 누적 합(running total)
SELECT  NAME, HIRE_DATE, SAL,
        SUM(SAL) OVER (ORDER BY HIRE_DATE
                       ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS RUN_TOT
FROM    EMP;
\`\`\`

---

## [Before & After]

D1·D2 두 부서 4명에 대해 "부서 안에서의 등수"와 "부서 평균"을 동시에 한 표에 보여줘야 하는 상황. GROUP BY로 묶으면 4행이 2행으로 줄어 개별 이름이 사라지지만, 윈도우 함수는 4행을 그대로 유지하면서 옆에 부서별 RANK와 부서 AVG를 붙인다. D2의 박·최가 4500으로 동률일 때 RANK가 어떻게 매겨지는지도 함께.

### EMP

| NAME | DEPT_ID | SAL |
|---|---|---|
| 김 | D1 | 3000 |
| 이 | D1 | 5000 |
| 박 | D2 | 4500 |
| 최 | D2 | 4500 |

### 쿼리

\`\`\`sql
SELECT  NAME, DEPT_ID, SAL,
        RANK() OVER (PARTITION BY DEPT_ID ORDER BY SAL DESC) AS DEPT_RANK,
        AVG(SAL) OVER (PARTITION BY DEPT_ID) AS DEPT_AVG
FROM    EMP;
\`\`\`

| NAME | DEPT_ID | SAL | DEPT_RANK | DEPT_AVG |
|---|---|---|---|---|
| 이 | D1 | 5000 | 1 | 4000 |
| 김 | D1 | 3000 | 2 | 4000 |
| 박 | D2 | 4500 | 1 | 4500 |
| 최 | D2 | 4500 | 1 | 4500 |

PARTITION BY DEPT_ID가 D1·D2를 각각 독립된 윈도우로 나누기 때문에 RANK와 AVG가 부서별로 따로 계산된다. D2의 박·최가 둘 다 RANK 1을 받는 이유는 RANK()가 동률을 같은 등수로 처리하기 때문이고(다음 등수는 3으로 건너뜀), AVG도 부서 안 두 명만으로 계산해 4500이 된다. 행을 합치지 않고 옆에 통계를 붙이는 윈도우 함수의 본질 — 같은 부서 두 명의 SAL이 그대로 보이면서 평균·등수가 함께 따라붙는다.

---

## 윈도우 함수 vs GROUP BY

| 항목 | GROUP BY | 윈도우 함수 |
|---|---|---|
| 결과 행수 | 줄어듦(그룹별 1행) | 유지(원본 그대로) |
| 개별 데이터 | 사라짐 | 남음 |
| 사용 목적 | 집계 결과만 | 개별 + 집계 |

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| RANK vs DENSE_RANK vs ROW_NUMBER | RANK 건너뜀 / DENSE_RANK 연속 / ROW_NUMBER 모두 다름 |
| \`OVER ()\` 빈 괄호 | 전체를 한 윈도우로 |
| PARTITION BY 생략 | 전체 행을 하나의 그룹으로 |
| 윈도우 함수에 WHERE 절 사용 | WHERE에 직접 못 씀 → 인라인뷰 사용 |
| LAG/LEAD 기본 오프셋 | 1 |
| 윈도우 함수 결과 행 수 | 원본과 동일 |

단골 함정: \`RANK() OVER(...)\` 결과를 WHERE 에 직접 사용 불가. 서브쿼리/인라인뷰로 감싸야 함(부서별 [Top N 쿼리](./2-2-5_TopN쿼리.md)도 같은 패턴).

---

> **30초 시험 직전 정리**
> · 결과 행 수 = **원본과 동일** (GROUP BY 처럼 합치지 않음)
> · 순위: **RANK** (동점 후 건너뜀: 1,1,3) / **DENSE_RANK** (연속: 1,1,2) / **ROW_NUMBER** (모두 다름)
> · 문법: \`함수() OVER (PARTITION BY ... ORDER BY ... ROWS BETWEEN ...)\`
> · PARTITION BY 생략 = **전체를 한 그룹**
> · LAG = 앞 / LEAD = 뒤 / FIRST_VALUE / LAST_VALUE / NTILE(N)
> · 윈도우 결과를 **WHERE 에서 직접 사용 불가** → 서브쿼리·인라인뷰로 감쌈
> · 누적합 = \`SUM(...) OVER (ORDER BY ... ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)\`
`,
  c225: `# 2-2-5. Top N 쿼리

## [핵심 요약]

> Top N 쿼리는 상위 N개 행만 뽑는 쿼리다(예: 매출 상위 10개 상품, 최신 5건의 주문).

---

## [개념 도식화] Top N의 4가지 방법

\`\`\`mermaid
flowchart TD
    T[Top N 구현] --> R[Oracle ROWNUM<br/>의사 컬럼<br/>정렬 전 매겨짐<br/>인라인뷰 필수]
    T --> F[표준 12c+<br/>FETCH FIRST<br/>OFFSET / WITH TIES / PERCENT]
    T --> P[MS-SQL TOP<br/>SELECT TOP n<br/>TOP n WITH TIES]
    T --> W[범용 윈도우<br/>ROW_NUMBER OVER<br/>PARTITION BY 로 그룹별 가능]
\`\`\`

---

## [SQL 실전]

### Oracle ROWNUM

ROWNUM = 행이 만들어진 순서대로 1, 2, 3... 이 매겨지는 의사 컬럼

\`\`\`sql
SELECT  *
FROM    EMP
WHERE   ROWNUM <= 5;
\`\`\`

#### ROWNUM의 함정

ROWNUM 은 [ORDER BY](./2-1-6_ORDERBY절.md) 적용 전에 매겨지므로 정렬을 먼저 한 인라인 뷰를 감싸야 함

\`\`\`sql
-- 잘못된 예: 정렬 전에 ROWNUM이 매겨짐
SELECT *
FROM   EMP
WHERE  ROWNUM <= 5
ORDER BY SAL DESC;     -- 5개 추출 후 정렬되어 의도와 다름

-- 올바른 예: 인라인 뷰로 정렬 먼저
SELECT *
FROM   (SELECT * FROM EMP ORDER BY SAL DESC)
WHERE  ROWNUM <= 5;
\`\`\`

| 조건 | 결과 |
|---|---|
| \`ROWNUM = 1\` | 가능 |
| \`ROWNUM <= 5\` | 가능 |
| \`ROWNUM = 5\` | 결과 없음 |
| \`ROWNUM > 5\` | 결과 없음 |

ROWNUM 은 1부터 시작하므로 "5번째만"을 ROWNUM = 5 로 표현 불가

---

### 표준 FETCH FIRST (Oracle 12c+)

\`\`\`sql
-- 상위 5개
SELECT *
FROM   EMP
ORDER BY SAL DESC
FETCH FIRST 5 ROWS ONLY;

-- 상위 10%(백분율)
SELECT *
FROM   EMP
ORDER BY SAL DESC
FETCH FIRST 10 PERCENT ROWS ONLY;

-- 동률 포함
SELECT *
FROM   EMP
ORDER BY SAL DESC
FETCH FIRST 5 ROWS WITH TIES;     -- 5등 동률은 모두 포함
\`\`\`

| 옵션 | 의미 |
|---|---|
| FETCH FIRST n ROWS ONLY | 정확히 n개 |
| FETCH FIRST n ROWS WITH TIES | n등 동률은 모두 포함 |
| OFFSET n ROWS | n개 건너뛰고 |

---

### MS-SQL TOP

\`\`\`sql
SELECT TOP 5 *
FROM   EMP
ORDER BY SAL DESC;

-- 동률 포함
SELECT TOP 5 WITH TIES *
FROM   EMP
ORDER BY SAL DESC;
\`\`\`

---

### [윈도우 함수](./2-2-4_윈도우함수.md) — 가장 안전·범용

\`\`\`sql
-- 부서별 급여 상위 3명
SELECT *
FROM   (SELECT NAME, DEPT_ID, SAL,
               ROW_NUMBER() OVER (PARTITION BY DEPT_ID ORDER BY SAL DESC) AS RN
        FROM   EMP)
WHERE  RN <= 3;
\`\`\`

| 함수 | 동률 처리 |
|---|---|
| ROW_NUMBER() | 무조건 다른 번호 → 정확히 N개 |
| RANK() | 동률 같은 등수, 건너뜀 |
| DENSE_RANK() | 동률 같은 등수, 연속 |

---

### 페이징(Pagination)

"1페이지: 1-10번째, 2페이지: 11-20번째..."

### Oracle

\`\`\`sql
-- 11~20번째
SELECT *
FROM   (SELECT ROWNUM RN, X.*
        FROM   (SELECT * FROM EMP ORDER BY SAL DESC) X
        WHERE  ROWNUM <= 20)
WHERE  RN >= 11;
\`\`\`

### 표준 (Oracle 12c+)

\`\`\`sql
SELECT *
FROM   EMP
ORDER BY SAL DESC
OFFSET 10 ROWS FETCH NEXT 10 ROWS ONLY;
\`\`\`

---

## [Before & After]

EMP 5명에서 급여 상위 3명을 뽑는 상황. 박·최가 4500으로 동률이라는 점이 핵심 차이를 만든다. 같은 "Top 3" 요구라도 ROWNUM 기반(정확히 3명)과 WITH TIES(동률 모두 포함)가 어떻게 갈라지는지.

### EMP

| NAME | SAL |
|---|---|
| 김 | 3000 |
| 이 | 5000 |
| 박 | 4500 |
| 최 | 4500 |
| 홍 | 2000 |

### 급여 상위 3명

\`\`\`sql
SELECT *
FROM   (SELECT * FROM EMP ORDER BY SAL DESC)
WHERE  ROWNUM <= 3;
\`\`\`

| NAME | SAL |
|---|---|
| 이 | 5000 |
| 박 | 4500 |
| 최 | 4500 |

### \`WITH TIES\` 사용 시

| NAME | SAL |
|---|---|
| 이 | 5000 |
| 박 | 4500 |
| 최 | 4500 |

4500이 더 있으면 4명 이상 가능

이 예에서는 박·최 두 명이 4500이라 마침 정확히 3명이 떨어졌지만, 만약 4500이 한 명 더 있었다면 ROWNUM 방식은 그중 임의의 3명만, WITH TIES는 동률 전원을 포함해 4명 이상을 반환한다. "정확한 N개"가 필요하면 ROW_NUMBER()/ROWNUM, "공평하게 동률 포함"이 원칙이면 WITH TIES 나 RANK(). 결과 행 수가 입력 데이터에 따라 달라질 수 있다는 점이 Top N 쿼리의 단골 함정.

---

## ROWNUM vs ROW_NUMBER()

| 항목 | ROWNUM | ROW_NUMBER() |
|---|---|---|
| 정렬 적용 시점 | WHERE보다 먼저(정렬 전) | OVER에 ORDER BY 명시 |
| 그룹별 번호 | 불가 | 가능(PARTITION BY) |
| \`=N\` 사용 | 안 됨(1만 가능) | 가능 |

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| \`WHERE ROWNUM = 5\` | 결과 없음(1만 매칭) |
| ROWNUM과 ORDER BY 조합 | 인라인 뷰로 정렬 먼저, ROWNUM은 나중에 |
| 동률 포함하려면 | WITH TIES 또는 RANK() |
| 부서별 Top N | 윈도우 함수(ROW_NUMBER + PARTITION BY) |
| 페이징 표준 문법 | OFFSET ... FETCH NEXT ... |

단골 함정: ROWNUM 은 \`=1\` 만 의미 있음. \`>n\` 은 항상 거짓.

---

> **30초 시험 직전 정리**
> · ROWNUM 함정 = **\`=1\`만 가능**, **\`>n\` 항상 거짓**, **정렬 전에 매겨짐** → 인라인뷰로 정렬 먼저
> · 표준: \`FETCH FIRST n ROWS ONLY\` (Oracle 12c+) / \`WITH TIES\` 면 동률 포함
> · MS-SQL: \`SELECT TOP n\` / \`TOP n WITH TIES\`
> · 부서별 Top N = **ROW_NUMBER() OVER (PARTITION BY ... ORDER BY ...)** + 인라인뷰
> · 페이징 표준 = \`OFFSET n ROWS FETCH NEXT m ROWS ONLY\`
`,
  c226: `# 2-2-6. 계층형 질의와 셀프 조인

## [핵심 요약]

> 계층형 질의는 트리 구조 데이터(예: 조직도)를 위에서 아래로 펼치는 쿼리다. 셀프 조인은 같은 테이블을 자기 자신과 조인하는 것이다.

ex) 가족 족보를 "사람ID, 부모ID" 한 테이블에 저장한 뒤 CONNECT BY로 할아버지 → 아버지 → 나 순으로 펼쳐 보는 쿼리.

---

## [개념 도식화] 계층형 데이터의 모양

\`\`\`
       사장(E1)
       /    \\
   매니저A(E2) 매니저B(E3)
     /
  사원(E4)
\`\`\`

| EMP_ID | NAME | MGR_ID |
|---|---|---|
| E1 | 사장 | NULL |
| E2 | 매니저A | E1 |
| E3 | 매니저B | E1 |
| E4 | 사원 | E2 |

---

## CONNECT BY (Oracle 계층형 질의)

\`\`\`sql
SELECT  LEVEL,
        EMP_ID, NAME, MGR_ID
FROM    EMP
START WITH MGR_ID IS NULL          -- 시작점(루트)
CONNECT BY PRIOR EMP_ID = MGR_ID;  -- 부모-자식 관계
\`\`\`

| 키워드 | 의미 |
|---|---|
| START WITH | 루트 행 조건 |
| CONNECT BY PRIOR | 부모-자식 연결 |
| LEVEL | 깊이(1=루트) |
| CONNECT_BY_ROOT | 루트의 컬럼값 |
| CONNECT_BY_ISLEAF | 리프(잎)이면 1 |
| SYS_CONNECT_BY_PATH(col, '/') | 경로 표시 |

### PRIOR 위치

| 표현 | 의미 |
|---|---|
| \`PRIOR EMP_ID = MGR_ID\` | 순방향: 부모→자식 |
| \`PRIOR MGR_ID = EMP_ID\` | 역방향: 자식→부모 |

---

## [SQL 실전] 계층형 질의

\`\`\`sql
SELECT  LEVEL,
        LPAD(' ', (LEVEL-1)*4) || NAME AS TREE,
        SYS_CONNECT_BY_PATH(NAME, '/') AS PATH
FROM    EMP
START WITH MGR_ID IS NULL
CONNECT BY PRIOR EMP_ID = MGR_ID;
\`\`\`

### 결과

| LEVEL | TREE | PATH |
|---|---|---|
| 1 | 사장 | /사장 |
| 2 | &nbsp;&nbsp;&nbsp;&nbsp;매니저A | /사장/매니저A |
| 3 | &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;사원 | /사장/매니저A/사원 |
| 2 | &nbsp;&nbsp;&nbsp;&nbsp;매니저B | /사장/매니저B |

---

## ORDER SIBLINGS BY — 형제 정렬

\`\`\`sql
SELECT  LEVEL, NAME
FROM    EMP
START WITH MGR_ID IS NULL
CONNECT BY PRIOR EMP_ID = MGR_ID
ORDER SIBLINGS BY NAME;     -- 형제(같은 LEVEL)끼리만 정렬
\`\`\`

일반 ORDER BY 는 계층 구조를 망가뜨리므로 ORDER SIBLINGS BY 사용

---

## SELF JOIN

같은 테이블을 두 번 사용해 [조인](./2-1-7_조인.md). 별칭으로 구분.

\`\`\`sql
SELECT  E.NAME    AS 사원,
        M.NAME    AS 매니저
FROM    EMP E,  EMP M
WHERE   E.MGR_ID = M.EMP_ID;
\`\`\`

표준 방식

\`\`\`sql
SELECT  E.NAME    AS 사원,
        M.NAME    AS 매니저
FROM    EMP E LEFT JOIN EMP M ON E.MGR_ID = M.EMP_ID;
\`\`\`

| 사원 | 매니저 |
|---|---|
| 사장 | NULL |
| 매니저A | 사장 |
| 매니저B | 사장 |
| 사원 | 매니저A |

---

## 계층형 질의 vs 셀프 조인

| 비교 | 계층형 질의 | 셀프 조인 |
|---|---|---|
| 구조 | 여러 단계 트리 | 보통 1단계(직속만) |
| 사용 함수 | LEVEL, PATH 등 | 일반 SQL |
| 표준 여부 | Oracle 전용(CONNECT BY) | 표준 |
| 깊이 제한 | 무제한 | 1단계 |

---

## 표준 재귀 쿼리 (참고)

\`\`\`sql
-- ANSI 표준: 재귀 CTE(Common Table Expression)
WITH RECURSIVE EMP_TREE(EMP_ID, NAME, MGR_ID, LV) AS (
    SELECT EMP_ID, NAME, MGR_ID, 1
    FROM   EMP
    WHERE  MGR_ID IS NULL
    UNION ALL
    SELECT E.EMP_ID, E.NAME, E.MGR_ID, T.LV + 1
    FROM   EMP E JOIN EMP_TREE T ON E.MGR_ID = T.EMP_ID
)
SELECT * FROM EMP_TREE;
\`\`\`

Oracle 12c 이상 및 다른 DBMS 도 지원

---

## [Before & After]

평면 테이블 EMP는 4행이 단순히 나열돼 있을 뿐, 사장-매니저-사원의 위계가 보이지 않는다. CONNECT BY로 MGR_ID 컬럼을 따라 부모-자식 관계를 펼치면 같은 4행이 트리 순서로 재배열되고 LEVEL 이 1·2·3으로 매겨진다.

### EMP

| EMP_ID | NAME | MGR_ID |
|---|---|---|
| E1 | 사장 | NULL |
| E2 | 매니저A | E1 |
| E3 | 매니저B | E1 |
| E4 | 사원 | E2 |

### 쿼리

\`\`\`sql
SELECT LEVEL, NAME, EMP_ID, MGR_ID
FROM   EMP
START WITH MGR_ID IS NULL
CONNECT BY PRIOR EMP_ID = MGR_ID;
\`\`\`

| LEVEL | NAME | EMP_ID | MGR_ID |
|---|---|---|---|
| 1 | 사장 | E1 | NULL |
| 2 | 매니저A | E2 | E1 |
| 3 | 사원 | E4 | E2 |
| 2 | 매니저B | E3 | E1 |

START WITH MGR_ID IS NULL 이 사장(E1)을 루트로 잡고, CONNECT BY PRIOR EMP_ID = MGR_ID 가 "현재 행의 EMP_ID = 다음 행의 MGR_ID"라는 부모-자식 규칙을 정의해 깊이 우선으로 트리를 펼친다. 그래서 매니저A 다음에 그 자식인 사원이 먼저 따라붙고, 그다음에야 매니저B가 출력된다(매니저B 자식이 없으면 거기서 가지 끝). 셀프 조인이라면 "사원-매니저" 1단계만 가능하지만, 계층형 질의는 깊이가 깊어져도 LEVEL 과 SYS_CONNECT_BY_PATH 로 재귀 추적이 가능 — 결정적 차이.

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| START WITH의 의미 | 루트(최상위) 조건 |
| \`CONNECT BY PRIOR EMP_ID = MGR_ID\` | 순방향(부모→자식) |
| LEVEL의 시작 값 | 1(루트) |
| 형제 정렬 | ORDER SIBLINGS BY |
| SYS_CONNECT_BY_PATH | 경로 문자열 |
| 셀프 조인 시 별칭 | 필수 |
| 계층형 질의는 표준인가 | Oracle 전용. 표준은 재귀 CTE |

단골 함정: PRIOR 의 위치를 바꾸면 순방향과 역방향이 뒤바뀜

---

> **30초 시험 직전 정리**
> · \`START WITH\` = **루트 조건** (보통 MGR_ID IS NULL)
> · \`CONNECT BY PRIOR EMP_ID = MGR_ID\` = **순방향** (부모 → 자식, 위에서 아래로)
> · \`CONNECT BY PRIOR MGR_ID = EMP_ID\` = **역방향** (자식 → 부모)
> · LEVEL = **1부터** (루트가 1) / SYS_CONNECT_BY_PATH = 경로 표시
> · 형제 정렬 = **ORDER SIBLINGS BY** (일반 ORDER BY 는 트리 깨짐)
> · 표준 재귀 쿼리 = **WITH RECURSIVE ... UNION ALL** (CTE)
> · SELF JOIN = 같은 테이블 두 번 사용, **별칭 필수** (1단계 관계만)
`,
  c227: `# 2-2-7. PIVOT 절과 UNPIVOT 절

## [핵심 요약]

> PIVOT은 세로 데이터를 가로 데이터로, UNPIVOT은 가로 데이터를 세로 데이터로 바꾼다. 데이터 모양 변환 도구다.

ex) "(김철수, 수학, 3) / (김철수, 영어, 2)" 같은 세로 카드 형태를 "김철수 | 수학3 | 영어2"의 가로 엑셀 표로 펼치는 작업이 PIVOT.

---

## [개념 도식화] PIVOT vs UNPIVOT

**세로형 (Long) — 원본**

| 회원 | 과목 | 수 |
|---|---|---|
| 김 | 수학 | 3 |
| 김 | 영어 | 2 |
| 김 | 과학 | 1 |

**↓ PIVOT (세로 → 가로) / ↑ UNPIVOT (가로 → 세로)**

**가로형 (Wide) — 변환**

| 회원 | 수학 | 영어 | 과학 |
|---|---|---|---|
| 김 | 3 | 2 | 1 |

---

## PIVOT 절 — 세로 → 가로

### 문법

\`\`\`sql
SELECT  *
FROM    원본테이블
        PIVOT (
            집계함수(값컬럼)
            FOR  변환대상컬럼 IN (값1 AS 별칭1, 값2 AS 별칭2, ...)
        );
\`\`\`

### 예시

\`\`\`sql
SELECT *
FROM   SCORE
       PIVOT (
           SUM(POINT)
           FOR SUBJECT IN ('수학' AS 수학, '영어' AS 영어, '과학' AS 과학)
       );
\`\`\`

### Before

| MEMBER | SUBJECT | POINT |
|---|---|---|
| 김 | 수학 | 90 |
| 김 | 영어 | 80 |
| 김 | 과학 | 70 |
| 이 | 수학 | 100 |

### After (PIVOT 결과)

| MEMBER | 수학 | 영어 | 과학 |
|---|---|---|---|
| 김 | 90 | 80 | 70 |
| 이 | 100 | NULL | NULL |

---

## UNPIVOT 절 — 가로 → 세로

### 문법

\`\`\`sql
SELECT  *
FROM    가로테이블
        UNPIVOT (
            값컬럼
            FOR 새컬럼명 IN (가로컬럼1 AS '값1', 가로컬럼2 AS '값2', ...)
        );
\`\`\`

### 예시

\`\`\`sql
SELECT *
FROM   SCORE_WIDE
       UNPIVOT (
           POINT
           FOR SUBJECT IN (수학 AS '수학', 영어 AS '영어', 과학 AS '과학')
       );
\`\`\`

### Before (가로형)

| MEMBER | 수학 | 영어 | 과학 |
|---|---|---|---|
| 김 | 90 | 80 | 70 |

### After (UNPIVOT 결과)

| MEMBER | SUBJECT | POINT |
|---|---|---|
| 김 | 수학 | 90 |
| 김 | 영어 | 80 |
| 김 | 과학 | 70 |

---

## CASE WHEN으로 PIVOT 흉내내기 (전통 방식)

PIVOT 절 없이도 같은 결과 가능

\`\`\`sql
SELECT  MEMBER,
        SUM(CASE WHEN SUBJECT = '수학' THEN POINT END) AS 수학,
        SUM(CASE WHEN SUBJECT = '영어' THEN POINT END) AS 영어,
        SUM(CASE WHEN SUBJECT = '과학' THEN POINT END) AS 과학
FROM    SCORE
GROUP BY MEMBER;
\`\`\`

시험 단골: PIVOT 절을 몰라도 CASE + GROUP BY 로 같은 효과 가능(소계까지 함께 뽑고 싶으면 [그룹 함수](./2-2-3_그룹함수.md)의 ROLLUP/CUBE 결합).

---

## NULL 처리

| 상황 | 결과 |
|---|---|
| PIVOT 후 매칭 없는 칸 | NULL |
| UNPIVOT 시 NULL 값 | 기본 제외 |
| INCLUDE NULLS 옵션 | NULL 값도 포함 |

\`\`\`sql
-- NULL도 포함시켜 UNPIVOT
... UNPIVOT INCLUDE NULLS (POINT FOR SUBJECT IN (...));
\`\`\`

---

## [SQL 실전] 추가 예시

\`\`\`sql
-- 부서별, 직무별 인원수를 가로로
SELECT *
FROM   EMP
       PIVOT (
           COUNT(*)
           FOR JOB IN ('MGR' AS 매니저, 'CLERK' AS 사무직, 'SALES' AS 영업)
       );
\`\`\`

| DEPT_ID | 매니저 | 사무직 | 영업 |
|---|---|---|---|
| D1 | 1 | 2 | 0 |
| D2 | 1 | 0 | 3 |

---

## [Before & After] CASE 방식과 PIVOT 절 비교

분기·카테고리·매출이 세로로 쌓인 SALES 표를 분기별 가로 보고서로 변환. Q2에는 카테고리 B 매출이 없어 결과에서 NULL 처리. 같은 변환을 Oracle PIVOT 절과 전통적인 CASE WHEN + GROUP BY 방식으로 동시에 표현해 비교.

### 원본

| 분기 | 카테고리 | 매출 |
|---|---|---|
| Q1 | A | 100 |
| Q1 | B | 200 |
| Q2 | A | 150 |

### 결과 (PIVOT)

| 분기 | A | B |
|---|---|---|
| Q1 | 100 | 200 |
| Q2 | 150 | NULL |

\`\`\`sql
-- PIVOT
SELECT * FROM SALES
PIVOT (SUM(매출) FOR 카테고리 IN ('A' AS A, 'B' AS B));

-- CASE WHEN 방식
SELECT 분기,
       SUM(CASE WHEN 카테고리='A' THEN 매출 END) AS A,
       SUM(CASE WHEN 카테고리='B' THEN 매출 END) AS B
FROM   SALES
GROUP BY 분기;
\`\`\`

두 방식의 결과가 같은 이유는 PIVOT 절이 사실상 "카테고리별 SUM(CASE WHEN ...)"을 분기 GROUP BY 로 묶는 동작을 문법적으로 단순화한 것이기 때문이다. Q2의 B 칸이 NULL 이 되는 것도 같은 메커니즘으로, 매칭되는 원본 행이 없어 SUM 의 입력이 비어 있기 때문. PIVOT 은 IN 절에 변환할 값을 명시적으로 나열해야 하므로 "어떤 컬럼이 만들어질지"를 미리 알아야 하고, CASE 방식은 분기 컬럼을 동적으로 조립할 수 있어 더 유연. 표현력의 차이.

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| PIVOT의 결과 모양 | 행 → 컬럼으로 변환 |
| UNPIVOT의 결과 모양 | 컬럼 → 행으로 변환 |
| PIVOT의 IN 절 | 변환할 값들 나열 |
| 매칭 안 되는 칸 | NULL |
| UNPIVOT 기본 NULL 처리 | 제외(INCLUDE NULLS로 포함 가능) |
| CASE WHEN으로 PIVOT 가능? | 가능(PIVOT 절 없이) |

단골 함정: PIVOT 은 SUM/COUNT 같은 집계함수 필수. 단순 변환만 하려고 해도 집계함수가 들어가야 함. UNPIVOT 결과를 다른 SELECT 결과와 합치려면 [집합 연산자](./2-2-2_집합연산자.md)(UNION ALL 등)를 함께 사용.

---

> **30초 시험 직전 정리**
> · PIVOT = **세로 → 가로** (행을 컬럼으로)
> · UNPIVOT = **가로 → 세로** (컬럼을 행으로)
> · PIVOT 은 **집계 함수 필수** (SUM/COUNT/MAX 등) — 단순 변환에도 형식상 필요
> · 매칭 안 되는 칸 → **NULL**
> · UNPIVOT 기본 = **NULL 제외**, \`INCLUDE NULLS\` 로 포함
> · PIVOT 절 없이 = **CASE WHEN + GROUP BY** 로 흉내 가능 (시험 단골)
`,
  c228: `# 2-2-8. 정규 표현식(Regular Expression)

## [핵심 요약]

> 정규 표현식은 문자 패턴을 표현하는 짧은 식이다. LIKE보다 훨씬 강력하게 문자열을 검색·추출·교체할 수 있다.

ex) LIKE는 "'김%'로 시작" 정도지만, 정규식은 "010-숫자4자리-숫자4자리" / "한글 3글자 이름" / "이메일 형식" 같은 정밀 패턴을 표현한다.

---

## [개념 도식화] LIKE vs REGEXP 비교

\`\`\`
LIKE       │ % (0개 이상), _ (1개)               │  단순·기본 검색
REGEXP     │ . * + ? [ ] ^ $ | { } \\d \\w \\s …    │  정밀·고급 검색
\`\`\`

| 비교 | LIKE | REGEXP |
|---|---|---|
| 표현력 | 약함 | 강함 |
| 와일드카드 | \`%\`, \`_\` | \`.\`, \`*\`, \`+\`, \`[]\`, \`^\`, \`$\` 등 다수 |
| 성능 | 빠름 | 약간 느림 |

---

## Oracle 정규식 [함수](./2-1-3_함수.md)

\`\`\`mermaid
flowchart TD
    R[REGEXP 함수 5종] --> L[REGEXP_LIKE<br/>조건절 — TRUE/FALSE]
    R --> P[REGEXP_REPLACE<br/>패턴 부분 교체]
    R --> S[REGEXP_SUBSTR<br/>패턴 일치 추출]
    R --> I[REGEXP_INSTR<br/>패턴 위치]
    R --> C[REGEXP_COUNT<br/>패턴 등장 횟수]
\`\`\`

| 함수 | 의미 |
|---|---|
| REGEXP_LIKE(s, pattern) | LIKE의 정규식 버전(조건절용) |
| REGEXP_REPLACE(s, pattern, replace) | 패턴 매칭 부분을 교체 |
| REGEXP_SUBSTR(s, pattern) | 패턴 일치 부분 추출 |
| REGEXP_INSTR(s, pattern) | 패턴이 처음 나타나는 위치 |
| REGEXP_COUNT(s, pattern) | 패턴 등장 횟수 |

---

## 자주 쓰는 메타문자

| 기호 | 의미 | 예 |
|---|---|---|
| \`.\` | 임의의 한 글자 | \`'a.c'\` → 'abc', 'aXc' |
| \`*\` | 0개 이상 반복 | \`'ab*'\` → 'a', 'ab', 'abb' |
| \`+\` | 1개 이상 반복 | \`'ab+'\` → 'ab', 'abb' (a 안 됨) |
| \`?\` | 0 또는 1 | \`'colou?r'\` → 'color', 'colour' |
| \`[abc]\` | a, b, c 중 하나 | \`'[Aa]pple'\` |
| \`[^abc]\` | a, b, c가 아닌 것 | |
| \`[0-9]\` | 숫자 한 글자 | |
| \`[a-z]\` | 영소문자 | |
| \`^\` | 문자열 시작 | \`^010\` (010으로 시작) |
| \`$\` | 문자열 끝 | \`\\.com$\` |
| <code>&#124;</code> | 또는 | \`(cat\\|dog)\` |
| \`{n}\` | 정확히 n번 | \`\\d{4}\` |
| \`{n,m}\` | n~m번 | \`\\d{2,4}\` |
| \`\\d\` | 숫자 [0-9] | |
| \`\\w\` | 단어문자 [A-Za-z0-9_] | |
| \`\\s\` | 공백 | |

---

## [SQL 실전]

### REGEXP_LIKE ([WHERE](./2-1-4_WHERE절.md) 조건절)

\`\`\`sql
-- 휴대폰 번호 형식 (010으로 시작, 3-4-4 자리)
SELECT *
FROM   MEMBER
WHERE  REGEXP_LIKE(PHONE, '^010-\\d{3,4}-\\d{4}$');

-- 이메일 형식
SELECT *
FROM   MEMBER
WHERE  REGEXP_LIKE(EMAIL, '^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,}$');

-- 한글 3글자 이름
SELECT *
FROM   MEMBER
WHERE  REGEXP_LIKE(NAME, '^[가-힣]{3}$');
\`\`\`

### REGEXP_REPLACE (교체)

\`\`\`sql
-- 모든 공백 제거
SELECT REGEXP_REPLACE('Hello   World', '\\s+', '') FROM DUAL;
-- 결과: 'HelloWorld'

-- 전화번호 가운데를 마스킹
SELECT REGEXP_REPLACE('010-1234-5678', '(\\d{3})-(\\d{4})-(\\d{4})', '\\1-****-\\3') FROM DUAL;
-- 결과: '010-****-5678'
\`\`\`

### REGEXP_SUBSTR (추출)

\`\`\`sql
-- 첫 번째 숫자 묶음 추출
SELECT REGEXP_SUBSTR('주문번호 12345 입니다', '\\d+') FROM DUAL;
-- 결과: '12345'
\`\`\`

### REGEXP_INSTR / REGEXP_COUNT

\`\`\`sql
-- 처음 숫자가 나타나는 위치
SELECT REGEXP_INSTR('abc123def', '\\d') FROM DUAL;  -- 4

-- 단어 개수
SELECT REGEXP_COUNT('one two three', '\\w+') FROM DUAL;  -- 3
\`\`\`

---

## [Before & After]

MEMBER 3건에서 "한글 이름 + 010 휴대폰 번호" 회원만 골라내야 하는 상황. 2번은 휴대폰이 02 지역번호라 탈락, 3번은 이름이 영문이라 탈락. LIKE 두 개로는 표현하기 까다로운 두 조건을 REGEXP_LIKE 로 정밀하게 거른다.

### MEMBER

| ID | NAME | PHONE |
|---|---|---|
| 1 | 김철수 | 010-1234-5678 |
| 2 | 이영희 | 02-1234-5678 |
| 3 | Jane | 010-9999-1234 |

### 한글 이름 + 010 휴대폰만

\`\`\`sql
SELECT *
FROM   MEMBER
WHERE  REGEXP_LIKE(NAME, '^[가-힣]+$')
  AND  REGEXP_LIKE(PHONE, '^010-');
\`\`\`

| ID | NAME | PHONE |
|---|---|---|
| 1 | 김철수 | 010-1234-5678 |

\`^[가-힣]+$\` 는 문자열 처음(\`^\`)부터 끝(\`$\`)까지가 모두 한글 한 글자(\`[가-힣]\`)의 1회 이상 반복으로만 채워질 때만 매칭하므로, "Jane"은 영문이라 즉시 탈락하고 김철수·이영희는 통과한다. 다음 조건 \`^010-\` 은 PHONE 이 정확히 "010-"으로 시작할 때만 통과시키므로 02 지역번호인 이영희가 떨어진다. 두 정규식 조건을 AND 로 조합해 "한글 이름 + 010 휴대폰" 교집합만 남는다. LIKE 의 \`%\`·\`_\` 로는 "글자 종류(한글)"를 표현할 수 없으니, 문자 종류·길이·위치를 동시에 검사해야 할 때는 정규식이 자연스러운 선택.

---

## 정규식 옵션 (4번째 인자)

\`\`\`sql
REGEXP_LIKE('ABC', 'abc', 'i')   -- 대소문자 무시
\`\`\`

| 옵션 | 의미 |
|---|---|
| i | 대소문자 무시 |
| c | 대소문자 구분(기본) |
| m | 멀티라인 |
| n | \`.\`이 줄바꿈도 매칭 |
| x | 공백 무시 |

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| \`^\`의 의미 | 문자열의 시작(\`[]\` 안에선 부정) |
| \`$\`의 의미 | 문자열의 끝 |
| \`*\` vs \`+\` | \`*\`은 0개 이상, \`+\`은 1개 이상 |
| \`.\`의 의미 | 임의의 한 글자(LIKE의 \`_\`와 같음) |
| \`\\d\`의 의미 | 숫자 [0-9] |
| \`[가-힣]\` | 한글 한 글자 |
| LIKE와 REGEXP_LIKE 차이 | LIKE는 \`%\`와 \`_\`만, REGEXP는 다양한 메타문자 사용 |

단골 함정: \`^\` 이 \`[]\` 안과 밖에서 의미가 다름. 밖에서 \`^\` 은 시작, \`[^abc]\` 안에서는 부정.

---

> **30초 시험 직전 정리**
> · 5개 함수: **REGEXP_LIKE** (조건) / **REGEXP_REPLACE** (교체) / **REGEXP_SUBSTR** (추출) / **REGEXP_INSTR** (위치) / **REGEXP_COUNT** (횟수)
> · 자주 쓰는 메타: \`.\` (1글자), \`*\` (0+), \`+\` (1+), \`?\` (0/1), \`[abc]\`, \`[^abc]\`, \`^\` (시작), \`$\` (끝)
> · \`\\d\` = 숫자 / \`\\w\` = 영숫자_  / \`\\s\` = 공백 / \`[가-힣]\` = 한글 1자
> · \`^\` 함정 = \`[]\` 밖이면 **시작** / \`[]\` 안이면 **부정**
> · 옵션 4번째 인자: \`i\` 대소문자 무시 / \`m\` 멀티라인
> · LIKE 와 차이 = 표현력 ↑, **성능 약간 ↓**
`,
  c231: `# 2-3-1. DML (Data Manipulation Language)

## [핵심 요약]

> DML은 테이블 안의 데이터를 입력·수정·삭제·조회하는 명령어들이다(INSERT, UPDATE, DELETE, MERGE, SELECT).

ex) 노트에 비유하면 INSERT는 새 줄 적기, UPDATE는 줄 고치기, DELETE는 줄 지우기, MERGE는 있으면 고치고 없으면 적기, SELECT는 읽기다.

---

## [개념 도식화] DML의 분류

\`\`\`mermaid
flowchart TD
    D[DML<br/>Data Manipulation] --> I[INSERT<br/>새 행 입력]
    D --> U[UPDATE<br/>기존 행 수정]
    D --> X[DELETE<br/>행 삭제<br/>롤백 가능]
    D --> M[MERGE<br/>UPSERT<br/>있으면 UPDATE / 없으면 INSERT]
    D --> S[SELECT<br/>조회]
\`\`\`

| 명령 | 용도 |
|---|---|
| INSERT | 새 행 입력 |
| UPDATE | 기존 행 수정 |
| DELETE | 행 삭제 |
| MERGE | UPSERT(있으면 UPDATE, 없으면 INSERT) |
| SELECT | 조회 |

---

## [SQL 실전]

## INSERT — 데이터 입력

### 기본 문법

\`\`\`sql
-- 모든 컬럼에 입력
INSERT INTO EMP VALUES (1, '김철수', 'D1', 3000);
-- 1 row inserted.

-- 특정 컬럼만
INSERT INTO EMP (EMP_ID, NAME) VALUES (2, '이영희');
-- 1 row inserted. (DEPT_ID, SAL은 NULL 또는 DEFAULT)
\`\`\`

### 다른 테이블에서 가져오기

\`\`\`sql
INSERT INTO EMP_BACKUP
SELECT * FROM EMP WHERE DEPT_ID = 'D1';
\`\`\`

### Multi-Insert (Oracle)

\`\`\`sql
INSERT ALL
    INTO TBL_A VALUES (1, 'A')
    INTO TBL_B VALUES (2, 'B')
SELECT * FROM DUAL;
\`\`\`

---

## UPDATE — 데이터 수정

\`\`\`sql
-- 모든 행 수정 (위험)
UPDATE EMP SET SAL = SAL * 1.1;

-- 조건부 수정
UPDATE EMP
SET    SAL = SAL * 1.1
WHERE  DEPT_ID = 'D1';

-- 여러 컬럼 동시 수정
UPDATE EMP
SET    SAL = 5000, DEPT_ID = 'D2'
WHERE  EMP_ID = 1;

-- 서브쿼리로 수정
UPDATE EMP
SET    SAL = (SELECT AVG(SAL) FROM EMP)
WHERE  EMP_ID = 1;
\`\`\`

WHERE를 빼먹으면 모든 행이 수정된다. 항상 WHERE를 확인한다.

---

## DELETE — 데이터 삭제

\`\`\`sql
-- 조건부 삭제
DELETE FROM EMP WHERE DEPT_ID = 'D9';

-- 모든 행 삭제(구조는 유지)
DELETE FROM EMP;
\`\`\`

### DELETE vs TRUNCATE vs DROP

| 명령 | 분류 | 데이터 | 테이블 구조 | 롤백 | 속도 |
|---|---|---|---|---|---|
| DELETE | DML | 삭제 | 유지 | 가능 | 느림 |
| TRUNCATE | [DDL](./2-3-3_DDL.md) | 모두 삭제 | 유지 | 불가 | 빠름 |
| DROP | DDL | 삭제 | 삭제 | 불가 | 매우 빠름 |

\`\`\`sql
DELETE FROM EMP;       -- 데이터만, 롤백 가능
TRUNCATE TABLE EMP;    -- 데이터만, 롤백 불가
DROP TABLE EMP;        -- 테이블 자체 삭제
\`\`\`

---

## MERGE — 있으면 UPDATE, 없으면 INSERT (UPSERT)

\`\`\`sql
MERGE INTO EMP E
USING NEW_EMP N
   ON (E.EMP_ID = N.EMP_ID)
WHEN MATCHED THEN
    UPDATE SET E.SAL = N.SAL
WHEN NOT MATCHED THEN
    INSERT (EMP_ID, NAME, SAL) VALUES (N.EMP_ID, N.NAME, N.SAL);
\`\`\`

### 시각화

\`\`\`
     NEW_EMP의 행
        │
   E.EMP_ID 매칭?
   ├ 있음 → UPDATE
   └ 없음 → INSERT
\`\`\`

---

## DML과 트랜잭션

\`\`\`sql
INSERT INTO EMP ...;
UPDATE EMP ...;
DELETE FROM EMP ...;
COMMIT;        -- 영구 반영
-- 또는
ROLLBACK;      -- 취소
\`\`\`

DML은 [트랜잭션](./1-2-3_모델이표현하는트랜잭션의이해.md)의 대상이며 COMMIT 전까지 메모리에만 있다. COMMIT/ROLLBACK 같은 트랜잭션 제어 명령은 [TCL](./2-3-2_TCL.md)에서 다룬다.

---

## [Before & After]

3행짜리 EMP 테이블에 INSERT → UPDATE → DELETE를 순서대로 적용한다. D1 부서 직원(김·이) 두 명에게만 10% 인상이 들어가고, 새로 추가했던 4번 직원(최)은 마지막에 다시 삭제되는 흐름이다.

### EMP

| EMP_ID | NAME | DEPT_ID | SAL |
|---|---|---|---|
| 1 | 김 | D1 | 3000 |
| 2 | 이 | D1 | 4000 |
| 3 | 박 | D2 | 3500 |

### \`INSERT INTO EMP VALUES (4, '최', 'D2', 4500);\`

| EMP_ID | NAME | DEPT_ID | SAL |
|---|---|---|---|
| 1 | 김 | D1 | 3000 |
| 2 | 이 | D1 | 4000 |
| 3 | 박 | D2 | 3500 |
| 4 | 최 | D2 | 4500 |

### \`UPDATE EMP SET SAL = SAL * 1.1 WHERE DEPT_ID = 'D1';\`

| EMP_ID | NAME | DEPT_ID | SAL |
|---|---|---|---|
| 1 | 김 | D1 | 3300 |
| 2 | 이 | D1 | 4400 |
| 3 | 박 | D2 | 3500 |
| 4 | 최 | D2 | 4500 |

### \`DELETE FROM EMP WHERE EMP_ID = 4;\`

| EMP_ID | NAME | DEPT_ID | SAL |
|---|---|---|---|
| 1 | 김 | D1 | 3300 |
| 2 | 이 | D1 | 4400 |
| 3 | 박 | D2 | 3500 |

DML은 행 단위로 데이터를 바꾸지만 테이블 구조는 건드리지 않는다. 위 작업은 모두 COMMIT 전까지 메모리에만 머무르므로, 잘못 친 UPDATE·DELETE는 ROLLBACK으로 되돌릴 수 있다. 반대로 같은 "삭제"라도 TRUNCATE·DROP은 DDL이라 자동 COMMIT되며 되돌릴 수 없다. 데이터만 비울지·구조까지 없앨지·되돌릴 여지를 둘지를 의식해서 명령을 고른다.

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| TRUNCATE는 DDL인가 DML인가 | DDL(롤백 불가) |
| DELETE 후 ROLLBACK 가능? | 가능(DML) |
| \`INSERT INTO ... VALUES\`는 한 번에 한 행만? | 일반 INSERT는 1행. INSERT ALL 또는 \`INSERT INTO ... SELECT\`로 다중행 |
| \`UPDATE ... WHERE\` 누락 시 | 모든 행 수정 |
| MERGE의 두 가지 절 | WHEN MATCHED / WHEN NOT MATCHED |
| DML 후 자동 커밋? | Oracle은 명시적 COMMIT 필요 |

단골 함정: TRUNCATE와 DELETE의 차이. TRUNCATE는 롤백 불가, AUTO_COMMIT, 빠름.

---

> **30초 시험 직전 정리**
> · DML 5종: **INSERT / UPDATE / DELETE / MERGE / SELECT**
> · DELETE = **DML** (롤백 O) / TRUNCATE = **DDL** (롤백 X, 자동 COMMIT) / DROP = 테이블 자체 삭제
> · UPDATE 에 WHERE 누락 = **모든 행 수정** (위험)
> · MERGE = \`WHEN MATCHED THEN UPDATE\` + \`WHEN NOT MATCHED THEN INSERT\` (UPSERT)
> · INSERT 다중행 = **\`INSERT ALL\`** 또는 **\`INSERT INTO ... SELECT\`**
> · DML 후 Oracle 은 **명시적 COMMIT 필요** (DDL 은 자동)
`,
  c232: `# 2-3-2. TCL (Transaction Control Language)

## [핵심 요약]

> TCL은 트랜잭션을 제어하는 명령어들이다(COMMIT, ROLLBACK, SAVEPOINT).

ex) 워드(Word)에서 작성 중인 내용은 메모리에만 있고, 저장 버튼이 COMMIT(영구 반영), CTRL+Z가 ROLLBACK(취소), 임시 저장 지점이 SAVEPOINT(부분 취소)에 해당한다.

---

## [개념 도식화] 트랜잭션의 흐름

\`\`\`mermaid
flowchart TD
    A[트랜잭션 시작] --> B[DML 작업들]
    B -->|필요시| F[SAVEPOINT<br/>중간 지점]
    F -->|부분 취소| G[ROLLBACK TO sp]
    B --> C{결정 시점}
    C -->|성공| D[COMMIT<br/>영구 저장 / 락 해제]
    C -->|실패| E[ROLLBACK<br/>마지막 COMMIT 이후 취소]
\`\`\`

---

## COMMIT — 영구 반영

[DML](./2-3-1_DML.md) 작업을 묶어서 영구 반영한다.

\`\`\`sql
INSERT INTO EMP VALUES (1, '김', 3000);
UPDATE EMP SET SAL = 4000 WHERE EMP_ID = 1;
COMMIT;     -- 위 두 작업을 영구 반영
\`\`\`

| COMMIT 후 | 결과 |
|---|---|
| 다른 사용자도 변경 사항을 볼 수 있음 | 가능 |
| ROLLBACK | 불가 |
| 락(Lock) 해제 | 됨 |

---

## ROLLBACK — 취소

\`\`\`sql
INSERT INTO EMP VALUES (1, '김', 3000);
DELETE FROM EMP WHERE EMP_ID = 999;
ROLLBACK;   -- 위 두 작업 모두 취소
\`\`\`

마지막 COMMIT 이후의 모든 DML이 취소된다.

---

## SAVEPOINT — 임시 저장 지점

\`\`\`sql
INSERT INTO EMP VALUES (1, '김', 3000);
SAVEPOINT SP1;

INSERT INTO EMP VALUES (2, '이', 4000);
SAVEPOINT SP2;

INSERT INTO EMP VALUES (3, '박', 5000);

-- SP2 시점으로 되돌리기 (3번 INSERT만 취소)
ROLLBACK TO SP2;
\`\`\`

| 시점 | 상태 |
|---|---|
| 시작 | EMP 비어있음 |
| SP1 | 김 |
| SP2 | 김, 이 |
| 마지막 INSERT 후 | 김, 이, 박 |
| ROLLBACK TO SP2 | 김, 이 |

---

## [트랜잭션](./1-2-3_모델이표현하는트랜잭션의이해.md)의 ACID

| 성질 | 의미 |
|---|---|
| Atomicity (원자성) | 전부 또는 전무 |
| Consistency (일관성) | 규칙 유지 |
| Isolation (고립성) | 동시 처리 격리 |
| Durability (지속성) | COMMIT 후 영구 |

---

## 자동 커밋 vs 명시적 커밋

| 상황 | Oracle | MS-SQL |
|---|---|---|
| DML 후 | 수동 COMMIT 필요 | 자동 커밋(설정 따라) |
| DDL 후 | 자동 COMMIT | 트랜잭션 가능 |
| 정상 종료 | 자동 COMMIT | — |
| 비정상 종료 | 자동 ROLLBACK | — |

Oracle에서 CREATE, DROP 같은 DDL은 자동 COMMIT된다.

---

## [SQL 실전]

\`\`\`sql
-- 트랜잭션 1: 정상 처리
INSERT INTO ACCOUNT VALUES (101, 'A', 10000);
INSERT INTO ACCOUNT VALUES (102, 'B', 5000);
COMMIT;     -- 둘 다 저장

-- 트랜잭션 2: 중간 오류
UPDATE ACCOUNT SET BALANCE = BALANCE - 1000 WHERE ID = 101;
SAVEPOINT BEFORE_DEPOSIT;

UPDATE ACCOUNT SET BALANCE = BALANCE + 1000 WHERE ID = 999;  -- 존재 X

-- 부분 취소 후 다른 처리
ROLLBACK TO BEFORE_DEPOSIT;
UPDATE ACCOUNT SET BALANCE = BALANCE + 1000 WHERE ID = 102;
COMMIT;
\`\`\`

---

## [Before & After] SAVEPOINT 동작

비어있는 EMP에 김·이·박을 차례로 INSERT하면서 SP1, SP2 두 지점을 찍어 둔다. 마지막에 \`ROLLBACK TO SP1\`을 걸면 어디까지 살아남는지, 그 뒤 COMMIT까지 어떻게 이어지는지를 시점별로 추적한다.

### EMP

| EMP_ID | NAME |
|---|---|
| (비어있음) | |

### 작업 시나리오

\`\`\`sql
INSERT INTO EMP VALUES (1, '김');
SAVEPOINT SP1;
INSERT INTO EMP VALUES (2, '이');
SAVEPOINT SP2;
INSERT INTO EMP VALUES (3, '박');
ROLLBACK TO SP1;
COMMIT;
\`\`\`

### 시점별 상태

| 시점 | 데이터 |
|---|---|
| 첫 INSERT 후 | 김 |
| SP1 | 김 |
| 둘째 INSERT 후 | 김, 이 |
| SP2 | 김, 이 |
| 셋째 INSERT 후 | 김, 이, 박 |
| ROLLBACK TO SP1 | 김 |
| COMMIT | 김 |

\`ROLLBACK TO SP1\`은 SP1 이후의 DML(이·박 INSERT)만 취소하고 그 이전 작업은 그대로 둔다. 그래서 최종 COMMIT 시점에는 김 한 행만 남는다. 트랜잭션 도중 일부만 되돌리고 싶을 때는 SAVEPOINT로 안전 지점을 찍어둔다. 단, 전체 ROLLBACK이나 COMMIT이 일어나면 SAVEPOINT는 모두 사라진다.

---

## 트랜잭션 격리 수준 (참고)

| 수준 | 설명 |
|---|---|
| READ UNCOMMITTED | 커밋 안 된 것도 읽음 |
| READ COMMITTED | 커밋된 것만(Oracle 기본) |
| REPEATABLE READ | 같은 쿼리는 같은 결과 |
| SERIALIZABLE | 완전 격리(가장 엄격) |

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| TCL 명령어 종류 | COMMIT / ROLLBACK / SAVEPOINT |
| DDL 실행 후 | Oracle은 자동 COMMIT |
| ROLLBACK의 범위 | 마지막 COMMIT 이후 모든 DML |
| SAVEPOINT의 의미 | 부분 ROLLBACK 가능 지점 |
| ROLLBACK TO SP1 후 SP1 이전 작업 | 유지 |
| ROLLBACK 후 SAVEPOINT는 | 모두 사라짐 |
| 비정상 종료 시 | 자동 ROLLBACK |

단골 함정: "DDL 실행 후 ROLLBACK으로 취소 가능"은 틀렸다. Oracle에서 DDL은 자동 COMMIT이다.

---

> **30초 시험 직전 정리**
> · TCL 3종: **COMMIT / ROLLBACK / SAVEPOINT**
> · COMMIT = 영구 반영 + 락 해제 + 다른 사용자도 변경 사항 조회 가능
> · ROLLBACK = **마지막 COMMIT 이후** 모든 DML 취소 (SAVEPOINT 도 함께 사라짐)
> · ROLLBACK TO sp = **부분 취소** (sp 이전 작업은 유지)
> · DDL 실행 = **자동 COMMIT** (롤백 불가)
> · 비정상 종료 = **자동 ROLLBACK** / 정상 종료 = 자동 COMMIT
> · Oracle 기본 격리 수준 = **READ COMMITTED**
`,
  c233: `# 2-3-3. DDL (Data Definition Language)

## [핵심 요약]

> DDL은 테이블·뷰·인덱스 같은 객체를 만들고 바꾸고 지우는 명령어들이다(CREATE, ALTER, DROP, TRUNCATE, RENAME).

---

## [개념 도식화] DDL 명령 5종

\`\`\`mermaid
flowchart TD
    D[DDL<br/>Data Definition] --> C[CREATE<br/>객체 생성<br/>테이블/뷰/인덱스]
    D --> A[ALTER<br/>구조 변경<br/>컬럼·제약]
    D --> X[DROP<br/>객체 자체 삭제]
    D --> T[TRUNCATE<br/>데이터만 비움<br/>구조 유지 · 롤백 불가]
    D --> R[RENAME<br/>객체 이름 변경]
\`\`\`

| 명령 | 의미 |
|---|---|
| CREATE | 객체(테이블/뷰/인덱스) 생성 |
| ALTER | 객체 구조 변경 |
| DROP | 객체 삭제 |
| TRUNCATE | 데이터만 비움(구조 유지) |
| RENAME | 이름 변경 |

DDL은 자동 COMMIT된다. 실행 즉시 영구 반영되며 ROLLBACK 불가.

---

## [SQL 실전]

## CREATE — 객체 생성

\`\`\`sql
CREATE TABLE EMP (
    EMP_ID    NUMBER       PRIMARY KEY,
    NAME      VARCHAR2(50) NOT NULL,
    SAL       NUMBER       DEFAULT 0,
    DEPT_ID   VARCHAR2(10),
    HIRE_DATE DATE         DEFAULT SYSDATE,
    CONSTRAINT FK_EMP_DEPT FOREIGN KEY (DEPT_ID) REFERENCES DEPT(DEPT_ID)
);
-- Table created.
\`\`\`

### 테이블 명명 규칙

| 규칙 | 설명 |
|---|---|
| 영문/숫자/\`_\`만 | 한글, 특수문자 불가 |
| 첫 글자는 영문 | 숫자로 시작 불가 |
| 예약어 사용 불가 | SELECT, FROM 등 |
| 길이 30바이트 이내 | (Oracle 기준) |
| 동일 사용자 내 중복 불가 | — |

---

## 제약조건(Constraints)

| 제약 | 의미 | 예 |
|---|---|---|
| PRIMARY KEY | 기본키(UNIQUE + NOT NULL), [식별자](./1-1-5_식별자.md) 역할 | EMP_ID |
| FOREIGN KEY | 외래키 | DEPT_ID 참조 |
| UNIQUE | 중복 불가([NULL](./1-2-4_NULL속성의이해.md) 허용) | EMAIL |
| NOT NULL | NULL 불가 | NAME |
| CHECK | 조건 만족 | CHECK (SAL > 0) |
| DEFAULT | 기본값 | DEFAULT 0 |

### PK vs UNIQUE

| 비교 | PRIMARY KEY | UNIQUE |
|---|---|---|
| 중복 | 불가 | 불가 |
| NULL | 불가 | 허용 |
| 테이블당 개수 | 1개 | 여러 개 |

### FK 옵션

\`\`\`sql
FOREIGN KEY (DEPT_ID) REFERENCES DEPT(DEPT_ID)
    ON DELETE CASCADE       -- 부모 삭제 시 자식도 삭제
    ON DELETE SET NULL      -- 부모 삭제 시 자식 FK NULL
\`\`\`

---

## ALTER — 구조 변경

\`\`\`sql
-- 컬럼 추가
ALTER TABLE EMP ADD (PHONE VARCHAR2(20));
-- Table altered.

-- 컬럼 변경
ALTER TABLE EMP MODIFY (PHONE VARCHAR2(30));
-- Table altered.

-- 컬럼 이름 변경
ALTER TABLE EMP RENAME COLUMN PHONE TO MOBILE;
-- Table altered.

-- 컬럼 삭제
ALTER TABLE EMP DROP COLUMN MOBILE;
-- Table altered.

-- 제약조건 추가/삭제
ALTER TABLE EMP ADD CONSTRAINT UQ_NAME UNIQUE (NAME);
ALTER TABLE EMP DROP CONSTRAINT UQ_NAME;
-- Table altered.
\`\`\`

---

## DROP — 객체 삭제

\`\`\`sql
DROP TABLE EMP;                  -- 테이블 자체 삭제
DROP TABLE EMP CASCADE CONSTRAINTS;  -- 참조 제약까지 함께
DROP TABLE EMP PURGE;            -- 휴지통도 비움(복구 불가)
-- Table dropped.
\`\`\`

---

## TRUNCATE — 데이터만 비우기

\`\`\`sql
TRUNCATE TABLE EMP;
-- Table truncated.
\`\`\`

| TRUNCATE 특징 | 설명 |
|---|---|
| 데이터 모두 삭제 | 됨 |
| 테이블 구조 | 유지 |
| 롤백 | 불가 |
| 속도 | 매우 빠름 |

---

## RENAME — 이름 변경

\`\`\`sql
RENAME EMP TO EMPLOYEE;
ALTER TABLE EMP RENAME TO EMPLOYEE;  -- 동일
-- Table renamed.
\`\`\`

---

## DELETE vs TRUNCATE vs DROP

| 비교 | DELETE | TRUNCATE | DROP |
|---|---|---|---|
| 분류 | [DML](./2-3-1_DML.md) | DDL | DDL |
| 데이터 | 삭제 | 모두 삭제 | 삭제 |
| 구조 | 유지 | 유지 | 삭제 |
| ROLLBACK | 가능 | 불가 | 불가 |
| WHERE | 가능 | 불가 | — |
| 속도 | 느림 | 빠름 | 매우 빠름 |
| 자동 COMMIT | 없음 | 있음 | 있음 |

---

## VIEW (뷰)

\`\`\`sql
CREATE VIEW V_EMP_HIGH AS
SELECT NAME, SAL FROM EMP WHERE SAL > 5000;

SELECT * FROM V_EMP_HIGH;     -- 사용
DROP VIEW V_EMP_HIGH;
\`\`\`

| 뷰의 장점 | |
|---|---|
| 복잡한 쿼리를 간단한 이름으로 | 됨 |
| 보안(특정 컬럼/행만 노출 — [DCL](./2-3-4_DCL.md)과 함께 권한 통제) | 됨 |
| 실제 테이블 아님(가상) | 됨 |

---

## 인덱스(INDEX)

\`\`\`sql
CREATE INDEX IDX_EMP_NAME ON EMP(NAME);
-- Index created.
DROP INDEX IDX_EMP_NAME;
-- Index dropped.
\`\`\`

도서관의 목차 같은 역할이다. 검색 속도는 빨라지지만 입력·수정 속도는 느려질 수 있다.

---

## [Before & After]

EMP_ID(PK)·NAME(NOT NULL)·SAL(DEFAULT 0)을 가진 EMP 테이블을 만들고, PHONE 컬럼을 더한 뒤, 같은 테이블에 TRUNCATE와 DROP을 차례로 적용한다. 데이터·구조·테이블 자체가 어느 명령에서 사라지는지를 비교한다.

### CREATE 후

\`\`\`
EMP
─────────
EMP_ID (PK)
NAME (NOT NULL)
SAL (DEFAULT 0)
\`\`\`

### \`ALTER TABLE EMP ADD (PHONE VARCHAR2(20))\` 후

\`\`\`
EMP
─────────
EMP_ID (PK)
NAME (NOT NULL)
SAL (DEFAULT 0)
PHONE      (추가됨)
\`\`\`

### \`TRUNCATE TABLE EMP\` 후

\`\`\`
EMP (구조 유지, 데이터 0행)
─────────
EMP_ID (PK)
NAME
SAL
PHONE
\`\`\`

### \`DROP TABLE EMP\` 후

\`\`\`
(테이블 자체가 사라짐)
\`\`\`

ALTER는 컬럼을 추가해 구조를 키우고, TRUNCATE는 데이터만 비우되 컬럼 정의(EMP_ID·NAME·SAL·PHONE)는 그대로 남기며, DROP은 테이블 객체 자체를 지운다. 셋 다 DDL이라 실행 즉시 자동 COMMIT되어 ROLLBACK으로 되돌릴 수 없다. "데이터만 비우고 싶다 → TRUNCATE", "테이블 자체가 필요 없다 → DROP", "구조만 바꾼다 → ALTER" 식으로 의도와 명령을 정확히 매칭한다.

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| TRUNCATE 분류 | DDL |
| DDL 실행 후 ROLLBACK 가능? | 불가(자동 COMMIT) |
| PRIMARY KEY는 NULL 허용? | 불허 |
| UNIQUE는 NULL 허용? | 허용 |
| 한 테이블에 PK는 몇 개? | 1개 |
| FK가 참조하는 컬럼 조건 | PK 또는 UNIQUE |
| CHECK 제약 예 | \`CHECK (SAL > 0)\` |
| ON DELETE CASCADE | 부모 삭제 시 자식도 삭제 |
| \`ALTER TABLE ... DROP COLUMN\`은 ROLLBACK 가능? | 불가 |

단골 함정: TRUNCATE는 DDL이지만 데이터만 삭제(구조 유지)된다. DROP은 테이블 자체를 삭제한다.

---

> **30초 시험 직전 정리**
> · DDL 5종: **CREATE / ALTER / DROP / TRUNCATE / RENAME** — 모두 **자동 COMMIT** (롤백 불가)
> · 제약 6종: **PK / FK / UNIQUE / NOT NULL / CHECK / DEFAULT**
> · PK = **UNIQUE + NOT NULL** / 테이블당 1개 / FK 가 참조하는 컬럼은 PK 또는 UNIQUE
> · UNIQUE = **NULL 허용** (중복만 막음)
> · DELETE / TRUNCATE / DROP — **데이터·구조·롤백** 차이 (가장 단골 함정)
> · ON DELETE CASCADE = 부모 삭제 시 자식도 자동 삭제
> · 컬럼 추가 = \`ALTER TABLE ADD\` / 변경 = \`MODIFY\` / 삭제 = \`DROP COLUMN\`
`,
  c234: `# 2-3-4. DCL (Data Control Language)

## [핵심 요약]

> DCL은 사용자에게 권한을 주거나 회수하는 명령어들이다(GRANT, REVOKE).

ex) 사무실 출입카드처럼 신입에게 발급(GRANT)·퇴사자 회수(REVOKE)·부서별 묶음(ROLE)으로 누가 어떤 테이블을 SELECT/INSERT/UPDATE 할 수 있는지를 통제한다.

---

## [개념 도식화] DCL 흐름

\`\`\`mermaid
flowchart LR
    A[관리자 DBA] -->|GRANT| U1[일반 사용자]
    A -->|REVOKE| U1
    A -->|ROLE 생성| R[권한 묶음<br/>ROLE]
    R -->|GRANT| U2[일반 사용자]
\`\`\`

---

## 권한의 종류

### 시스템 권한 (System Privilege)

| 권한 | 의미 |
|---|---|
| CREATE SESSION | 접속 권한 |
| CREATE TABLE | 테이블 생성 권한 |
| CREATE VIEW | 뷰 생성 권한 |
| CREATE USER | 사용자 생성 권한 |
| DROP ANY TABLE | 모든 테이블 삭제 권한 |

### 객체 권한 (Object Privilege)

| 권한 | 의미 |
|---|---|
| SELECT | 조회 |
| INSERT | 입력 |
| UPDATE | 수정 |
| DELETE | 삭제 |
| ALTER | 변경 |
| EXECUTE | 프로시저/함수 실행 |
| REFERENCES | FK로 참조 |

---

## GRANT — 권한 부여

\`\`\`sql
-- 시스템 권한
GRANT CREATE SESSION TO USER1;
GRANT CREATE TABLE   TO USER1;

-- 객체 권한
GRANT SELECT ON EMP TO USER1;
GRANT SELECT, INSERT, UPDATE ON EMP TO USER1;

-- 모든 사용자에게
GRANT SELECT ON EMP TO PUBLIC;

-- 권한 부여 가능 권한도 함께(재부여 가능)
GRANT SELECT ON EMP TO USER1 WITH GRANT OPTION;
\`\`\`

| 옵션 | 의미 |
|---|---|
| WITH GRANT OPTION | 객체 권한: 받은 사람이 다른 사람에게 다시 줄 수 있음 |
| WITH ADMIN OPTION | 시스템 권한: 관리자처럼 다시 부여 가능 |

---

## REVOKE — 권한 회수

\`\`\`sql
REVOKE SELECT ON EMP FROM USER1;
REVOKE CREATE TABLE FROM USER1;
\`\`\`

### 연쇄 회수

WITH GRANT OPTION으로 다른 사람에게 부여된 권한도 함께 회수된다.

\`\`\`
관리자 → USER1 (WITH GRANT OPTION) → USER2
        REVOKE 시 USER2 권한도 함께 사라짐
\`\`\`

---

## ROLE — 권한 묶음

\`\`\`sql
-- 1. 역할 만들기
CREATE ROLE DEV_ROLE;

-- 2. 역할에 권한 부여
GRANT SELECT, INSERT, UPDATE ON EMP TO DEV_ROLE;
GRANT SELECT ON DEPT                TO DEV_ROLE;

-- 3. 사용자에게 역할 부여
GRANT DEV_ROLE TO USER1;
GRANT DEV_ROLE TO USER2;

-- 회수
REVOKE DEV_ROLE FROM USER1;
\`\`\`

\`\`\`mermaid
flowchart LR
    P1[권한 1] --> R[ROLE 묶음]
    P2[권한 2] --> R
    P3[권한 3] --> R
    R -->|GRANT| U1[USER 1]
    R -->|GRANT| U2[USER 2]
\`\`\`

---

## 사용자 관리 (참고)

CREATE USER, ALTER USER, DROP USER 자체는 [DDL](./2-3-3_DDL.md)이지만 권한 관리와 짝을 이룬다.

\`\`\`sql
-- 사용자 생성
CREATE USER USER1 IDENTIFIED BY password123;

-- 비밀번호 변경
ALTER USER USER1 IDENTIFIED BY newpass;

-- 사용자 삭제
DROP USER USER1;
DROP USER USER1 CASCADE;     -- 소유 객체까지 모두 삭제
\`\`\`

Oracle에서 사용자 = 스키마(객체 모음). 사용자 생성 권한은 DBA가 가진다.

---

## [SQL 실전] 권한 흐름

\`\`\`sql
-- 관리자(DBA)로 접속
CREATE USER STAFF IDENTIFIED BY pw1234;

GRANT CREATE SESSION TO STAFF;       -- 접속 권한
GRANT SELECT ON HR.EMP TO STAFF;     -- HR 스키마 EMP 조회 권한

-- STAFF로 접속
SELECT * FROM HR.EMP;     -- 가능

-- 권한 회수
REVOKE SELECT ON HR.EMP FROM STAFF;

SELECT * FROM HR.EMP;     -- 권한 부족 에러
\`\`\`

---

## [Before & After]

방금 만들어진 USER1 계정에 권한을 한 단계씩 부여하다가 마지막에 SELECT 한 가지만 회수한다. 시스템 권한(CREATE SESSION)이 먼저 있어야 접속이 되고, 그 위에 객체 권한(SELECT/INSERT)이 올라가는 순서를 따라간다.

### USER1의 권한 변화

| 시점 | 가능한 작업 |
|---|---|
| 초기 | 접속도 불가 |
| GRANT CREATE SESSION 후 | 접속 가능 |
| GRANT SELECT ON EMP 후 | EMP 조회 가능 |
| GRANT INSERT ON EMP 후 | EMP 입력 가능 |
| REVOKE SELECT ON EMP 후 | EMP 조회 불가 |

권한은 "접속(시스템 권한) → 개별 객체 작업(객체 권한)" 순으로 쌓인다. CREATE SESSION이 없으면 SELECT 권한이 있어도 접속이 안 되고, REVOKE는 해당 권한만 떼어내므로 INSERT 권한은 그대로 남는다. WITH GRANT OPTION으로 재부여된 권한이라면 REVOKE 한 번에 연쇄적으로 회수된다.

---

## DBA의 대표 ROLE

| ROLE | 설명 |
|---|---|
| CONNECT | 접속 권한(CREATE SESSION 포함) |
| RESOURCE | 객체 생성 권한(테이블, 시퀀스 등) |
| DBA | 모든 권한 |

\`\`\`sql
GRANT CONNECT, RESOURCE TO USER1;
\`\`\`

---

## DCL vs 다른 명령어 분류

\`\`\`
DDL: CREATE, ALTER, DROP, TRUNCATE, RENAME       (객체)
DML: INSERT, UPDATE, DELETE, MERGE, SELECT       (데이터)
TCL: COMMIT, ROLLBACK, SAVEPOINT                 (트랜잭션)
DCL: GRANT, REVOKE                               (권한)
\`\`\`

---

## [시험 출제 포인트]

| 함정 | 정답 |
|---|---|
| DCL 명령어 | GRANT, REVOKE |
| 시스템 권한 부여 시 옵션 | WITH ADMIN OPTION |
| 객체 권한 부여 시 옵션 | WITH GRANT OPTION |
| ROLE의 목적 | 권한 묶음을 한 번에 관리 |
| \`GRANT DEV_ROLE TO USER1\`의 분류 | DCL |
| 모든 사용자에게 권한 | TO PUBLIC |
| WITH GRANT OPTION 회수 영향 | 연쇄 회수(다시 부여한 사용자도) |

단골 함정: GRANT/REVOKE를 DDL이나 DML로 잘못 분류하는 보기. DCL이 정답이다.

---

> **30초 시험 직전 정리**
> · DCL 명령: **GRANT / REVOKE** (ROLE 부여도 GRANT 로 함)
> · 권한 종류: **시스템 권한** (CREATE SESSION/TABLE 등) + **객체 권한** (SELECT/INSERT/UPDATE 등)
> · 옵션: **WITH GRANT OPTION** (객체 권한 재부여) / **WITH ADMIN OPTION** (시스템 권한 재부여)
> · ROLE = 권한 묶음 → 여러 사용자에 한 번에 부여 (CONNECT / RESOURCE / DBA 가 대표)
> · \`TO PUBLIC\` = 모든 사용자에게
> · WITH GRANT OPTION 으로 재부여한 권한 = REVOKE 시 **연쇄 회수**
> · 4대 분류 한 줄 정리: **DDL=객체 / DML=데이터 / TCL=트랜잭션 / DCL=권한**
`
};
