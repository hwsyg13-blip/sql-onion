// @ts-nocheck
// 자동 생성 — 'SQLD 이론/_mockups/exam-day-summary.html' 의 <article class="theory-md"> 본문.
// 다시 빌드: node scripts/build-exam-day-summary.mjs
//
// CheatSheetScreen 이 dangerouslySetInnerHTML 로 렌더. 페이지 스타일은
// src/styles/examDaySummary.css 가 main.tsx 에서 로드.

export const EXAM_DAY_SUMMARY_HTML = "<nav class=\"day-toc reveal\">\n    <span class=\"toc-label\">바로가기</span>\n    <a href=\"#chapters\">챕터별 정리</a>\n    <a href=\"#checklist\">체크리스트</a>\n  </nav>\n<!-- ── 챕터별 핵심 정리 + 빈출 함정 ── -->\n  <section class=\"day-section\" id=\"chapters\">\n    <h2><span class=\"num\">1</span>챕터별 핵심 정리 + 빈출 함정</h2>\n    <p class=\"section-lead\">30개 챕터의 핵심 포인트와 자주 출제되는 함정을 한 카드에. 4과목 분류로 묶음.</p>\n\n    <h3 class=\"subj-h\">1과목 — 데이터 모델링의 이해</h3>\n    <div class=\"day-card-grid\">\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">1-1-1</span><span class=\"card-title\">데이터 모델의 이해</span></div>\n        <ul class=\"points\">\n          <li>3단계: <strong>개념 → 논리 → 물리</strong></li>\n          <li>3대 관점: <strong>데이터 / 프로세스 / 상관</strong></li>\n          <li>3층 스키마: 외부(View) / 개념(ERD) / 내부(저장)</li>\n          <li>좋은 모델 6요소: 완전성·중복배제·업무규칙·재사용·의사소통·통합성</li>\n        </ul>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>\"물리 모델링이 가장 먼저\" → <strong>X</strong> (개념 → 논리 → 물리)</li>\n            <li>\"정규화는 물리 단계\" → <strong>X</strong> (논리 단계)</li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">1-1-2</span><span class=\"card-title\">엔터티</span></div>\n        <ul class=\"points\">\n          <li>5대 특징 <strong>업·식·인·속·관</strong> — 업무 필요·식별·인스턴스 ≥ 2·속성 ≥ 2·관계</li>\n          <li>유무형 분류: 유형 / 개념 / 사건</li>\n          <li>발생 시점 분류: <strong>기본 → 중심 → 행위</strong></li>\n        </ul>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>인스턴스 1개도 OK → <strong>X (≥ 2)</strong></li>\n            <li>속성 1개도 OK → <strong>X (≥ 2)</strong></li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">1-1-3</span><span class=\"card-title\">속성</span></div>\n        <ul class=\"points\">\n          <li>분류: <strong>기본 / 설계 / 파생</strong></li>\n          <li>구성 분류: 단순 / 복합 / 다치</li>\n          <li>한 컬럼에 한 값(원자성) — 1NF의 기본</li>\n        </ul>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>다치 속성(여러 값) 그대로 OK → <strong>X (1NF 위반)</strong></li>\n            <li>파생 속성은 저장하면 안 된다 → 상황에 따라(성능 시 허용)</li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">1-1-4</span><span class=\"card-title\">관계</span></div>\n        <ul class=\"points\">\n          <li>차수: <strong>1:1 / 1:N / M:N</strong></li>\n          <li>식별 관계 vs 비식별 관계</li>\n          <li>M:N은 분해(연관 엔터티) 필요</li>\n        </ul>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>M:N 그대로 RDB 구현 → <strong>X (분해 필수)</strong></li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">1-1-5</span><span class=\"card-title\">식별자</span></div>\n        <ul class=\"points\">\n          <li>4대 조건 <strong>유·최·불·존</strong> — 유일성·최소성·불변성·존재성</li>\n          <li>4대 분류: 주/보조, 본질/인조, 단일/복합, 내부/외부</li>\n          <li><strong>PK = UNIQUE + NOT NULL</strong>, 테이블당 1개</li>\n        </ul>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>PK 여러 개 가능? → <strong>X (1개)</strong></li>\n            <li>UNIQUE는 NULL 안 됨? → <strong>X (NULL 허용)</strong></li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">1-2-1</span><span class=\"card-title\">정규화</span></div>\n        <ul class=\"points\">\n          <li><strong>1NF → 2NF → 3NF → BCNF</strong></li>\n          <li><strong>부분종속 = 2NF 위반</strong> / <strong>이행종속 = 3NF 위반</strong></li>\n          <li>이상현상 3종: 삽입·갱신·삭제 이상</li>\n          <li>반정규화: 의도적 중복 허용(성능 위함)</li>\n        </ul>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>BCNF가 3NF보다 약하다 → <strong>X (더 강함)</strong></li>\n            <li>정규화는 항상 성능 개선 → <strong>X (조인 늘어 느려질 수 있음)</strong></li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">1-2-2</span><span class=\"card-title\">관계와 조인의 이해</span></div>\n        <ul class=\"points\">\n          <li><strong>관계 = FK = JOIN</strong>의 토대</li>\n          <li>필수성(NOT NULL)·식별성(PK 포함 여부)</li>\n        </ul>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>FK 없으면 조인 불가 → <strong>X (논리적으론 가능, 권장 X)</strong></li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">1-2-3</span><span class=\"card-title\">트랜잭션의 이해</span></div>\n        <ul class=\"points\">\n          <li><strong>ACID</strong> — 원자성 / 일관성 / 고립성 / 지속성</li>\n          <li>모델의 필수성·식별성이 트랜잭션 무결성과 연결</li>\n        </ul>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>ACID는 3개 원칙 → <strong>X (4개)</strong></li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">1-2-4</span><span class=\"card-title\">NULL 속성의 이해</span></div>\n        <ul class=\"points\">\n          <li><code>= NULL</code> 안 됨 — <strong><code>IS NULL</code> / <code>IS NOT NULL</code>만 가능</strong></li>\n          <li>NULL과 산술 → 결과는 모두 <strong>NULL</strong></li>\n          <li>SQL 3-값 논리: TRUE / FALSE / <strong>UNKNOWN</strong></li>\n          <li>집계: <code>COUNT(*)</code>만 NULL 포함 / 나머지는 NULL 무시</li>\n          <li>NULL 처리 함수: <code>NVL</code> / <code>NVL2</code> / <code>NULLIF</code> / <code>COALESCE</code></li>\n        </ul>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정 (시험 단골)</div>\n          <ul>\n            <li><code>NULL = NULL</code> → <strong>UNKNOWN</strong> (FALSE 아님)</li>\n            <li><code>WHERE COL = NULL</code> → <strong>결과 0건</strong>. <code>IS NULL</code> 사용</li>\n            <li><code>NULL + 100</code> → <strong>NULL</strong></li>\n            <li><code>AVG(컬럼)</code>의 분모는 <strong>NULL 제외 개수</strong> (0 포함 평균과 다름)</li>\n            <li><code>NULLIF(A, B)</code> — A=B면 <strong>NULL</strong></li>\n            <li><code>COALESCE(NULL, NULL, 'X')</code> → <strong>'X'</strong></li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">1-2-5</span><span class=\"card-title\">본질·인조 식별자</span></div>\n        <ul class=\"points\">\n          <li><strong>본질식별자</strong>: 업무 의미가 있는 자연 키 (주민번호, ISBN 등)</li>\n          <li><strong>인조식별자</strong>: 의도적으로 만든 대리키 (시퀀스, GUID)</li>\n          <li>인조키는 가독성↓·중복 위험, 별도 UNIQUE 제약 필요</li>\n        </ul>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>본질식별자가 항상 좋다 → <strong>X (보안·변경 위험 시 인조 선호)</strong></li>\n          </ul>\n        </div>\n      </div>\n    </div>\n\n    <h3 class=\"subj-h\">2과목 — SQL 기본 (2-1)</h3>\n    <div class=\"day-card-grid\">\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">2-1-1</span><span class=\"card-title\">관계형 DB 개요</span></div>\n        <ul class=\"points\">\n          <li>SQL 분류: <strong>DDL / DML / DCL / TCL</strong></li>\n          <li>DDL: CREATE/ALTER/DROP/TRUNCATE/RENAME — <strong>자동 COMMIT</strong></li>\n          <li>DML: INSERT/UPDATE/DELETE/MERGE — <strong>ROLLBACK 가능</strong></li>\n          <li>DCL: GRANT/REVOKE — 자동 COMMIT</li>\n          <li>TCL: COMMIT/ROLLBACK/SAVEPOINT</li>\n        </ul>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li><code>TRUNCATE</code>는 DML이다 → <strong>X (DDL — 자동 COMMIT, ROLLBACK 불가)</strong></li>\n            <li><code>DELETE</code>는 DDL이다 → <strong>X (DML — ROLLBACK 가능)</strong></li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">2-1-2</span><span class=\"card-title\">SELECT문</span></div>\n        <ul class=\"points\">\n          <li>작성 순서: <strong>SELECT</strong> - FROM - WHERE - GROUP BY - HAVING - ORDER BY</li>\n          <li>실행 순서: <strong>FROM → WHERE → GROUP → HAVING → SELECT → ORDER</strong> (FWGHSO)</li>\n          <li>DUAL: Oracle 시스템 가상 테이블 (1행 1컬럼)</li>\n          <li>DISTINCT로 중복 제거</li>\n        </ul>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>SELECT가 가장 먼저 실행 → <strong>X (거의 마지막)</strong></li>\n            <li>SELECT의 별칭(AS)을 WHERE에서 사용 → <strong>X (WHERE는 SELECT보다 먼저 실행)</strong></li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">2-1-3</span><span class=\"card-title\">함수</span></div>\n        <ul class=\"points\">\n          <li>단일행 vs 다중행(집계)</li>\n          <li>문자: UPPER/LOWER/SUBSTR/INSTR/LENGTH/LPAD/RPAD/REPLACE/CONCAT(<code>||</code>)</li>\n          <li>숫자: ROUND/TRUNC/MOD/CEIL/FLOOR</li>\n          <li>날짜: SYSDATE/ADD_MONTHS/MONTHS_BETWEEN/LAST_DAY/NEXT_DAY</li>\n          <li>변환: TO_CHAR/TO_NUMBER/TO_DATE</li>\n          <li>NULL: NVL/NVL2/NULLIF/COALESCE</li>\n          <li>조건: <strong>CASE</strong>(범위 가능) vs <strong>DECODE</strong>(동등비교만)</li>\n        </ul>\n        <div class=\"ex-sql\">\n          <div class=\"ex-label\">예시</div>\n<pre><span class=\"cm\">-- 문자/숫자/날짜</span>\n<span class=\"kw\">SELECT</span> SUBSTR(<span class=\"kw\">'SQLD'</span>,1,3)        <span class=\"cm\">→ 'SQL'</span>\n     , LENGTH(<span class=\"kw\">'양파'</span>)             <span class=\"cm\">→ 2</span>\n     , ROUND(123.456, 1)         <span class=\"cm\">→ 123.5</span>\n     , MOD(10, 3)                <span class=\"cm\">→ 1</span>\n     , TO_CHAR(SYSDATE,<span class=\"kw\">'YYYY-MM-DD'</span>)\n<span class=\"kw\">FROM</span> DUAL;\n\n<span class=\"cm\">-- NULL/조건</span>\nNVL(SAL, 0), NVL2(COMM,<span class=\"kw\">'있음'</span>,<span class=\"kw\">'없음'</span>)\n<span class=\"kw\">CASE WHEN</span> SAL &gt; 3000 <span class=\"kw\">THEN</span> <span class=\"kw\">'高'</span> <span class=\"kw\">ELSE</span> <span class=\"kw\">'低' END</span>\nDECODE(JOB, <span class=\"kw\">'CLERK'</span>, 1, <span class=\"kw\">'SALES'</span>, 2, 0)</pre>\n        </div>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>DECODE로 BETWEEN 같은 범위 비교 가능 → <strong>X (CASE만 가능)</strong></li>\n            <li>날짜 - 날짜 = 날짜 → <strong>X (일수, 숫자)</strong></li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">2-1-4</span><span class=\"card-title\">WHERE절</span></div>\n        <ul class=\"points\">\n          <li>비교: =, &lt;&gt;, !=, &gt;, &lt;</li>\n          <li>논리: AND > OR (AND 우선)</li>\n          <li>SQL 연산자: <strong>BETWEEN</strong>(양 끝 포함) / <strong>IN</strong> / <strong>LIKE</strong>(<code>_</code>=1글자, <code>%</code>=0+) / <strong>IS NULL</strong></li>\n        </ul>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>BETWEEN 100 AND 200 → 100, 200 <strong>포함</strong></li>\n            <li><code>NOT IN (서브쿼리)</code>에 NULL 있으면 → <strong>결과 0건</strong> 위험</li>\n            <li>AND/OR 우선순위 헷갈리면 <strong>괄호 사용</strong></li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">2-1-5</span><span class=\"card-title\">GROUP BY / HAVING</span></div>\n        <ul class=\"points\">\n          <li><strong>WHERE는 집계 전</strong>, <strong>HAVING은 집계 후</strong></li>\n          <li>SELECT의 일반 컬럼은 GROUP BY에 명시 필수 (집계 함수는 예외)</li>\n          <li>NULL은 하나의 그룹으로 처리</li>\n        </ul>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>HAVING 없이 집계 결과 필터 → <strong>X</strong></li>\n            <li>SELECT의 일반 컬럼이 GROUP BY 없으면 → <strong>에러</strong></li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">2-1-6</span><span class=\"card-title\">ORDER BY절</span></div>\n        <ul class=\"points\">\n          <li>실행 순서 마지막. ASC(기본) / DESC</li>\n          <li>NULLS FIRST / NULLS LAST 직접 지정 가능</li>\n          <li>SELECT의 별칭·컬럼 위치(1, 2, ...) 사용 가능</li>\n        </ul>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>Oracle ASC 기본 NULL 위치 → <strong>마지막 (NULLS LAST)</strong></li>\n            <li>MS-SQL ASC 기본 NULL 위치 → <strong>처음 (NULLS FIRST)</strong></li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">2-1-7</span><span class=\"card-title\">조인 (JOIN)</span></div>\n        <ul class=\"points\">\n          <li>5종: <strong>EQUI / Non-EQUI / SELF / OUTER / CROSS</strong></li>\n          <li>EQUI: <code>=</code>로 연결 (가장 흔함)</li>\n          <li>Non-EQUI: BETWEEN, &gt;, &lt; 등으로 연결</li>\n          <li>SELF: 같은 테이블 두 번 조인 (별칭 필수)</li>\n          <li>OUTER: LEFT/RIGHT/FULL — 매칭 안 돼도 한쪽 다 포함</li>\n          <li>CROSS: 카티션 곱 (조인 조건 없음)</li>\n        </ul>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>OUTER JOIN 조건은 <strong>ON에</strong>. WHERE에 두면 NULL 행 제외돼 INNER 효과</li>\n            <li>CROSS JOIN: 조인 조건 빼먹어도 발생 → <strong>의도치 않으면 위험</strong></li>\n            <li>Oracle <code>(+)</code>는 <strong>NULL로 보충될 쪽(없을 수 있는 쪽)</strong>에 붙임</li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">2-1-8</span><span class=\"card-title\">표준 조인</span></div>\n        <ul class=\"points\">\n          <li>INNER / LEFT OUTER / RIGHT OUTER / FULL OUTER / CROSS / NATURAL / USING</li>\n          <li><strong>NATURAL</strong>: 같은 이름·타입 컬럼 자동 조인 (조건 명시 X)</li>\n          <li><strong>USING</strong>: 같은 이름 컬럼 명시</li>\n          <li>FULL OUTER: 양쪽 다 (없는 쪽 NULL)</li>\n        </ul>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>NATURAL JOIN: 이름 같고 <strong>타입 다르면 에러</strong></li>\n            <li>MySQL은 FULL OUTER 미지원 → LEFT UNION RIGHT</li>\n          </ul>\n        </div>\n      </div>\n    </div>\n\n    <h3 class=\"subj-h\">2과목 — SQL 활용 (2-2)</h3>\n    <div class=\"day-card-grid\">\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">2-2-1</span><span class=\"card-title\">서브쿼리</span></div>\n        <ul class=\"points\">\n          <li>위치별: 스칼라 / 인라인뷰 / WHERE 서브쿼리</li>\n          <li>행 수별: 단일행(=, &lt;, &gt; 등) / 다중행(IN, ANY, ALL, EXISTS)</li>\n          <li>연관 vs 비연관 서브쿼리</li>\n        </ul>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li><strong>ANY = 최소 기준</strong>(중 어느 하나보다 작아도 OK) / <strong>ALL = 최대 기준</strong>(모두보다 커야)</li>\n            <li><code>IN (a, b, c)</code> = OR 연결의 단축</li>\n            <li>스칼라 서브쿼리는 <strong>1행 1컬럼만 반환</strong></li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">2-2-2</span><span class=\"card-title\">집합 연산자</span></div>\n        <ul class=\"points\">\n          <li><strong>UNION</strong>: 합집합 (정렬 + 중복 제거)</li>\n          <li><strong>UNION ALL</strong>: 합집합 (그대로, 빠름)</li>\n          <li><strong>INTERSECT</strong>: 교집합 / <strong>MINUS</strong>(EXCEPT): 차집합</li>\n          <li>컬럼 수·타입 일치 필수</li>\n        </ul>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>UNION ALL은 정렬 안 함 → <strong>O (UNION만 정렬)</strong></li>\n            <li>컬럼 수 다르면 → <strong>에러</strong></li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">2-2-3</span><span class=\"card-title\">그룹 함수</span></div>\n        <ul class=\"points\">\n          <li><strong>ROLLUP(A, B)</strong>: N+1 단계 — (A,B), (A), () 3단계</li>\n          <li><strong>CUBE(A, B)</strong>: 2^N 단계 — 모든 조합 4가지</li>\n          <li><strong>GROUPING SETS</strong>: 지정한 조합만 (가장 유연)</li>\n          <li><code>GROUPING()</code> 함수로 소계 행 식별 (1=소계, 0=원본)</li>\n        </ul>\n        <div class=\"ex-sql\">\n          <div class=\"ex-label\">예시</div>\n<pre><span class=\"kw\">SELECT</span> DEPT, JOB, <span class=\"kw\">SUM</span>(SAL)\n<span class=\"kw\">FROM</span> EMP\n<span class=\"kw\">GROUP BY</span> ROLLUP(DEPT, JOB);\n<span class=\"cm\">-- (DEPT,JOB) → (DEPT) → ()  3단계</span>\n\n<span class=\"kw\">GROUP BY</span> CUBE(DEPT, JOB);\n<span class=\"cm\">-- (D,J),(D),(J),()  4단계 (2^2)</span>\n\n<span class=\"kw\">GROUP BY</span> GROUPING SETS((DEPT,JOB),(DEPT));\n<span class=\"cm\">-- 지정한 조합만</span></pre>\n        </div>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>ROLLUP(A,B,C) → <strong>4단계</strong> ((A,B,C), (A,B), (A), ())</li>\n            <li>CUBE(A,B,C) → <strong>8단계 (2^3)</strong></li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">2-2-4</span><span class=\"card-title\">윈도우 함수</span></div>\n        <ul class=\"points\">\n          <li>순위: <strong>RANK</strong>(1,1,3) / <strong>DENSE_RANK</strong>(1,1,2) / <strong>ROW_NUMBER</strong>(1,2,3)</li>\n          <li>행 순서: LAG / LEAD (앞·뒤 행 값)</li>\n          <li>집계 OVER: SUM/AVG/COUNT OVER PARTITION BY</li>\n          <li>WINDOWING: ROWS BETWEEN ... AND ... (행 범위 지정)</li>\n        </ul>\n        <div class=\"ex-sql\">\n          <div class=\"ex-label\">예시</div>\n<pre><span class=\"kw\">SELECT</span> ENAME, DEPT, SAL,\n  <span class=\"kw\">RANK</span>()       <span class=\"kw\">OVER</span>(<span class=\"kw\">PARTITION BY</span> DEPT <span class=\"kw\">ORDER BY</span> SAL <span class=\"kw\">DESC</span>) R,\n  DENSE_RANK() <span class=\"kw\">OVER</span>(<span class=\"kw\">PARTITION BY</span> DEPT <span class=\"kw\">ORDER BY</span> SAL <span class=\"kw\">DESC</span>) DR,\n  ROW_NUMBER() <span class=\"kw\">OVER</span>(<span class=\"kw\">ORDER BY</span> SAL <span class=\"kw\">DESC</span>) RN,\n  LAG(SAL,1)   <span class=\"kw\">OVER</span>(<span class=\"kw\">ORDER BY</span> HIRE) PREV,\n  <span class=\"kw\">SUM</span>(SAL)    <span class=\"kw\">OVER</span>(<span class=\"kw\">PARTITION BY</span> DEPT) DEPT_SUM\n<span class=\"kw\">FROM</span> EMP;\n<span class=\"cm\">-- RANK: 1,1,3 / DENSE_RANK: 1,1,2 / ROW_NUMBER: 1,2,3</span></pre>\n        </div>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>RANK는 동률 다음 순위 <strong>건너뜀</strong>, DENSE_RANK는 <strong>연속</strong></li>\n            <li>LAG/LEAD 기본 오프셋은 <strong>1</strong></li>\n            <li>PARTITION BY 없으면 전체가 한 그룹</li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">2-2-5</span><span class=\"card-title\">Top N 쿼리</span></div>\n        <ul class=\"points\">\n          <li>Oracle <strong>ROWNUM</strong>: 의사컬럼, 인라인뷰 + ORDER BY 후 사용</li>\n          <li>표준 <strong>FETCH FIRST</strong> N ROWS ONLY/WITH TIES (Oracle 12c+)</li>\n          <li>MS-SQL <strong>TOP</strong> N / TOP N WITH TIES</li>\n          <li>윈도우 <strong>ROW_NUMBER() OVER</strong> + PARTITION BY (그룹별 TopN)</li>\n        </ul>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>ROWNUM은 <strong>정렬 전 매겨짐</strong> — 인라인뷰로 정렬 먼저</li>\n            <li><code>ROWNUM = N</code> (N≥2) → <strong>결과 0건</strong></li>\n            <li><code>ROWNUM &gt; N</code> → <strong>항상 거짓</strong></li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">2-2-6</span><span class=\"card-title\">계층형 질의</span></div>\n        <ul class=\"points\">\n          <li><code>START WITH</code> ... <code>CONNECT BY PRIOR</code></li>\n          <li><strong>LEVEL</strong> 의사컬럼 (계층 깊이)</li>\n          <li>SYS_CONNECT_BY_PATH (경로 출력)</li>\n          <li>ORDER SIBLINGS BY (같은 LEVEL 정렬)</li>\n        </ul>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>PRIOR 위치가 <strong>부모 결정</strong> (PRIOR 컬럼 = 부모 컬럼)</li>\n            <li>일반 ORDER BY는 LEVEL 순서를 흩트림 → ORDER SIBLINGS BY 사용</li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">2-2-7</span><span class=\"card-title\">PIVOT / UNPIVOT</span></div>\n        <ul class=\"points\">\n          <li><strong>PIVOT</strong>: 세로 → 가로 변환</li>\n          <li><strong>UNPIVOT</strong>: 가로 → 세로 변환</li>\n          <li>PIVOT은 <strong>집계 함수 필수</strong> (SUM, COUNT 등)</li>\n          <li>UNPIVOT는 NULL 기본 제외 → <code>INCLUDE NULLS</code> 옵션으로 포함</li>\n        </ul>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>PIVOT은 집계 없이도 가능 → <strong>X (집계 필수)</strong></li>\n            <li>UNPIVOT은 NULL 자동 포함 → <strong>X (기본 제외)</strong></li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">2-2-8</span><span class=\"card-title\">정규 표현식</span></div>\n        <ul class=\"points\">\n          <li>5함수: REGEXP_LIKE / REPLACE / SUBSTR / INSTR / COUNT</li>\n          <li>메타문자: <code>.</code> <code>*</code> <code>+</code> <code>?</code> <code>[]</code> <code>^</code> <code>$</code> <code>|</code> <code>{n}</code></li>\n          <li>한글: <code>[가-힣]</code></li>\n        </ul>\n        <div class=\"ex-sql\">\n          <div class=\"ex-label\">예시</div>\n<pre><span class=\"cm\">-- 한글만 / 휴대폰 형식</span>\n<span class=\"kw\">WHERE</span> REGEXP_LIKE(NAME, <span class=\"kw\">'^[가-힣]+$'</span>)\n<span class=\"kw\">WHERE</span> REGEXP_LIKE(PHONE, <span class=\"kw\">'^010-[0-9]{4}-[0-9]{4}$'</span>)\n\n<span class=\"cm\">-- 숫자 추출</span>\n<span class=\"kw\">SELECT</span> REGEXP_SUBSTR(<span class=\"kw\">'A123B45'</span>, <span class=\"kw\">'[0-9]+'</span>) <span class=\"kw\">FROM</span> DUAL;\n<span class=\"cm\">→ '123'</span>\n\n<span class=\"kw\">SELECT</span> REGEXP_REPLACE(STR, <span class=\"kw\">'[^0-9]'</span>, <span class=\"kw\">''</span>) <span class=\"kw\">FROM</span> ...;\n<span class=\"cm\">-- 숫자만 남김</span></pre>\n        </div>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>Oracle은 <strong>POSIX 1003.2 기반</strong> — <code>\\d</code>·<code>\\w</code>·<code>\\s</code>는 12c 이후 일부 동작</li>\n            <li>호환 안전: <code>[0-9]</code>·<code>[[:digit:]]</code> 사용</li>\n          </ul>\n        </div>\n      </div>\n    </div>\n\n    <h3 class=\"subj-h\">2과목 — 관리 구문 (2-3) · 명령어 분류 포함</h3>\n    <div class=\"day-card-grid\">\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">2-3-1</span><span class=\"card-title\">DML</span></div>\n        <ul class=\"points\">\n          <li><strong>분류</strong>: INSERT / UPDATE / DELETE / MERGE (SELECT 포함 시 5종)</li>\n          <li>ROLLBACK 가능 → TCL로 확정 필요</li>\n          <li>MERGE: INSERT + UPDATE 합쳐진 형태 (UPSERT)</li>\n        </ul>\n        <div class=\"ex-sql\">\n          <div class=\"ex-label\">예시</div>\n<pre><span class=\"kw\">INSERT INTO</span> EMP (ID, NAME, SAL) <span class=\"kw\">VALUES</span> (1, <span class=\"kw\">'홍길동'</span>, 3000);\n\n<span class=\"kw\">UPDATE</span> EMP <span class=\"kw\">SET</span> SAL = SAL * 1.1 <span class=\"kw\">WHERE</span> DEPT = 10;\n\n<span class=\"kw\">DELETE FROM</span> EMP <span class=\"kw\">WHERE</span> ID = 1;\n\n<span class=\"kw\">MERGE INTO</span> TGT <span class=\"kw\">USING</span> SRC <span class=\"kw\">ON</span> (TGT.ID = SRC.ID)\n  <span class=\"kw\">WHEN MATCHED THEN UPDATE SET</span> TGT.NAME = SRC.NAME\n  <span class=\"kw\">WHEN NOT MATCHED THEN INSERT</span> (ID, NAME) <span class=\"kw\">VALUES</span> (SRC.ID, SRC.NAME);</pre>\n        </div>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정 (단골)</div>\n          <ul>\n            <li><code>DELETE</code>는 DDL이다 → <strong>X (DML)</strong></li>\n            <li>DELETE WHERE 빠뜨림 → <strong>전체 삭제 위험</strong> (다만 ROLLBACK 가능)</li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">2-3-2</span><span class=\"card-title\">TCL</span></div>\n        <ul class=\"points\">\n          <li><strong>분류</strong>: COMMIT / ROLLBACK / SAVEPOINT</li>\n          <li>SAVEPOINT 후 <code>ROLLBACK TO SP</code> = SP 이후 작업만 취소</li>\n          <li>전체 ROLLBACK 또는 COMMIT 시 SAVEPOINT 소멸</li>\n        </ul>\n        <div class=\"ex-sql\">\n          <div class=\"ex-label\">예시</div>\n<pre><span class=\"kw\">INSERT INTO</span> EMP <span class=\"kw\">VALUES</span> (1, <span class=\"kw\">'A'</span>);\n<span class=\"kw\">SAVEPOINT</span> SP1;\n<span class=\"kw\">UPDATE</span> EMP <span class=\"kw\">SET</span> NAME = <span class=\"kw\">'B'</span> <span class=\"kw\">WHERE</span> ID = 1;\n<span class=\"kw\">ROLLBACK TO</span> SP1;   <span class=\"cm\">-- UPDATE만 취소 (INSERT는 살아있음)</span>\n<span class=\"kw\">COMMIT</span>;             <span class=\"cm\">-- INSERT 확정</span></pre>\n        </div>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>COMMIT 후 ROLLBACK 가능 → <strong>X (불가)</strong></li>\n            <li>DDL 실행 시 펜딩 트랜잭션 → <strong>자동 COMMIT됨</strong></li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">2-3-3</span><span class=\"card-title\">DDL</span></div>\n        <ul class=\"points\">\n          <li><strong>분류</strong>: CREATE / ALTER / DROP / TRUNCATE / RENAME</li>\n          <li><strong>자동 COMMIT</strong> — ROLLBACK 불가</li>\n          <li>제약조건: PRIMARY KEY / FOREIGN KEY / UNIQUE / NOT NULL / CHECK / DEFAULT</li>\n          <li>PK = UNIQUE + NOT NULL (1개) / UNIQUE는 NULL 허용 (여러 개)</li>\n        </ul>\n        <div class=\"ex-sql\">\n          <div class=\"ex-label\">예시</div>\n<pre><span class=\"kw\">CREATE TABLE</span> EMP (\n  ID      <span class=\"kw\">NUMBER PRIMARY KEY</span>,\n  NAME    <span class=\"kw\">VARCHAR2</span>(20) <span class=\"kw\">NOT NULL</span>,\n  EMAIL   <span class=\"kw\">VARCHAR2</span>(50) <span class=\"kw\">UNIQUE</span>,\n  SAL     <span class=\"kw\">NUMBER DEFAULT</span> 0 <span class=\"kw\">CHECK</span> (SAL &gt;= 0),\n  DEPT_ID <span class=\"kw\">NUMBER REFERENCES</span> DEPT(ID)\n);\n\n<span class=\"kw\">ALTER TABLE</span> EMP <span class=\"kw\">ADD</span> (HIRE_DT <span class=\"kw\">DATE</span>);\n<span class=\"kw\">ALTER TABLE</span> EMP <span class=\"kw\">MODIFY</span> (NAME <span class=\"kw\">VARCHAR2</span>(30));\n<span class=\"kw\">ALTER TABLE</span> EMP <span class=\"kw\">DROP COLUMN</span> EMAIL;\n\n<span class=\"kw\">TRUNCATE TABLE</span> EMP;   <span class=\"cm\">-- 전체 삭제, 자동 COMMIT</span>\n<span class=\"kw\">DROP TABLE</span> EMP;</pre>\n        </div>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정 (단골)</div>\n          <ul>\n            <li><code>TRUNCATE</code>는 DML이다 → <strong>X (DDL — ROLLBACK 불가)</strong></li>\n            <li>TRUNCATE는 WHERE 가능 → <strong>X (전체 삭제만)</strong></li>\n            <li>FK는 부모의 일반 컬럼 참조 가능 → <strong>X (PK or UNIQUE만)</strong></li>\n          </ul>\n        </div>\n      </div>\n\n      <div class=\"day-card\">\n        <div class=\"card-head\"><span class=\"card-tag\">2-3-4</span><span class=\"card-title\">DCL</span></div>\n        <ul class=\"points\">\n          <li><strong>분류</strong>: GRANT / REVOKE</li>\n          <li>시스템 권한: <strong>WITH ADMIN OPTION</strong> (ex: CREATE TABLE)</li>\n          <li>객체 권한: <strong>WITH GRANT OPTION</strong> (ex: SELECT ON EMP)</li>\n          <li>ROLE: 권한 묶음. GRANT/REVOKE 한 번에</li>\n        </ul>\n        <div class=\"ex-sql\">\n          <div class=\"ex-label\">예시</div>\n<pre><span class=\"cm\">-- 객체 권한</span>\n<span class=\"kw\">GRANT SELECT</span>, <span class=\"kw\">INSERT ON</span> EMP <span class=\"kw\">TO</span> USER1;\n<span class=\"kw\">GRANT SELECT ON</span> EMP <span class=\"kw\">TO</span> USER1 <span class=\"kw\">WITH GRANT OPTION</span>;\n\n<span class=\"cm\">-- 시스템 권한</span>\n<span class=\"kw\">GRANT CREATE TABLE TO</span> USER1 <span class=\"kw\">WITH ADMIN OPTION</span>;\n\n<span class=\"cm\">-- 회수</span>\n<span class=\"kw\">REVOKE SELECT ON</span> EMP <span class=\"kw\">FROM</span> USER1;\n\n<span class=\"cm\">-- ROLE</span>\n<span class=\"kw\">CREATE ROLE</span> DEV_ROLE;\n<span class=\"kw\">GRANT SELECT ON</span> EMP <span class=\"kw\">TO</span> DEV_ROLE;\n<span class=\"kw\">GRANT</span> DEV_ROLE <span class=\"kw\">TO</span> USER1;</pre>\n        </div>\n        <div class=\"traps\">\n          <div class=\"trap-label\">⚠ 함정</div>\n          <ul>\n            <li>GRANT는 자동 COMMIT 안 됨 → <strong>X (자동 COMMIT)</strong></li>\n            <li>REVOKE WITH GRANT OPTION → <strong>연쇄 회수</strong> (위임 받은 사용자도 잃음)</li>\n            <li>WITH ADMIN OPTION 회수 → <strong>위임 받은 사용자는 유지</strong> (객체 권한과 차이)</li>\n          </ul>\n        </div>\n      </div>\n    </div>\n  </section>\n\n  <!-- ── 당일 체크리스트 ── -->\n  <section class=\"day-section\" id=\"checklist\">\n    <h2><span class=\"num\">2</span>시험 당일 체크리스트</h2>\n    <p class=\"section-lead\">시험 1시간 전부터 종료까지의 흐름.</p>\n\n    <div class=\"checklist-grid\">\n      <div class=\"checklist-card\">\n        <div class=\"title\">📋 준비물</div>\n        <ul>\n          <li><strong>신분증</strong>(필수 — 없으면 응시 불가)</li>\n          <li>수험표 출력본</li>\n          <li>컴퓨터용 사인펜 또는 검정 볼펜 2자루</li>\n          <li>수정테이프(필요 시)</li>\n          <li>아날로그 시계(스마트워치 X)</li>\n        </ul>\n      </div>\n      <div class=\"checklist-card\">\n        <div class=\"title\">⏰ 시간 관리</div>\n        <ul>\n          <li>시험 시작 <strong>30분 전 도착</strong></li>\n          <li>총 <strong>50문항 / 90분</strong> (1문항 평균 1.8분)</li>\n          <li>막히면 일단 ① 표시 후 다음으로</li>\n          <li>마지막 5분 OMR 검토</li>\n        </ul>\n      </div>\n      <div class=\"checklist-card\">\n        <div class=\"title\">💪 컨디션</div>\n        <ul>\n          <li>전날 <strong>충분한 수면</strong>(밤샘 X)</li>\n          <li>아침 가벼운 식사</li>\n          <li>커피·차는 평소 양만</li>\n          <li>화장실 시험 전 미리</li>\n          <li>긴장될 땐 깊게 호흡 3번</li>\n        </ul>\n      </div>\n    </div>\n\n    <h3 class=\"subj-h\" style=\"margin-top: 28px;\">마음가짐</h3>\n    <div class=\"mindset-list\">\n      <div class=\"mindset-item\"><span class=\"star\">★</span>모르는 문제가 보여도 당황 X. 보기만 봐도 답이 좁혀지는 경우 많음.</div>\n      <div class=\"mindset-item\"><span class=\"star\">★</span>함정 패턴(NULL, ROWNUM, ON vs WHERE) 떠올리면서 침착하게.</div>\n      <div class=\"mindset-item\"><span class=\"star\">★</span>완전히 막히면 패스 후 끝까지 푼 다음 돌아오기.</div>\n      <div class=\"mindset-item\"><span class=\"star\">★</span>합격 기준 60점(60/100). <strong>킬러 문제 욕심 X</strong>, 풀 수 있는 문제부터.</div>\n    </div>\n  </section>\n\n  <div class=\"final-msg reveal\">\n    <div class=\"emoji\">🧅</div>\n    <div class=\"msg-title\">행운을 빕니다!</div>\n    <div class=\"msg-body\">\n      양파처럼 한 겹씩 차근차근 벗겨내며 풀어보세요.<br>\n      여러분의 합격을 응원합니다.\n    </div>\n  </div>";
