# 제보 검증 보고서 — 2026-09-21 (배치 1)

출처: SQLD양파 CS 시트 (1,481건, 2026-04-25 ~ 09-20). 미처리 1,468건.
문항 기준 251개 그룹으로 묶임. 이번 배치는 제보 수 상위 8개 그룹(제보 650건, 전체의 44%).

## 요약

| 판정 | 문항 |
|---|---|
| 수정 완료 (커밋됨) | 6 — R58 2/46, 2/28, 2/32, 2/44, R59 2/25, R60 1/3 |
| NEEDS_EXPERT (원문 확인 필요) | 2 — R57 2/48, R45 2/20 |

시트의 요일별 접수량은 8/16~8/22에 집중됨(8/21 하루 348건). 같은 문항을 같은 취지로 반복 제보한 경우가 많아 문항 단위로 합쳐 판정함.

---

## 1. R58 2과목 28번 — 제보 264건 — VALID (정답 키 오류)
- 제보 핵심: "정답이 1번인데 2번으로 표시된다"
- 검증: EMP = (11,1002) (10,1002) (11,1004) (NULL,1003).
  `ORDER BY EMPNO DESC NULLS LAST, DEPTNO ASC FETCH FIRST 3` → EMPNO 11 중 DEPTNO 오름차순이므로 (11,1002), (11,1004), (10,1002).
  이는 선택지 ①「11 1002 / 11 1004 / 10 1002」과 일치. 현재 correctIndex=1(②)은 오답.
- 해설도 오류: "그중 DEPTNO 오름차순으로 1002가 뒤에 온다" (실제로는 앞).
- 수정안: `correctIndex: 0`, 해설 →
  "EMPNO 내림차순(NULLS LAST)이므로 11, 11, 10 순이고, EMPNO가 같은 11 중에서는 DEPTNO 오름차순이라 1002가 먼저, 1004가 나중에 온다. 상위 3건은 (11,1002), (11,1004), (10,1002)로 ①번이다."

## 2. R58 2과목 32번 — 제보 65건 (이 중 2건은 다른 문항 제목) — VALID (정답 2개)
- 제보 핵심: "㉠㉡㉣ 모두 X라 1번, 3번이 중복 정답"
- 검증: ㉠ NVL(NULL,'X')=X, ㉡ COALESCE(NULL,'X')=X, ㉢ NULLIF('X','X')=NULL, ㉣ NULLIF('X','Y')=X.
  같은 결과 쌍: ㉠㉡, ㉡㉣ (㉠㉣도 동일) → 선택지 ①「㉠과 ㉡」, ③「㉡과 ㉣」이 모두 옳음.
- 수정안(원문 미확인 — 전문가 확인 후 적용): ㉣을 `SELECT NVL('Y', 'X')`(결과 Y)로 변경하면 정답이 ①만 남음.
  해설에 "NVL('Y','X')는 첫 인자가 NULL이 아니므로 Y" 추가.
- 제목이 "아래 SQL의 결과로 옳은 것은?"인 2건은 옛 번호 체계의 제보로 보이며 이 문항과 무관 (CANT_FIND 처리).

## 3. R58 2과목 44번 — 제보 80건 — VALID (정답 2개)
- 제보 핵심: "2번, 3번 복수정답"
- 검증: ② "ROW_NUMBER는 동일 값에도 항상 서로 다른 순번을 부여한다" 옳음.
  ③ "RANK와 DENSE_RANK는 동일 값에 동일 순위를 부여한다는 점은 같다" 역시 옳음. ①, ④는 틀림.
- 수정안: ③을 "RANK와 DENSE_RANK는 동일 값 다음 순위를 건너뛴다는 점이 같다."(DENSE_RANK는 건너뛰지 않으므로 틀림)로 교체.
  correctIndex=1 유지. 해설에 "DENSE_RANK는 다음 순위를 건너뛰지 않는다" 추가.

## 4. R58 2과목 46번 — 제보 33건 — VALID → 수정 완료
- 제보 핵심: "보기에 정답이 나와 있음"
- 검증: 보기 sql 블록에 각 함수의 결과와 "← 정답"이 그대로 표시됨.
- 조치: references 제거. 선택지·정답·해설은 그대로. 커밋 `9a92b29`. 로컬 화면에서 보기 블록이 사라진 것 확인.

## 5. R59 2과목 25번 — 제보 93건 — VALID (옳지 않은 것 3개)
- 제보 핵심: "옳지 않은 것이 3개(①②④)이다. '옳은 것'으로 바꾸고 답을 ③으로"
- 검증: CTAS는 컬럼 타입과 NOT NULL은 전달하고 PK/FK 등 제약은 전달하지 않음.
  ① 재정의 필요(거짓) ② NOT NULL 미전달(거짓) ③ PK/FK 미전달(참) ④ 타입 미전달(거짓).
  "옳지 않은 것"이면 정답이 3개가 되어 문제가 성립하지 않음. 현재 해설도 ①, ②, ④가 틀렸다고 설명함.
- 수정안: title →「CTAS(CREATE TABLE AS SELECT)에 대한 설명 중 옳은 것은?」, `correctIndex: 2`, 해설 →
  "CTAS는 컬럼 타입과 NOT NULL 제약은 그대로 전달하지만 기본키·외래키 등 다른 제약은 전달하지 않는다. 따라서 ③이 옳은 설명이다."

## 6. R60 1과목 3번 — 제보 61건 — VALID (옳지 않은 것 2개)
- 제보 핵심: "②도 틀린 설명이다(속성에는 하나의 값). 2, 3번 복수정답"
- 검증: 속성에는 하나의 값만 들어가므로 ② "두 개 이상의 값이 들어갈 수 있다"는 거짓.
  ③ "인스턴스는 두 가지의 속성값을 가진다"도 거짓 → 옳지 않은 것이 2개.
- 수정안: ②를 "하나의 속성에는 하나의 값만 들어갈 수 있다."로 교체(제보자 다수가 원문이라고 지적). correctIndex=2 유지.
  해설에 "속성은 하나의 속성값만 가진다(원자값)" 추가.

## 7. R57 2과목 48번 — 제보 52건 — NEEDS_EXPERT (문제 복원 필요)
- 제보 핵심: "(ㄱ)과 X가 보이지 않는다. 풀 수 없다"
- 검증: 보기가 `SELECT * FROM 회원 M ( ㄱ )`뿐이고 (ㄱ)에 들어갈 내용과 X 정의가 없음. 선택지는 이 정의를 전제로 함.
  해설로 추정하면 (ㄱ)은 `WHERE NOT EXISTS (SELECT X FROM 탈퇴 T WHERE T.회원ID = M.회원ID)`로 보이나 원문 확인 없이는 확정할 수 없음.
- 필요: 원문(기출 복원본)에서 보기 SQL 확인 → 확인 후 references 복원.

## 8. R45 2과목 20번 — 제보 34건 — NEEDS_EXPERT (재현 안 됨)
- 제보 핵심: "아래 트랜잭션이 안 보임 / 그림 안 보임"
- 검증: 로컬과 라이브(sqldyangpa.com) 모두 보기 블록이 정상 표시됨. 정답 ④(1, 4, 5)도 맞음.
  제보는 5월~9월 내내 꾸준히 들어와 일시적 장애로 보기 어려움. 보기가 `INSERT 1 ...`처럼 축약돼 있어 "실제 문장이 없다"는 뜻일 가능성이 큼.
- 필요: 원문에 구체 문장이 있었는지 확인. 없다면 표시 문제가 아닌 INVALID.

---

## 남은 작업
- 적용 완료(승인됨): 1, 2, 3, 5, 6번의 정답 키·선택지·해설 변경. 32번 ㉣ 변경은 원문 미확인 상태의 수정이므로 원문 확인 시 재검토
- 원문 확인 후: 7, 8번
- 다음 배치: 나머지 243개 그룹 (제보 수순)

---

# 배치 2 (제보 수 9~18위 그룹, 제보 243건)

## 요약
| 판정 | 문항 |
|---|---|
| 프론트엔드 원인 발견·수정 | R45 1/3(30), R45 2/20(34, 배치 1에서 이월), R46 1/3(23), R60 2/13(26 중 일부) |
| 수정 완료 (데이터) | R58 2/11(14), R59 2/32(19), R60 2/13(26) |
| 승인 대기 (정답 키·선택지 변경) | R52 2/14(31), R52 2/20(13), R52 2/48(15), R59 2/47(23) |
| NEEDS_EXPERT | R56 2/13(17) |

## A. "안 보인다" 제보의 공통 원인 — 이론 미니테스트가 보기 블록을 그리지 않음
- 증상: R45 2/20 "트랜잭션이 안 보임", R45 1/3 "[고객] 엔터티가 안 보임", R46 1/3 "ERD가 안 보임", R60 2/13 "결과 표가 안 보임" 등. 제보자는 대부분 데스크톱 Chrome이라 환경 문제가 아님.
- 확인: 같은 문항이 CBT 화면과 라이브 사이트에서는 정상 표시됨. 네 문항 모두 이론 페이지의 미니테스트(`src/data/miniTest/examMapping.ts`)에 포함돼 있음 (R45 3번→c112·c113, R46 3번→c115, R45 20번→c123·c231·c232, R60 13번→c227).
- 원인: `src/components/MiniTestSidebar.tsx` 가 references 중 `type === 'text'` 만 그리고 sql·table·erd·image 는 `null` 로 버림.
- 수정: `QuestionReferences` 컴포넌트로 교체 + 좁은 사이드바용 CSS. 이론 c227 페이지에서 표·SQL 보기가 표시되는 것을 확인함(데스크톱·모바일 폭).
- 영향: 미니테스트 문항 81개 전체에 해당. 이 수정으로 같은 성격의 제보가 함께 해소될 것으로 보임.

## B. 수정 완료 (데이터)
- **R58 2/11 (14건)**: 보기의 sql 블록에 각 함수 결과가 그대로 적혀 있어 정답 노출 → references 제거.
- **R59 2/32 (19건)**: 해설이 "③ ANY, ④ ALL 도 의미가 다르다"고 하여 ②③④ 모두 옳지 않은 듯 서술 → 서브쿼리가 MAX 단일 값이라 ANY/ALL 로 바꿔도 동일(옳은 설명)으로 정정. 정답 ②는 유지. 최종 결과가 공집합임도 명시.
- **R60 2/13 (26건)**: "아래 결과"가 없다는 제보. 기존 references 의 "급여 테이블"은 실제로는 PIVOT 이후의 결과 표였고 원본 급여 테이블(부서·직급·연봉)이 없었음 → 결과 표에서 역산해 원본 12행을 복원하고, 기존 표의 캡션을 "결과"로 바꿔 [급여 테이블 → SQL → 결과] 순으로 배치. (역산 복원이므로 원문과 대조 시 확인 권장)

## C. 승인 대기 — 정답 키·선택지 변경
1. **R52 2/14 (31건)**: `START WITH 매니저 IS NULL CONNECT BY (ㄱ) 사원 = (ㄴ) 매니저` 는 루트에서 아래로 내려가는 순방향이므로 `PRIOR 사원 = 매니저` → PRIOR 는 ㄱ. 현재 correctIndex=1(ㄴ)은 오답이고 해설 본문도 "PRIOR 사원"이라 쓰면서 ㄴ을 정답으로 적어 자기모순.
   수정안: `correctIndex: 0`, 해설 "루트(START WITH 매니저 IS NULL)에서 시작해 부모 행의 사원 값과 자식 행의 매니저 값이 일치하는 행을 따라 내려가는 순방향 전개이므로 PRIOR 는 사원 앞(ㄱ)에 온다."
2. **R52 2/20 (13건)**: "명령어와 분류가 올바르게 짝지어지지 않은 것은?" 에서 ② `SELECT — DQL` 도 SQLD 기준(SELECT 는 DML)으로는 틀린 짝 → 정답이 ①, ② 두 개.
   수정안: ② → `SELECT — DML`. 해설 "INSERT 는 DML 이므로 DCL 과 짝지어지지 않는다. SELECT 도 DML(데이터 조회)에 속한다." 정답 ① 유지.
3. **R52 2/48 (15건)**: `START WITH 매니저 IS NULL CONNECT BY (ㄷ) mgr = (ㄹ) empno` 는 `mgr = PRIOR empno` 가 순방향 → PRIOR 는 ㄹ. 현재 정답 ㄷ(index 2)은 역방향이라 루트 1행만 나옴. 또한 SQL 이 `SELECT 사원, 매니저` 로 표의 컬럼(empno, mgr)과 불일치.
   수정안: `correctIndex: 3`, SQL 의 SELECT 컬럼을 `empno, mgr` 로, 해설 "mgr = PRIOR empno 는 이전(부모) 행의 empno 가 현재 행의 mgr 와 같다는 뜻으로 부모→자식 순방향이다. 루트(mgr IS NULL)에서 시작하므로 PRIOR 는 ㄹ(empno 앞)에 온다."
4. **R59 2/47 (23건)**: M=MAX(VAL) OVER() → 100 전 행, S=SUM(VAL) OVER(ORDER BY ID) → 100~600 누적, F=FIRST_VALUE(...) → 100 전 행. 그런데 선택지 어디에도 이 조합이 없음(현재 정답 ④ "M=S=F=100"은 S 가 틀림).
   수정안: 선택지 ① → "M = 100 (모든 행), S = 누적 (100, 200, ..., 600), F = 100 (모든 행)", `correctIndex: 0`, 해설 재작성.

## D. NEEDS_EXPERT
- **R56 2/13 (17건)**: "결과 행의 수가 나머지와 다른 것은? (양쪽에 매칭 안 되는 행 존재 가정)" — 이 가정에서는 INNER < LEFT·RIGHT < FULL 로 네 개 모두 다를 수 있어 문제가 성립하지 않음(제보 "1번도 답", "정답 없음"과 일치). 원본 문제(테이블 데이터)를 확인해야 함.

---

# 배치 3 (제보 수 19~28위 그룹, 제보 112건)

## 요약
| 판정 | 문항 |
|---|---|
| 프론트엔드 수정으로 해소 | R48 1/6 "ERD 안 보임"(10) — 미니테스트 c125 에 포함된 문항 |
| 수정 완료 (데이터) | R48 1/6, R56 2/50(13), R59 2/15(11), R59 2/19(11), R59 2/45(10), R60 2/26(10), R60 2/46(12) |
| 승인 대기 | R49 2/20(12), R56 2/22(10), R56 1/7(13) |

## A. 수정 완료
- **R48 1/6**: ERD 보기의 instanceTable 이 "식별자 위치 매핑 (옵션 정답 순서와 일치)"이라는 이름으로 ①~④ 의 정답 분류를 그대로 보여주고 있었음 → 제거. ERD 의 ①②③④ 표시는 유지되어 문제 풀이에는 지장 없음(화면 확인).
- **R56 2/50 (13건)**: SQL 주석에 각 단계의 행 상태와 결과(`-- 3`)가 적혀 있고 "최종 TAB1 상태" 표도 있음 → 주석과 최종 상태 표 제거. 정답 ③(3)은 계산으로 재확인(COL1=4 행: (4,2),(4,1),(4,1)).
- **R59 2/15 (11건)**: 제목이 "빈칸에 들어갈 SQL"이지만 보기에 빈칸이 없고 선택지가 완성된 SQL → "가장 적절한 SQL은?"으로 정정. 선택지·정답 그대로.
- **R59 2/19 (11건)**: SQL 주석에 "묵시적 커밋(30건 확정)", "마지막 INSERT 취소" 등 정답 과정이 적혀 있음 → 주석 제거. 정답 30 은 재확인(DDL 묵시적 커밋 후 20건 INSERT 롤백).
- **R59 2/45 (10건)**: SQL 아래 주석과 DEPT 표 캡션이 "서브쿼리 결과: NULL → UNKNOWN → 공집합"이라고 알려 줌 → 제거.
- **R60 2/26 (10건)**: 제목은 `ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW` 절을 묻는데 보기 쿼리는 `ROWS UNBOUNDED PRECEDING`(=정답 ①과 동일)이라 정답이 노출되고 문제와 불일치 → 쿼리를 제목의 절로 정정. 정답 ① 그대로.
- **R60 2/46 (12건)**: "상위 3 명"인데 정답 DENSE_RANK 는 동순위 때문에 4명을 조회 → 제목·해설을 "상위 3위까지"로 정정(선택지·정답 그대로).

## B. 승인 대기
1. **R49 2/20 (12건)**: "회원번호·상품별 집계와 회원번호별 소계, 전체 총계"는 (회원번호,상품)·(회원번호)·() 를 모두 산출하는 `ROLLUP(회원번호, 상품)` 이므로 정답은 ①. 현재 정답 ②(`회원번호, ROLLUP(상품)`)는 전체 총계가 없음.
   수정안: `correctIndex: 0`, 해설 "(회원번호, 상품) 세부, (회원번호) 소계, () 전체 총계를 모두 산출하는 것은 ROLLUP(회원번호, 상품) 이다. ②는 전체 총계가 빠지고, ③ CUBE 는 (상품) 소계까지 추가하며, ④ GROUPING SETS(회원번호, 상품) 은 세부 행이 없다."
2. **R56 2/22 (10건)**: ① 단순 CASE(NULL 등치 비교 → 항상 0) 만이 아니라 ④ `NVL(칼럼, -1)` 도 (칼럼이 NULL 이 아니면 원래 값을 반환하므로) ②③ 과 결과가 달라 "나머지와 다른 것"이 둘.
   수정안(원문 미확인): ④ → `NVL2(칼럼, 0, -1)` (NULL 이면 -1, 아니면 0 — ②③ 과 동일). 이러면 ①만 다름. 정답 ① 유지. 원문에서 ④가 무엇이었는지 확인 권장.
3. **R56 1/7 (13건)**: 보기 ERD 의 ㄴ 관계가 `의사 ||--o| 수술` 이라 수술마다 의사가 반드시 1명 필요 → ④ "의사가 집도하지 않는 수술이 있을 수 있다"도 거짓이 되어 옳지 않은 것이 ①, ④ 두 개.
   수정안(원문 미확인): ㄴ 을 `의사 |o--o| 수술` 로 바꿔 수술의 의사 참여를 선택으로 표현. ②③ 은 그대로 참. 원문 ERD 확인 권장.

---

# 배치 4 (제보 수 29~38위 그룹, 제보 80건)

## 요약
| 판정 | 문항 |
|---|---|
| 프론트엔드 수정으로 해소 (미니테스트) | R45 2/29(8), R45 2/33(8), R46 1/2(7) — "표·ERD가 안 보임" |
| 수정 완료 (데이터) | R59 2/14(9), R51 2/29(7) |
| INVALID | R55 2/28(7) |
| 승인 대기 | R47 2/40(8), R48 1/4(8), R57 2/11(8) |
| NEEDS_EXPERT | R51 2/38(9) |

## A. 프론트엔드 수정으로 해소
R45 2/29, R45 2/33, R46 1/2 는 모두 이론 미니테스트(c124, c124·c221, c121)에 포함된 문항이며, 제보 내용이 "표가 안 보임", "ERD가 안 보임"이라 배치 2의 미니테스트 보기 렌더링 수정과 같은 원인. CBT 화면에서는 정상. 데이터 문제 없음(정답도 재확인: R45 2/29 → (10+20+0)/3=10, R45 2/33 → NOT IN 에 NULL 포함 시 0건).

## B. 수정 완료
- **R59 2/14 (9건)**: 보기가 `NVL2(expr, a, b) -- expr이 NULL이면 b를 반환, NOT NULL이면 a를 반환` 으로 정답 근거를 그대로 적음 → references 제거.
- **R51 2/29 (7건)**: "나머지와 다른 것"이면 RANK(1,2,2,4)와 DENSE_RANK(1,2,2,3)가 모두 ROW_NUMBER·NTILE(1,2,3,4)와 다르게 보여 성립하지 않음. 해설은 "마지막 값이 다르다"는 기준으로 쓰여 있음 → 제목을 "마지막 행의 값이 나머지와 다른 것은?"으로 명확화. 정답 ②(DENSE_RANK: 3, 나머지는 4) 그대로.

## C. INVALID
- **R55 2/28 (7건)**: "4건이어야 한다" 제보. `WHERE 고객번호='XX' OR 고객번호='YY' AND 등급=1 AND 지역='서울'` 은 AND 가 OR 보다 우선하므로 XX 3건 + (YY·등급1·서울) 2건 = 5건이 맞음. 제보자는 `(XX OR YY) AND ...` 로 읽어 4건을 계산. 문제와 정답 정상.
  응대 초안: "AND 는 OR 보다 우선순위가 높아 조건이 `고객번호='XX' OR (고객번호='YY' AND 등급=1 AND 지역='서울')` 로 평가됩니다. 따라서 XX 3건과 YY 2건을 더한 5건이 정답입니다. 괄호가 있었다면 4건이 되지만 이 문제는 괄호가 없는 형태로 출제되었습니다."

## D. 승인 대기
1. **R47 2/40 (8건)**: ④ `CONCAT(CONCAT(NULL, 'BC'), 'E')` = 'BCE' 로 ①③('BCD')과 다르고 ②('EBCD')도 다름 → "나머지와 다른 것"이 둘. 제보 다수가 ④의 'E'가 'D'의 오타라고 지적.
   수정안: ④ → `CONCAT(CONCAT(NULL, 'BC'), 'D')`, 해설의 ④ 항목을 'BCD'로. 정답 ② 유지.
2. **R48 1/4 (8건)**: "옳지 않은 것"이 ③ 외에 ④ "UML 에서는 연관관계와 의존관계를 단일 표기법으로 표기한다"(②와 정면 모순, 해설도 UML 만 구분한다고 함)도 거짓 → 두 개.
   수정안(원문 미확인): ④ → "존재적 관계는 엔터티 간의 상태를, 행위적 관계는 엔터티 간 행위로 발생하는 관계를 나타낸다."(참). 정답 ③ 유지.
3. **R57 2/11 (8건)**: 보기 다 "결과가 5개 이상 행이 나올 수 있다"는 FETCH FIRST 5 ROWS ONLY 에서 5건이 나오면 참이 되어 정답 "가"만이라는 해설과 충돌. 보기 나 "3번째 행을 제외한다"도 OFFSET 3 이면 참이라는 지적.
   수정안(원문 미확인): 다 → "결과가 5개 초과 행이 나올 수 있다.", 나 → "3번째 행만 제외한다." 정답 가 유지.

## E. NEEDS_EXPERT
- **R51 2/38 (9건)**: 제목이 "SQL Server 에서"인데 선택지에 NVL, NULLS FIRST/LAST 가 있음(SQL Server 는 미지원). 제보자는 ④(NULLS LAST)가 정답이라고 주장하나 ② `NVL(COL, 0)` 도 데이터에 따라 다름. 원문의 DBMS 와 선택지를 확인해야 함.

---

# 배치 5 (제보 수 39~48위 그룹, 제보 53건)

## 요약
| 판정 | 문항 |
|---|---|
| 프론트엔드 수정으로 해소 | R46 2/29(5), R46 2/30(6) — 미니테스트 "SQL이 안 보임" |
| 프론트엔드 신규 수정 | 모바일에서 표·코드가 넓은 문항의 카드가 화면 밖으로 늘어나 다음 버튼이 가려짐 (R59 2/23 제보) |
| 수정 완료 (데이터) | R46 2/17(6), R58 2/38(5), R59 2/23(6) |
| INVALID / 재현 불가 | R56 2/34(5) |
| 승인 대기 | R47 2/29(5) |
| NEEDS_EXPERT | R53 2/39(5), R57 2/27(5), R60 1/9(5) |

## A. 프론트엔드
1. **미니테스트**: R46 2/29(c216), R46 2/30(c123·c211·c232·c233), R46 2/17(c223), R53 2/39(c228) 는 모두 미니테스트 포함 문항으로, "SQL이 안 보임/화면 안 나옴" 제보는 배치 2의 보기 렌더링 수정으로 해소.
2. **모바일 레이아웃 (신규)**: R59 2/23 제보 "아이폰 세로에서 오른쪽이 잘려 다음 문제로 못 넘어감".
   - 재현: 375px 폭에서 문항 카드가 499px 로 늘어나 다음 버튼 오른쪽 끝이 499px(화면 밖).
   - 원인: `src/index.css` 의 `@media (max-width: 900px)` 에서 `.cbt-grid { grid-template-columns: 1fr !important }` 가 JSX 의 `minmax(0, 1fr)` 을 덮어씀. `1fr` 은 `min-width: auto` 라 폭이 넓은 표·코드가 열을 밀어냄.
   - 수정: `minmax(0, 1fr)` 로 변경. 수정 후 카드 319px, 다음 버튼 오른쪽 347px(375px 안), 표는 가로 스크롤.
   - 영향: 넓은 표·SQL 이 있는 모든 문항의 모바일 CBT 화면.

## B. 수정 완료 (데이터)
- **R46 2/17 (6건)**: 보기의 SQL 블록이 정답(`GROUP BY ROLLUP(매장ID, 매장명), 년도`)이었고 결과표 캡션도 그룹 구조를 설명해 정답 노출 → SQL 을 `( ? )` 빈칸 형태로, 캡션을 "출력 결과"로 정정. 정답 ②(ROLLUP)는 재확인.
- **R58 2/38 (5건)**: "반환 행 수는?" 은 1 이지만 정답·해설은 COUNT(*) 값 2 → 제목을 "실행 결과(COUNT(*) 값)로 옳은 것은?"으로 정정. 정답 ③(2) 그대로.
- **R59 2/23 (6건)**: 보기 표가 패턴별 매칭 결과('B.D'→'BCD', 'b.d'→NULL)를 그대로 보여줌 → 표 제거. SQL 빈칸은 유지.

## C. INVALID / 재현 불가
- **R56 2/34 (5건)**: "3번과 4번 선택지가 같다"는 제보. 화면(CBT)에서 4개 선택지 모두 서로 다름 확인: ③ `LEFT OUTER JOIN ... ON A.EMP_ID = B.MGR_ID`, ④ `... ON A.EMP_ID = B.EMP_ID`. 데이터는 5월 이후 변경 없음. 
  응대 초안: "③은 조인 조건이 `A.EMP_ID = B.MGR_ID`(사원과 그 사원이 관리하는 부하), ④는 `A.EMP_ID = B.EMP_ID`(자기 자신)로 서로 다른 선택지입니다. 셀프 조인으로 부하가 없는 사원까지 출력하려면 ③이 맞습니다."

## D. 승인 대기
- **R47 2/29 (5건)**: MERGE 결과 — A(1,0),(2,0), B(1,5),(3,7). 매칭(ID 1) → V=100, 비매칭 B(ID 3) → INSERT V=100. A 의 ID 2 는 그대로 0. 최종 A = (1,100),(2,0),(3,100) → "100, 0, 100". 현재 선택지에 이 조합이 없고 정답 ②(100,100,100)는 틀림.
  수정안: ② → "100, 0, 100", `correctIndex: 1` 유지, 해설 "ID 1 은 매칭되어 UPDATE 로 100, ID 2 는 B 에 없어 그대로 0, ID 3 은 매칭되지 않아 INSERT 로 100 이 되므로 100, 0, 100 이다."

## E. NEEDS_EXPERT
- **R53 2/39 (5건)**: 해설에 "원본 기출에서 구체적 SQL 표기와 선지 텍스트가 유실되어 정답 번호만 보존"이라고 적혀 있고 보기 SQL 이 없음. 원문에서 REGEXP_SUBSTR 식과 선지를 복원해야 함.
- **R57 2/27 (5건)**: `REGEXP_LIKE(요일, '^[^mw][[:lowercase:]]*[u]*day$', 'i')` 에 Tuesday·Sunday·Friday·Saturday 네 값이 모두 매칭되어 "조회되지 않는 값"이 없음(제보와 일치). 또한 `[[:lowercase:]]` 는 Oracle 에서 인정되는 POSIX 클래스가 아니어서(`[:lower:]`) 오류 가능성. 해설도 자기모순. 원문 패턴 확인 필요.
- **R60 1/9 (5건)**: "주식별자 선정과 가장 관련이 적은 정규화"의 정답이 3정규화(현재)냐 1정규화(제보)냐 이견. 교재 기준 확인 필요.
- (참고) **R46 2/30**: 제보 1건이 "SQL Server 는 기본 자동 커밋이라 ③도 틀림" 주장. 교재 기준(SQL Server 는 DDL 도 트랜잭션에 포함)을 따르는 문항이라 현재는 유지, 원문 확인 시 재검토.

---

# 배치 6 (제보 수 49~58위 그룹, 제보 43건)

## 요약
| 판정 | 문항 |
|---|---|
| 수정 완료 | R54 2/21(4), R54 2/38(4), R59 2/17(5), R52 2/32(5, 노출 메모만) |
| INVALID | R51 2/12(4), R57 2/23(4, "선택지 동일" 주장) |
| 승인 대기 | R56 2/32(5), R57 2/28(4) |
| NEEDS_EXPERT | R51 1/7(4), R52 2/32(SQL 유실), R55 2/26(4) |

## A. 수정 완료
- **R54 2/21 (4건)**: 선택지별 결과표 캡션이 "(정답 시나리오)", "(부분 카운트 오류)", "(조인 조건 오해)" 등 정답·오답 사유를 노출 → "보기 ① 결과" 등으로 정리.
- **R54 2/38 (4건)**: SQL 주석 `-- 정상`, `-- CHECK 조건 위반`, `-- PK 조건 위반` 이 정답 과정을 노출 → 제거.
- **R59 2/17 (5건)**: SQL 첫 줄 `-- 빈칸에 EXISTS 또는 INTERSECT 사용` 이 정답을 노출 → 제거.
- **R52 2/32 (5건)**: 보기 텍스트가 복원 메모("원본 PDF 표기 ... 정답: 4번")로 정답을 노출 → 제거. 단 이 문항은 원본 SQL 표와 테이블 T 의 데이터가 유실되어 풀 수 없는 상태(NEEDS_EXPERT).

## B. INVALID
- **R51 2/12 (4건)**: "정답이 3(RANGE)이어야 한다" 제보. 같은 일자(2023-11-02)가 두 행이므로 RANGE(및 기본값)는 같은 일자에 같은 누적값을 주고, ROWS 만 행마다 다른 누적값을 줌 → "일자별 누적합을 얻을 수 없는 것"은 ②(ROWS) 가 맞음. 정답·해설 정상.
  응대 초안: "일자별 누적합은 같은 일자의 행이 같은 누적값을 가져야 합니다. RANGE 는 동일 일자를 한 묶음으로 처리해 같은 값(500)을 주지만 ROWS 는 행마다 누적해 250, 500 으로 달라지므로 일자별 누적합을 얻을 수 없는 것은 ROWS 입니다."
- **R57 2/23 (4건)**: "1,2번과 3,4번이 같다" 제보. ①② 는 `ORDER BY AVG(SAL) > ...`, ③④ 는 `HAVING AVG(SAL) > ...` 로 절이 다르고 ANY/ALL 도 다름. 다만 제보 중 "문제가 요구하는 내용과 정답 SQL 의 의미가 어긋난다"는 지적은 타당성이 있음(부서 평균이 모든 부서 평균보다 큰 부서는 존재할 수 없어 결과가 빈다) — 원문 확인 시 재검토.

## C. 승인 대기
1. **R56 2/32 (5건)**: TAB1 (A,10),(B,20),(C,30), TAB2 (A,20),(B,20),(C,NULL). `(COL1, COL2) NOT IN (subquery)`: (A,10) 은 모든 비교가 확정적으로 불일치라 NOT IN 이 참 → 반환, (B,20) 은 일치 → 제외, (C,30) 은 (C,NULL) 과의 비교가 UNKNOWN → 제외. 결과는 **1행 (A,10)**. 현재 선택지 0/10/20/40 에 "1"이 없고 정답 0 은 틀림. 제보 "행 수를 묻는데 답이 10단위"와 일치 — 선택지는 결과 행의 COL2 값(10)으로 보임.
   수정안(원문 미확인): 제목 → "아래 쿼리 결과 행의 COL2 값은?", `correctIndex: 1`(10), 해설 → "(A,10) 은 TAB2 의 모든 행과 비교해 확정적으로 불일치이므로 NOT IN 이 참이라 반환된다. (B,20) 은 일치하여 제외되고, (C,30) 은 (C,NULL) 과의 비교가 UNKNOWN 이라 제외된다. 따라서 결과는 (A,10) 한 행이고 COL2 는 10 이다."
2. **R57 2/28 (4건)**: ③ "NULL 이 중복되지 않을 때만 NOT NULL 제약조건을 설정할 수 있다."는 뜻이 성립하지 않는 문장(NOT NULL 은 NULL 이 없어야 설정 가능). 정답 ①(디폴트 → 성능 저하)과 ③ 모두 "옳지 않은 것"이 됨. 제보 "2번도 틀림"은 NUMBER 크기 축소 가능 여부(데이터가 수용되면 가능)에 대한 이견.
   수정안(원문 미확인): ③ → "NULL 이 없을 때만 NOT NULL 제약조건을 설정할 수 있다." 정답 ① 유지. ②는 교재 기준 확인 권장.

## D. NEEDS_EXPERT
- **R51 1/7 (4건)**: "식별자의 분류가 올바르게 짝지어진 것은?" 인데 선택지가 분류 기준 없이 이름만 나열되어 있고 보기(분류 표)가 없음. 원문의 표·기준을 확인해야 함.
- **R52 2/32**: 위 A 참조. 테이블 T 데이터와 SQL 을 원문에서 복원해야 함.
- **R55 2/26 (4건)**: 제목의 "로그인 관련 쿼리"가 보기에 없음(references 없음). 선택지는 ALIAS, DISTINCT, 5일간 3회 로그인 등을 전제로 함. 원문 쿼리 복원 필요.

---

# 부록: 정답 노출 후보 전수 스캔 (스크립트 임시 실행)
SQL 주석과 캡션에서 결과·풀이·정답 표현을 검색한 결과 22개 문항이 후보로 나옴. 이 중 일부는 정상 사용(초기 상태 등)이라 문항별 판단이 필요함.
- R46 2/12, R47 2/42, R50 2/17·2/28·2/49, R52 1/2·2/35, R53 2/21, R54 2/15·2/30, R55 2/13·2/35·2/42, R56 2/30, R58 2/16·2/19·2/32·2/43·2/47, R59 2/16·2/19·2/23
- 제보 다발 문항: R58 2/47, R59 2/16, R54 2/15 등에서 "주석에 답이 있다"는 제보가 이미 존재

---

# 배치 7 (제보 수 59~70위 그룹, 제보 40건)

## 요약
| 판정 | 문항 |
|---|---|
| 프론트엔드 수정으로 해소 | R48 2/50(3) — 미니테스트 c234 "GRANT 구문 안 보임" |
| 수정 완료 | R58 1/8(4), R58 2/17(4 중 일부), R58 2/43(3), R60 2/14(3) |
| INVALID | R60 2/21(3) |
| 승인 대기 | R54 2/12(3), R55 2/33(3) |
| NEEDS_EXPERT | R54 1/9(4), R54 2/36(4), R56 1/4(3), R58 1/1(3) |

## A. 수정 완료
- **R58 1/8 (4건)**: 선택지별 ERD 캡션이 "1:1 관계", "자기 참조 관계", "Arc(상호배타적) 관계 — 차량은 법인/개인 소유자 중 하나에만 연결" 등으로 정답을 설명하고, ④ 의 관계 라벨이 "보유 (Arc)" 였음 → 캡션을 "보기 ①~④"로, 라벨의 "(Arc)" 를 제거. ④ 는 차량이 법인소유자·개인소유자 두 엔터티로 분기되는 구조로 식별 가능. 해설도 이에 맞게 수정. (mermaid 는 반원(Arc) 기호를 그릴 수 없어 구조로 구분해야 함 — 원문 이미지 확보 시 교체 권장)
- **R58 2/17 (제보 4건 중 해당 문항 1~2건)**: 해설이 "'PRIOR 자식 = 부모' 는 순방향"이라 적어 표준(PRIOR 부모 = 자식 → 순방향, PRIOR 자식 = 부모 → 역방향)과 반대 → 정정. 정답 ①("동시에 전개")이 옳지 않다는 결론은 그대로. 나머지 제보는 옛 번호 체계의 다른 문항 제목.
- **R58 2/43 (3건)**: SQL 주석 `-- 결과: T1 삭제 + T2 의 FK 제약 삭제`, `-- T2 테이블 자체는 그대로 남음` 이 정답 노출 → 제거.
- **R60 2/14 (3건)**: 선택지가 MIN_SAL 컬럼 값만 나열하는데 제목이 "결과로 올바른 것"이라 혼동("컬럼이 세 개인데 쿼리는 두 개") → 제목을 "SQL 실행 결과의 MIN_SAL 컬럼 값(행 순서대로)"으로 명확화. 정답 ①(1000 1000 1000)은 재확인.

## B. INVALID
- **R60 2/21 (3건)**: `ROWNUM IN (1, 2)` 는 첫 행(1)·둘째 행(2)이 모두 통과해 2건, `ROWNUM < 1` `ROWNUM > 1` `ROWNUM = 2` 는 0건 → 결과가 다른 것은 ①. 정답 정상. ("정답이 1이어야 하는데 4로 표시" 제보는 현재 데이터와 맞지 않음)

## C. 승인 대기
1. **R54 2/12 (3건)**: "뷰에 대한 설명으로 적절하지 않은 것은?" ① "뷰는 조회 속도를 높이기 위해 사용된다"는 뷰의 목적(독립성·편리성·보안)과 다른 명백한 오답. 현재 정답 ③ "컬럼이 추가될 때 뷰를 변경하지 않아도 응용 프로그램에 영향을 미치지 않는다"는 뷰의 독립성 설명으로 옳은 진술.
   수정안: `correctIndex: 0`, 해설 "뷰는 조회 속도를 높이려고 쓰는 객체가 아니라 논리적 독립성, 질의 단순화, 보안(접근 제어)을 위해 사용한다. 뷰는 실제 데이터를 저장하지 않고 다른 테이블을 참조하며, 기본 테이블에 컬럼이 추가되어도 뷰 정의를 바꾸지 않으면 응용 프로그램에 영향이 없다."
2. **R55 2/33 (3건)**: UNION ALL 컬럼명은 첫 SELECT 의 별칭(AAB, BBA)을 따르므로 ① 이 정답이지만 ④ "첫 번째 SELECT 별칭만 유효"도 같은 뜻의 옳은 진술 → 정답 2개.
   수정안(원문 미확인): ④ → "두 번째 SELECT 의 별칭이 우선 적용된다". 정답 ① 유지.

## D. NEEDS_EXPERT
- **R54 1/9 (4건)**: IE·Barker 표기법 설명. ②·③·④ 모두 표기법 해석이 교재마다 달라 제보자(실선/점선, O 기호)와 해설이 충돌. 해설도 "본 시험에서는 정답 ② 를 기준으로 보존"이라고 명시. 교재 기준 확인 필요.
- **R54 2/36 (4건)**: "행이 2건인 테이블, 결과가 나머지와 다른 SQL" — ① ROWNUM=1 (1행), ② ROWNUM=2 (0행), ③ ROWNUM<3 (2행), ④ ROWNUM<=2 (2행) 로 세 갈래라 "나머지와 다른 것"이 성립하지 않음. 원문 선택지 확인 필요.
- **R56 1/4 (3건)**: "아래 설명 중 옳지 않은 것은?" 이 고객·상품·주문·주문상품 ERD 를 전제하나 보기(ERD)가 없음(references 없음). 원문 ERD 복원 필요.
- **R58 1/1 (3건)**: ERD 가 회사→부서→사원→프로젝트 모두 1:M 식별자 관계 → 식별자 관계에서 종속 엔터티는 키(기본) 엔터티가 아니므로 회사만 키 엔터티이고 부서·사원·프로젝트가 모두 "해당하지 않는 것"이 되어 정답이 3개("정답이 3개" 제보와 일치). 원문의 관계 유형 확인 필요.

---

# 배치 8 (제보 수 71~85위 그룹, 제보 45건)

## 요약
| 판정 | 문항 |
|---|---|
| 프론트엔드 수정으로 해소 (미니테스트) | R45 2/31(3), R45 2/40(3), R45 2/48(3), R47 2/36(3), R55 2/23(3) — "SQL·표가 안 보임" |
| 프론트엔드 신규 수정 | 문항 텍스트의 `*` 가 사라지는 문제 (R57 2/18 제보) |
| 수정 완료 (데이터) | R51 2/28(3), R55 2/36(3), R57 2/33(3), R58 2/33(3), R60 2/32(3) |
| INVALID | R53 2/32(3), R58 1/3(2) |
| NEEDS_EXPERT | R52 2/11(3), R59 1/6(3) |

## A. 프론트엔드 (신규): 문항 텍스트의 `*` 가 사라짐
- 제보: R57 2/18 "2번 보기에 SELECT 다음에 아무것도 없다(*이 없다)".
- 원인: `renderInlineMd` 의 기울임 정규식 `\*([^*\n]+?)\*` 가 `SELECT * FROM A MINUS SELECT * FROM B` 의 두 별표 사이를 기울임으로 바꿔 별표 자체를 지움 → `SELECT  FROM A MINUS SELECT  FROM B`.
- 영향: 옵션·제목이 깨진 문항 5개 — R53 2/36 ③, R57 2/18 ②, R57 2/33 ④, R60 2/23 ④, R58 2/15 제목. 해설의 `3*3=9`, `b*c`, 정규식 `*` 도 함께 손상됨(8개 문항).
- 수정: `*italic*` 지원을 제거(굵게 `**`·코드만 유지). 문항·이론·미니테스트 데이터에서 의도적 기울임 사용은 없음을 확인. R57 2/18, 2/33 이 정상 표시됨을 화면에서 확인.

## B. 수정 완료 (데이터)
- **R51 2/28 (3건)**: 제목 "현재 행부터 이전 행까지의 누적합"이 정답 `ROWS UNBOUNDED PRECEDING`(첫 행부터 현재 행까지)과 모순 → "첫 행부터 현재 행까지의 누적합"으로 정정. 정답 ① 그대로.
- **R55 2/36 (3건)**: SQL 주석("(나)·(다) 취소", "(가)·(라)·(마) 영구 반영")과 "각 SQL 의 최종 운명" 표가 정답 전체를 노출 → 주석과 표 제거.
- **R57 2/33 (3건)**: 제목 "아래 데이터에서"인데 보기 데이터가 없음 → "Oracle 에서"로 정정. 정답 ④ 그대로.
- **R58 2/33 (3건)**: 데이터 표는 A/B 인데 선택지가 소문자(a a b b null) → 대문자로 통일. 정답 ②(A A B B NULL: 상세 4건 + 총계 1건)는 재확인.
- **R60 2/32 (3건)**: ① "이미 값이 들어 있는 NOT NULL 제약을 추가하는 경우" 는 뜻이 성립하지 않음(제보: "말이 끊겨 이해불가") → "이미 NULL 값이 들어 있는 컬럼에 NOT NULL 제약을 추가하는 경우". 정답 ④(TIMESTAMP 컬럼 추가) 그대로.

## C. INVALID
- **R53 2/32 (3건)**: "③④가 같다" 제보. ③ 은 각 집합에 **GROUP BY**, ④ 는 **ORDER BY** 를 쓸 수 있다는 서로 다른 진술(정답 ④ 가 옳지 않은 것). 응대 초안: "③은 GROUP BY, ④는 ORDER BY 에 대한 설명입니다. UNION 의 각 SELECT 에는 GROUP BY 는 쓸 수 있지만 ORDER BY 는 최종 결과에만 쓸 수 있어 ④가 옳지 않은 설명입니다."
- **R58 1/3 (2건)**: 제목이 옛 번호의 다른 문항. 현재 문항은 정답·선택지 정상.
- (참고) "선택지가 같다"는 제보가 R56 2/34, R57 2/23, R53 2/32 세 문항에서 반복. 세 문항 모두 선택지가 실제로 다르고 CBT 화면에서도 정상 표시됨. 특정 화면(모의고사 등)에서 옵션이 잘려 보이는지는 확인하지 못함.

## D. NEEDS_EXPERT
- **R52 2/11 (3건)**: `WHERE NOT (COL1 = 1 OR COL2 = NULL)` 은 표준 NULL 규칙으로 공집합인데 선택지에 없음. 해설도 "원본 기출의 모호한 케이스"라고 명시. `COL2 IS NULL` 로 읽으면 (3,C), (4,D) 두 행이 나와 제보("답이 두 개")와 일치. 원문 확인 필요.
- **R59 1/6 (3건)**: ② "비식별로 바꾸면 조회가 빠르다"와 ④ "B의 FK를 비식별자로 바꾸면 C에서 조인할 때 더 편하다"가 모두 옳지 않은 진술로 보임(제보: 정답은 2번). 원문 선택지 확인 필요.

---

# 배치 9 (제보 수 86~100위 그룹, 제보 30건)

## 요약
| 판정 | 문항 |
|---|---|
| 프론트엔드 수정으로 해소 (미니테스트) | R46 2/36(2) — c216 "SQL 이 안 보임" |
| 수정 완료 | R47 2/48(2), R52 1/9(2), R53 2/19(2), R54 2/15(2), R54 2/19(2), R56 2/18(2), R58 2/35(2) |
| INVALID | R54 2/40(2), R57 2/32(2), R52 2/22(2, 옛 번호), R52 2/38(2, 옛 번호), R58 2/23(2, 옛 번호) |
| 승인 대기 | R53 2/24(2) |
| NEEDS_EXPERT | R53 2/28(2) |

## A. 수정 완료
- **R47 2/48 (2건)**: 제목에 목적이 없어 빈칸(`GROUP BY`/`ASC·DESC`)을 정할 수 없음 → "부서별 연봉 합계를 구해 합계가 큰 순서대로 조회하려 한다"를 제목에 추가. 정답 ① 그대로.
- **R52 1/9 (2건)**: ERD 가 이미 정규화된 상태(연결 엔터티 수강·이수 포함)였고 캡션도 "[수강][이수] 두 개의 연결 엔터티 추가"라고 정답을 적음 → 교수-학생, 학생-과목 M:N 관계 ERD 로 교체하고 캡션 정리(화면 확인). 정답 ③(2개)는 그대로.
- **R53 2/19 (2건)**: "동순위를 포함하여 조회하는 함수"만으로는 DENSE_RANK 와 RANK 가 모두 해당 → 제목에 "동순위 다음 순위를 건너뛰지 않고 부여하는"을 추가. 정답 ① 그대로.
- **R54 2/15 (2건)**: SQL 아래 주석 `-- 100(KING) → 200(JONES) → 300(SCOTT)/400(FORD) 순방향 전개` 가 정답 근거를 노출 → 제거.
- **R54 2/19 (2건)**: 선택지 ① 끝의 "— 테이블 구분 없이 컬럼 참조"가 오답임을 알려줌 → 제거. 정답 ① 그대로.
- **R56 2/18 (2건)**: "행 수" 열이 COUNT 결과처럼 보여 "COUNT(*)가 30이 나올 수 없다"고 혼동 → 캡션을 "업체 테이블 (물품별 행 개수)"로 명확화. 정답 ②(3, 30)는 표의 p 가 30행이라는 전제에서 정상.
- **R58 2/35 (2건)**: `||` 의 NULL 처리는 DBMS 별로 다름 → 제목에 "(Oracle)" 명시. 정답 ① 그대로.

## B. INVALID
- **R54 2/40 (2건)**: "정답은 2번" 제보. A: (10,10,11), B: (20,20,NULL), C: (NULL,NULL,NULL) — B·C 의 SUM 이 NULL 이므로 "각 행별로 정상 집계"(②)는 틀리고 ④ 가 정답.
- **R57 2/32 (2건)**: "5,5,8,2 20"이라는 계산. INNER 1 + LEFT 4 + RIGHT 4 + FULL 7 = 16 이 맞음(공통 행은 G 하나).
- **R52 2/22, R52 2/38, R58 2/23**: 제보의 문제 제목이 현재 번호의 문항과 달라 옛 번호 체계의 제보. 현재 문항은 정답·표시 정상(R52 2/22: `'__L%'`, R52 2/38: DEFAULT 소급 적용 안 됨 → ④ 정답). 제보가 가리킨 원래 문항은 "PRIOR 위치"(R52 2/14·2/48, 승인 대기)와 "ON DELETE CASCADE"(R52 1/2 — 현재 정상).

## C. 승인 대기
- **R53 2/24 (2건)**: `(COL1, COL2) IN ((1,3),(1,4))` 과 동일한 조건은 ④ `COL1 = 1 AND (COL2 = 3 OR COL2 = 4)` 뿐 아니라 ③ `COL1 IN (1) AND COL2 IN (3, 4)` 도 동일(`COL1 IN (1)` ≡ `COL1 = 1`) → 정답 2개. 해설의 "③은 다른 조합도 매칭"은 틀림.
  수정안: ③ → `SELECT * FROM T WHERE COL1 IN (1, 2) AND COL2 IN (3, 4);`, 해설의 ③ 설명은 "(2,3), (2,4) 도 매칭하므로 다르다"로. 정답 ④ 유지.

## D. NEEDS_EXPERT
- **R53 2/28 (2건)**: 테이블 A(1,X), B(1,Y)를 NATURAL JOIN(ID·COL1 모두 일치해야 함) → 공집합(정답 ①은 정상). 그러나 오답 선택지 60·90·225 는 이 문제와 무관한 값으로 다른 문항의 선택지가 섞인 것으로 보임(제보 일치). 원문 선택지 확인 필요.

---

# 배치 10 (제보 수 101~125위 그룹, 제보 50건)

## 요약
| 판정 | 문항 |
|---|---|
| 프론트엔드 수정으로 해소 | R45 2/23(2), R45 2/35(2), R46 2/11(2), R47 2/23(2), R57 2/45(2) — 미니테스트 "테이블·SQL 안 보임"; R60 2/23(2) — `*` 표시 버그 |
| 수정 완료 | R51 2/36(2), R54 2/35(2), R55 2/37(2), R59 2/36(2), R60 2/24(2) |
| INVALID | R51 2/41, R52 2/33·2/44, R55 2/25, R56 2/14, R57 2/22, R58 2/12·2/34, R59 2/48, R60 2/15 |
| 승인 대기 | R49 2/25, R54 2/32, R59 2/40 |
| NEEDS_EXPERT | R53 2/40 |
| 기존 승인 대기 문항의 중복 제보 | R47 2/50 라벨의 제보 = R47 2/40 (④의 E→D 오타) |

## A. 프론트엔드
- 미니테스트 포함 문항(R45 2/23 c221, R45 2/35 c222, R46 2/11 c226, R47 2/23 c225, R57 2/45 c227)의 "테이블·SQL 이 안 보임" 제보 10건은 배치 2의 보기 렌더링 수정으로 해소.
- R60 2/23 "4번 보기 SELECT 와 FROM 사이에 아무것도 없음": 배치 8의 `*` 표시 수정으로 해소(선택지 ④ `SELECT * FROM A LEFT OUTER JOIN (SELECT * FROM B ...)`).

## B. 수정 완료
- **R51 2/36 (2건)**: 선택지에 LIMIT(MySQL)·TOP(SQL Server)·ROWNUM(Oracle)이 섞여 DBMS 를 알 수 없음 → 제목에 "Oracle 에서" 추가. 정답 ④(인라인 뷰 + ROWNUM) 그대로.
- **R54 2/35 (2건)**: "T2 테이블이 없다" 제보. 쿼리의 `FROM T1 T2` 는 자기 조인의 별칭 → 제목에 "(T2 는 T1 의 별칭)" 추가. 정답 ① 그대로(자기보다 점수가 높은 사람 수 + 1).
- **R55 2/37 (2건)**: 기본 NULL 정렬은 DBMS 별로 다름 → 제목에 "Oracle 에서" 추가. 정답 ②(ASC 시 NULLS LAST, DESC 시 NULLS FIRST) 그대로.
- **R59 2/36 (2건)**: `UNION ALL` 은 정렬하지 않아 결과가 A, B, C, B 순인데 선택지 ③ 이 정렬된 형태(A B B C) → "A B C B"로 정정. 정답 ③ 위치·의미 그대로.
- **R60 2/24 (2건)**: 기대 결과 표 캡션 "(PIVOT 후)"가 정답을 노출 → "기대 결과"로 정정.

## C. INVALID
- **R51 2/41 (2건)**: "2023-01-01 아닌가요". Oracle `TO_DATE('2023','YYYY')` 는 연도만 지정하면 월·일이 현재 월의 1일(SYSDATE=2023-05-10 → 2023-05-01)로 채워짐. 정답 ②(2023-05-01) 정상.
- **R52 2/44 (라벨 R52 2/33, 2건)**: "정답 1번"이라는 제보. `AVG(COL)` 만 NULL 을 제외해 15, 나머지는 모두 10 → 정답 ④ 정상.
- **R55 2/25 (2건)**: ROW_NUMBER() OVER (ORDER BY VAL DESC) → RN=1 이 (2,150) → ② 정상.
- **R56 2/14 (2건)**: ②③④ 는 "200 이 COL3~COL4 범위에 포함"으로 동치, ① 만 부등호 방향이 반대 → 정답 ①. 제보 두 건이 서로 반대 주장(4번 ↔ 1번).
- **R57 2/22 (2건)**: 100번 부서는 하위 노드(10의 자식). `PRIOR 상위 = 부서` 는 100→10→1 로 거슬러 올라가 최상위까지 탐색 → ④ 정상.
- **R58 2/12 (2건)**: SQL 의 `--` 는 두 쿼리를 구분하는 라벨이고 결과가 적혀 있지 않음. 정답 ③(㉠ 4건, ㉡ 1건) 정상.
- **R58 2/34 (2건)**: 표현 제안("결과 집합"). 정답 ② 정상(순서가 바뀌어도 결과 집합은 동일).
- **R59 2/48 (2건)**: "질의" 는 올바른 표현. CASE 는 첫 번째로 참인 조건('A') 반환 → 정답 ① 정상.
- **R60 2/15 (2건)**: "IN 은 OR 아닌가요". 다중 컬럼 IN `(A,B) IN ((x,y))` 는 A=x AND B=y 와 동일 → 정답 ② 정상.
  응대 초안: "단일 컬럼 IN 은 OR 로 풀리지만 (사번, 회원번호) 처럼 여러 컬럼을 묶은 IN 은 한 쌍이 동시에 일치해야 하므로 AND 조건입니다."
- **R58 2/23, R52 2/22 등**: 옛 번호 체계의 제보(문항 제목 불일치)는 정답·표시 정상.

## D. 승인 대기
1. **R49 2/25 (2건)**: 월별매출을 합산하면 홍길동 700, 이순신 700, 강감찬 550, 이상화 600, 이규혁 700 → RANK 는 1,1,1,4,5. 그러나 선택지·해설은 "700, 700, 550, 350, 350 → 1,1,3,4,4" 를 전제로 하며 데이터와 불일치(제보와 일치: 이순신 700, 이상화 600, 강감찬 550).
   수정안(데이터 복원 추정): 월별매출 002(이순신)·003(강감찬)·004(이상화) 의 세 달 매출을 합계가 550, 350, 350 이 되도록 조정하고(001, 005 는 이미 700 이므로 유지) 선택지·정답은 그대로. 원문 데이터 확인 시 재검토.
2. **R54 2/32 (2건)**: "연봉 오름차순, −100 ~ 200 사이의 연봉자 수" 의 값 범위(RANGE) 윈도우는 `ORDER BY SALARY` 가 있어야 하는데 ② `RANGE BETWEEN ...` 은 ORDER BY 가 없고 ④ 만 `ORDER BY SALARY RANGE BETWEEN 100 PRECEDING AND 200 FOLLOWING` 을 포함(제보: "ORDER BY가 2번에 없다").
   수정안: `correctIndex: 3`(④), 해설 "값 범위 기준 RANGE 윈도우는 ORDER BY 로 정렬 기준 컬럼이 지정되어야 한다. ②는 ORDER BY 가 없어 성립하지 않고 ④ 가 연봉 오름차순 정렬과 RANGE 범위를 함께 지정한다."
3. **R59 2/40 (2건)**: ① NATURAL JOIN 은 동일 컬럼명을 자동 조인 조건으로 사용, ② USING 절의 컬럼에는 별칭을 붙일 수 없다 — 둘 다 옳은 진술(해설도 "②도 사실"이라 인정) → 정답 2개.
   수정안: ② → "USING 절의 조인 컬럼에는 테이블 별칭을 붙여 참조할 수 있다."(거짓), 정답 ① 유지.

## E. NEEDS_EXPERT
- **R53 2/40 (2건)**: 보기가 "ROWS/RANGE 단위 키워드를 생략한 `BETWEEN 1 PRECEDING AND 1 FOLLOWING`"인데 Oracle 은 단위 키워드 생략을 허용하지 않고, 해설의 "생략 시 기본 단위 ROWS"도 사실과 다름(제보: 기본은 RANGE). 원문 보기 확인 필요.

---

# 승인 대기 19건 일괄 적용 (사용자 지시: 끝까지 진행)

정답 키 오류가 논리적으로 확정되는 것과 오탈자 성격이 분명한 것을 모두 적용. 아래 항목은 **원문 미확인 추정 수정**이므로 원문과 대조 권장: R56 2/22(④ NVL2), R56 1/7(ERD ㄴ 관계), R48 1/4(④ 문장), R57 2/11(보기 나·다), R47 2/29(② 100, 0, 100), R56 2/32(제목 COL2 값), R57 2/28(③), R53 2/24(③), R49 2/25(월별매출 3사원 데이터), R59 2/40(② 문장), R55 2/33(④ 문장).

| 문항 | 적용 내용 |
|---|---|
| R52 2/14 | 정답 ㄴ→ㄱ, 해설 정정 |
| R52 2/20 | ② `SELECT — DQL` → `SELECT — DML` |
| R52 2/48 | 정답 ㄷ→ㄹ, SQL 컬럼명을 empno, mgr 로 통일, 해설 정정 |
| R59 2/47 | ① 을 정답 조합으로 교체 |
| R49 2/20 | 정답 ②→① (ROLLUP(회원번호, 상품)) |
| R56 2/22 | ④ → `NVL2(칼럼, 0, -1)` |
| R56 1/7 | ERD ㄴ 관계를 `의사 \|o--o\| 수술` 로 |
| R47 2/40 | ④ 의 'E' → 'D' |
| R48 1/4 | ④ 를 참인 진술로 교체 |
| R57 2/11 | 보기 나·다 문장 정정 (3번째 행만 제외 / 5개 초과) |
| R47 2/29 | ② → 100, 0, 100 |
| R54 2/12 | 정답 ③→① |
| R55 2/33 | ④ → 두 번째 SELECT 의 별칭이 우선 적용된다 |
| R56 2/32 | 제목 "COL2 값", 정답 10 |
| R57 2/28 | ③ → NULL 이 없을 때만 NOT NULL 설정 가능 |
| R53 2/24 | ③ → `COL1 IN (1, 2) AND ...` |
| R49 2/25 | 002·003·004 월별매출을 합계 550/350/350 으로 조정 (선택지·정답 유지) |
| R54 2/32 | 정답 ②→④ |
| R59 2/40 | ② → USING 컬럼에 별칭을 붙여 참조할 수 있다(거짓) |

---

# 나머지 126개 그룹 (제보 1건씩) 일괄 처리

제목 매칭으로 현재 문항을 특정한 결과:
- **이미 수정한 문항에 대한 옛 번호 제보 29건** — R52 PRIOR 위치 계열(2/14·2/48), R49 ROLLUP 계열(2/20), R47 ④ E→D 계열(2/40), R58 2/32·2/44·2/46, R51 2/29, R49 2/25 등. 승인 대기 일괄 적용으로 함께 해소.
- **미니테스트·`*` 표시 프론트엔드 수정으로 해소 15건** — R45 1/1·1/2·2/12·2/13·2/26·2/28·2/42·2/44, R46 1/8·2/16·2/21·2/23·2/31, R49 2/46, R53 2/12. R48 2/47 "해설 답 틀림"은 해설의 `3*3=9, 1*3=3` 이 기울임 버그로 `3=9, 1` 로 깨져 보인 것 (해소).
- **제목 불일치(옛 번호) 6건** — R58 2/16·2/42(=R58 2/28 정답 키, 수정됨), R59 2/29, R47 2/34, R53 2/15, R51 2/22.
- **직접 수정 18건**

## 직접 수정 (18건)
| 문항 | 수정 |
|---|---|
| R58 2/47 | SQL 아래 주석 `-- 7902(FORD) → 7566(JONES) → 7839(KING) 순으로 거슬러 올라감` 제거 (정답 노출) |
| R50 2/28 | SQL 주석의 계산 결과(`= 4000`, `= 4`, `= 3`) 제거 (정답 노출) |
| R55 2/13 | 선택지별 결과표 캡션의 "(정답 시나리오)", "— ROW_NUMBER 적용 시 결과" 등 제거 |
| R59 2/16 | 결과표 캡션 "A 의 모든 행이 보존되고 B 매칭 부재 시 NULL"(LEFT JOIN 노출) → "기대 결과" |
| R54 2/30 | 제목 "COL2 만 NULL" → "COL1 에만 NULL"(표와 불일치), 결과표 캡션의 힌트 제거 |
| R47 2/42 | 선택지 ①②의 주문ID 가 기존 PK(501·502)와 중복되어 FK 가 아닌 PK 오류가 남 → 503·504 |
| R59 2/27 | 주문일자가 모두 달라 ROWS/RANGE(③④)를 구분할 수 없음 → 같은 일자(2025-01-08) 행 추가 |
| R54 2/36 | ①을 `ROWNUM > 0`(2건)으로 바꿔 "ROWNUM = 2 만 0건" 하나만 다르게 |
| R60 2/42 | ④ `SELECT NULL, NULL` 은 컬럼 수가 달라 애매 → `SELECT NULL` |
| R53 2/30 | ③ "15 이하의 모든 값"이 ①과 같은 뜻 → "50 이하의 모든 값" |
| R48 1/7 | ④ "속성의 분류에는 PK·FK·일반 속성이 있다"도 옳은 진술 → "속성은 인스턴스 없이 독립적으로 존재할 수 있다"(거짓) |
| R53 1/10 | 제목을 "필수 참여(인스턴스가 반드시 존재해야 함)를 표현하는 선의 형태"로 명확화 |
| R55 2/41 | "A, B 에 각각 중복 행이 없다고 가정" 전제를 제목에 추가 |
| R49 2/26 | 결과가 보이지 않는 문항 → "동순위가 있을 때 결과가 1, 1, 2 가 되는 순위 함수"로 명확화 |
| R51 2/40 | 제목의 "NULL 이 포함된 상황" ↔ 전제 "NULL 없음" 모순 → "포함되지 않은 상황" |
| R51 2/37 | 제목 "결과로 옳은 것" → "결과 건수로 옳은 것" (선택지가 건수) |
| R58 2/15 | 제목의 `*` 글머리표가 깨져 보임 → 쉼표 구분으로 정리 |
| R59 2/34 | 해설 "일의 자리에서 반올림" → "십의 자리로 반올림"(ROUND(136.2, -1)=140) |

## INVALID (제보자의 계산·개념 오해, 데이터 정상) — 응대 초안은 각 문항 정답 근거 참조
R60 1/1(행위 엔터티), R60 2/35(UNION 컬럼 4개), R58 2/48(CUBE 8행), R47 1/9, R58 2/40, R57 2/14, R58 2/26, R53 2/47, R57 2/25, R56 2/38, R57 2/30, R54 2/33, R58 2/22, R58 2/31, R56 1/8, R59 2/39, R57 2/44, R57 1/6, R57 2/21, R59 1/8(문항 수는 1과목 10 + 2과목 40 = 50), R60 2/20, R60 2/12, R60 1/10, R53 2/50, R51 2/30, R50 2/26, R57 1/10, R51 2/26, R55 1/5, R49 2/29, R57 2/15, R59 2/49, R55 2/49, R59 2/28, R55 2/50, R46 2/50, R51 1/1, R60 1/4, R49 2/11, R59 2/42, R59 2/11, R54 1/6(ERD 정상 렌더링 확인), R56 1/10, R57 2/41, R60 2/17, R60 1/2, R52 2/42

## 원문 확인이 필요한 항목 (자동 수정하지 않음)
- 문제 자체가 유실되어 복원 불가: **R57 2/48**(보기 (ㄱ)·X), **R52 2/32**(SQL·T 데이터), **R53 2/39**(REGEXP_SUBSTR 식), **R55 2/26**(로그인 쿼리), **R56 1/4**(ERD), **R51 1/7**(식별자 분류표)
- 문제가 성립하지 않아 원문 대조 필요: **R56 2/13**(조인 행 수 비교), **R51 2/38**(DBMS·선택지), **R57 2/27**(정규식 모두 매칭), **R60 1/9**(정규화), **R58 1/1**(식별자 관계 ERD), **R58 1/5**(③ 독립 존재), **R54 1/9**(Barker 표기), **R52 2/11**(`= NULL`), **R59 1/6**, **R53 2/40**(윈도우 단위 생략), **R53 2/28**(오답 선택지가 다른 문항 것), **R57 2/23**(정답 SQL 의미)

---

# 최종 요약

- 시트 제보 1,481건 → 문항 기준 251개 그룹 → **전부 검증 완료**
- **프론트엔드 버그 3건 수정**: 이론 미니테스트가 표·SQL·ERD·이미지 보기를 그리지 않던 문제(관련 제보 약 130건), 모바일에서 넓은 표·코드가 다음 버튼을 화면 밖으로 밀던 문제, 문항 텍스트의 `*` 가 기울임으로 처리되어 사라지던 문제
- **데이터 문항 83개 수정**(원본 대비 변경): 정답 키 오류 약 12건, 정답 중복·누락 약 18건, 정답 노출 제거 약 25건, 제목·선택지·캡션 명확화 나머지
- **원문 확인 필요 18건**은 수정하지 않고 위에 정리
- 검증: `validate-rounds` 오류 0건(경고 47건, 작업 전 48건), `npm run build` 성공, `tsc` 는 기존 `erd` 타입 오류만(이 브랜치에는 `chore/2026-09-21-typecheck-fix` 브랜치의 수정이 합쳐지지 않음)
- 추정으로 수정한 항목(원문 대조 권장)은 "승인 대기 19건 일괄 적용" 절에 표시
